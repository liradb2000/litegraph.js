var $jscomp = $jscomp || {};
$jscomp.scope = {};
$jscomp.ASSUME_ES5 = !1;
$jscomp.ASSUME_NO_NATIVE_MAP = !1;
$jscomp.ASSUME_NO_NATIVE_SET = !1;
$jscomp.defineProperty = $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties ? Object.defineProperty : function(r, l, y) {
  r != Array.prototype && r != Object.prototype && (r[l] = y.value);
};
$jscomp.getGlobal = function(r) {
  return "undefined" != typeof window && window === r ? r : "undefined" != typeof global && null != global ? global : r;
};
$jscomp.global = $jscomp.getGlobal(this);
$jscomp.SYMBOL_PREFIX = "jscomp_symbol_";
$jscomp.initSymbol = function() {
  $jscomp.initSymbol = function() {
  };
  $jscomp.global.Symbol || ($jscomp.global.Symbol = $jscomp.Symbol);
};
$jscomp.Symbol = function() {
  var r = 0;
  return function(l) {
    return $jscomp.SYMBOL_PREFIX + (l || "") + r++;
  };
}();
$jscomp.initSymbolIterator = function() {
  $jscomp.initSymbol();
  var r = $jscomp.global.Symbol.iterator;
  r || (r = $jscomp.global.Symbol.iterator = $jscomp.global.Symbol("iterator"));
  "function" != typeof Array.prototype[r] && $jscomp.defineProperty(Array.prototype, r, {configurable:!0, writable:!0, value:function() {
    return $jscomp.arrayIterator(this);
  }});
  $jscomp.initSymbolIterator = function() {
  };
};
$jscomp.arrayIterator = function(r) {
  var l = 0;
  return $jscomp.iteratorPrototype(function() {
    return l < r.length ? {done:!1, value:r[l++]} : {done:!0};
  });
};
$jscomp.iteratorPrototype = function(r) {
  $jscomp.initSymbolIterator();
  r = {next:r};
  r[$jscomp.global.Symbol.iterator] = function() {
    return this;
  };
  return r;
};
$jscomp.makeIterator = function(r) {
  $jscomp.initSymbolIterator();
  $jscomp.initSymbol();
  $jscomp.initSymbolIterator();
  var l = r[Symbol.iterator];
  return l ? l.call(r) : $jscomp.arrayIterator(r);
};
$jscomp.polyfill = function(r, l, y, n) {
  if (l) {
    y = $jscomp.global;
    r = r.split(".");
    for (n = 0; n < r.length - 1; n++) {
      var u = r[n];
      u in y || (y[u] = {});
      y = y[u];
    }
    r = r[r.length - 1];
    n = y[r];
    l = l(n);
    l != n && null != l && $jscomp.defineProperty(y, r, {configurable:!0, writable:!0, value:l});
  }
};
$jscomp.FORCE_POLYFILL_PROMISE = !1;
$jscomp.polyfill("Promise", function(r) {
  function l() {
    this.batch_ = null;
  }
  function y(h) {
    return h instanceof u ? h : new u(function(l, n) {
      l(h);
    });
  }
  if (r && !$jscomp.FORCE_POLYFILL_PROMISE) {
    return r;
  }
  l.prototype.asyncExecute = function(h) {
    null == this.batch_ && (this.batch_ = [], this.asyncExecuteBatch_());
    this.batch_.push(h);
    return this;
  };
  l.prototype.asyncExecuteBatch_ = function() {
    var h = this;
    this.asyncExecuteFunction(function() {
      h.executeBatch_();
    });
  };
  var n = $jscomp.global.setTimeout;
  l.prototype.asyncExecuteFunction = function(h) {
    n(h, 0);
  };
  l.prototype.executeBatch_ = function() {
    for (; this.batch_ && this.batch_.length;) {
      var h = this.batch_;
      this.batch_ = [];
      for (var l = 0; l < h.length; ++l) {
        var n = h[l];
        delete h[l];
        try {
          n();
        } catch (F) {
          this.asyncThrow_(F);
        }
      }
    }
    this.batch_ = null;
  };
  l.prototype.asyncThrow_ = function(h) {
    this.asyncExecuteFunction(function() {
      throw h;
    });
  };
  var u = function(h) {
    this.state_ = 0;
    this.result_ = void 0;
    this.onSettledCallbacks_ = [];
    var l = this.createResolveAndReject_();
    try {
      h(l.resolve, l.reject);
    } catch (C) {
      l.reject(C);
    }
  };
  u.prototype.createResolveAndReject_ = function() {
    function h(h) {
      return function(r) {
        n || (n = !0, h.call(l, r));
      };
    }
    var l = this, n = !1;
    return {resolve:h(this.resolveTo_), reject:h(this.reject_)};
  };
  u.prototype.resolveTo_ = function(h) {
    if (h === this) {
      this.reject_(new TypeError("A Promise cannot resolve to itself"));
    } else {
      if (h instanceof u) {
        this.settleSameAsPromise_(h);
      } else {
        a: {
          switch(typeof h) {
            case "object":
              var l = null != h;
              break a;
            case "function":
              l = !0;
              break a;
            default:
              l = !1;
          }
        }
        l ? this.resolveToNonPromiseObj_(h) : this.fulfill_(h);
      }
    }
  };
  u.prototype.resolveToNonPromiseObj_ = function(h) {
    var l = void 0;
    try {
      l = h.then;
    } catch (C) {
      this.reject_(C);
      return;
    }
    "function" == typeof l ? this.settleSameAsThenable_(l, h) : this.fulfill_(h);
  };
  u.prototype.reject_ = function(h) {
    this.settle_(2, h);
  };
  u.prototype.fulfill_ = function(h) {
    this.settle_(1, h);
  };
  u.prototype.settle_ = function(h, l) {
    if (0 != this.state_) {
      throw Error("Cannot settle(" + h + ", " + l | "): Promise already settled in state" + this.state_);
    }
    this.state_ = h;
    this.result_ = l;
    this.executeOnSettledCallbacks_();
  };
  u.prototype.executeOnSettledCallbacks_ = function() {
    if (null != this.onSettledCallbacks_) {
      for (var h = this.onSettledCallbacks_, l = 0; l < h.length; ++l) {
        h[l].call(), h[l] = null;
      }
      this.onSettledCallbacks_ = null;
    }
  };
  var z = new l;
  u.prototype.settleSameAsPromise_ = function(h) {
    var l = this.createResolveAndReject_();
    h.callWhenSettled_(l.resolve, l.reject);
  };
  u.prototype.settleSameAsThenable_ = function(h, l) {
    var n = this.createResolveAndReject_();
    try {
      h.call(l, n.resolve, n.reject);
    } catch (F) {
      n.reject(F);
    }
  };
  u.prototype.then = function(h, l) {
    function n(f, h) {
      return "function" == typeof f ? function(h) {
        try {
          r(f(h));
        } catch (w) {
          y(w);
        }
      } : h;
    }
    var r, y, z = new u(function(f, h) {
      r = f;
      y = h;
    });
    this.callWhenSettled_(n(h, r), n(l, y));
    return z;
  };
  u.prototype.catch = function(h) {
    return this.then(void 0, h);
  };
  u.prototype.callWhenSettled_ = function(h, l) {
    function n() {
      switch(r.state_) {
        case 1:
          h(r.result_);
          break;
        case 2:
          l(r.result_);
          break;
        default:
          throw Error("Unexpected state: " + r.state_);
      }
    }
    var r = this;
    null == this.onSettledCallbacks_ ? z.asyncExecute(n) : this.onSettledCallbacks_.push(function() {
      z.asyncExecute(n);
    });
  };
  u.resolve = y;
  u.reject = function(h) {
    return new u(function(l, n) {
      n(h);
    });
  };
  u.race = function(h) {
    return new u(function(l, n) {
      for (var r = $jscomp.makeIterator(h), u = r.next(); !u.done; u = r.next()) {
        y(u.value).callWhenSettled_(l, n);
      }
    });
  };
  u.all = function(h) {
    var l = $jscomp.makeIterator(h), n = l.next();
    return n.done ? y([]) : new u(function(h, r) {
      function u(l) {
        return function(n) {
          f[l] = n;
          z--;
          0 == z && h(f);
        };
      }
      var f = [], z = 0;
      do {
        f.push(void 0), z++, y(n.value).callWhenSettled_(u(f.length - 1), r), n = l.next();
      } while (!n.done);
    });
  };
  return u;
}, "es6", "es3");
$jscomp.executeAsyncGenerator = function(r) {
  function l(l) {
    return r.next(l);
  }
  function y(l) {
    return r.throw(l);
  }
  return new Promise(function(n, u) {
    function z(h) {
      h.done ? n(h.value) : Promise.resolve(h.value).then(l, y).then(z, u);
    }
    z(r.next());
  });
};
$jscomp.polyfill("Array.prototype.fill", function(r) {
  return r ? r : function(l, r, n) {
    var u = this.length || 0;
    0 > r && (r = Math.max(0, u + r));
    if (null == n || n > u) {
      n = u;
    }
    n = Number(n);
    0 > n && (n = Math.max(0, u + n));
    for (r = Number(r || 0); r < n; r++) {
      this[r] = l;
    }
    return this;
  };
}, "es6", "es3");
$jscomp.iteratorFromArray = function(r, l) {
  $jscomp.initSymbolIterator();
  r instanceof String && (r += "");
  var y = 0, n = {next:function() {
    if (y < r.length) {
      var u = y++;
      return {value:l(u, r[u]), done:!1};
    }
    n.next = function() {
      return {done:!0, value:void 0};
    };
    return n.next();
  }};
  n[Symbol.iterator] = function() {
    return n;
  };
  return n;
};
$jscomp.polyfill("Array.prototype.values", function(r) {
  return r ? r : function() {
    return $jscomp.iteratorFromArray(this, function(l, r) {
      return r;
    });
  };
}, "es8", "es3");
$jscomp.polyfill("Array.prototype.keys", function(r) {
  return r ? r : function() {
    return $jscomp.iteratorFromArray(this, function(l) {
      return l;
    });
  };
}, "es6", "es3");
$jscomp.owns = function(r, l) {
  return Object.prototype.hasOwnProperty.call(r, l);
};
$jscomp.polyfill("Object.values", function(r) {
  return r ? r : function(l) {
    var r = [], n;
    for (n in l) {
      $jscomp.owns(l, n) && r.push(l[n]);
    }
    return r;
  };
}, "es8", "es3");
(function(r) {
  function l(a) {
    f.debug && console.log("Graph created");
    this.list_of_graphcanvas = null;
    this.clear();
    a && this.configure(a);
  }
  function y(a, b, c, d, e, k) {
    this.id = a;
    this.type = b;
    this.origin_id = c;
    this.origin_slot = d;
    this.target_id = e;
    this.target_slot = k;
    this._data = null;
    this._pos = new Float32Array(2);
  }
  function n(a) {
    this._ctor(a);
  }
  function u(a) {
    this._ctor(a);
  }
  function z(a, b) {
    this.offset = new Float32Array([0, 0]);
    this.scale = 1;
    this.max_scale = 10;
    this.min_scale = 0.1;
    this.onredraw = null;
    this.enabled = !0;
    this.last_mouse = [0, 0];
    this.element = null;
    this.visible_area = new Float32Array(4);
    a && (this.element = a, b || this.bindEvents(a));
  }
  function h(a, b, c) {
    c = c || {};
    this.background_image = h.DEFAULT_BACKGROUND_IMAGE;
    a && a.constructor === String && (a = document.querySelector(a));
    this.ds = new z;
    this.zoom_modify_alpha = !0;
    this.title_text_font = "" + f.NODE_TEXT_SIZE + "px Arial";
    this.inner_text_font = "normal " + f.NODE_SUBTEXT_SIZE + "px Arial";
    this.node_title_color = f.NODE_TITLE_COLOR;
    this.default_link_color = f.LINK_COLOR;
    this.default_connection_color = {input_off:"#778", input_on:"#7F7", output_off:"#778", output_on:"#7F7"};
    this.highquality_render = !0;
    this.use_gradients = !1;
    this.editor_alpha = 1;
    this.pause_rendering = !1;
    this.clear_background = !0;
    this.read_only = !1;
    this.render_only_selected = !0;
    this.live_mode = !1;
    this.allow_searchbox = this.allow_interaction = this.allow_dragnodes = this.allow_dragcanvas = this.show_info = !0;
    this.drag_mode = this.allow_reconnect_links = !1;
    this.filter = this.dragging_rectangle = null;
    this.set_canvas_dirty_on_mouse_event = !0;
    this.always_render_background = !1;
    this.render_canvas_border = this.render_shadows = !0;
    this.render_connections_shadows = !1;
    this.render_connections_border = !0;
    this.render_connection_arrows = this.render_curved_connections = !1;
    this.render_collapsed_slots = !0;
    this.render_execution_order = !1;
    this.render_link_tooltip = this.render_title_colored = !0;
    this.links_render_mode = f.SPLINE_LINK;
    this.mouse = [0, 0];
    this.canvas_mouse = this.graph_mouse = [0, 0];
    this.onAfterChange = this.onBeforeChange = this.onConnectingChange = this.onSelectionChange = this.onNodeMoved = this.onDrawLinkTooltip = this.onDrawOverlay = this.onDrawForeground = this.onDrawBackground = this.onMouse = this.onSearchBoxSelection = this.onSearchBox = null;
    this.connections_width = 3;
    this.round_radius = 8;
    this.over_link_center = this.node_widget = this.current_node = null;
    this.last_mouse_position = [0, 0];
    this.visible_area = this.ds.visible_area;
    this.visible_links = [];
    b && b.attachCanvas(this);
    this.setCanvas(a);
    this.clear();
    c.skip_render || this.startRendering();
    this.autoresize = c.autoresize;
  }
  function K(a, b) {
    return Math.sqrt((b[0] - a[0]) * (b[0] - a[0]) + (b[1] - a[1]) * (b[1] - a[1]));
  }
  function C(a, b, c, d, e, k) {
    return c < a && c + e > a && d < b && d + k > b ? !0 : !1;
  }
  function F(a, b) {
    var c = a[0] + a[2], d = a[1] + a[3], e = b[1] + b[3];
    return a[0] > b[0] + b[2] || a[1] > e || c < b[0] || d < b[1] ? !1 : !0;
  }
  function G(a, b) {
    function c(a) {
      var c = parseInt(k.style.top);
      k.style.top = (c + a.deltaY * b.scroll_speed).toFixed() + "px";
      a.preventDefault();
      return !0;
    }
    this.options = b = b || {};
    var d = this;
    b.parentMenu && (b.parentMenu.constructor !== this.constructor ? (console.error("parentMenu must be of class ContextMenu, ignoring it"), b.parentMenu = null) : (this.parentMenu = b.parentMenu, this.parentMenu.lock = !0, this.parentMenu.current_submenu = this));
    var e = null;
    b.event && (e = b.event.constructor.name);
    "MouseEvent" !== e && "CustomEvent" !== e && "PointerEvent" !== e && (console.error("Event passed to ContextMenu is not of type MouseEvent or CustomEvent. Ignoring it."), b.event = null);
    var k = document.createElement("div");
    k.className = "litegraph litecontextmenu litemenubar-panel";
    b.className && (k.className += " " + b.className);
    k.style.minWidth = 100;
    k.style.minHeight = 100;
    k.style.pointerEvents = "none";
    setTimeout(function() {
      k.style.pointerEvents = "auto";
    }, 100);
    k.addEventListener("mouseup", function(a) {
      a.preventDefault();
      return !0;
    }, !0);
    k.addEventListener("contextmenu", function(a) {
      if (2 != a.button) {
        return !1;
      }
      a.preventDefault();
      return !1;
    }, !0);
    k.addEventListener("mousedown", function(a) {
      if (2 == a.button) {
        return d.close(), a.preventDefault(), !0;
      }
    }, !0);
    b.scroll_speed || (b.scroll_speed = 0.1);
    k.addEventListener("wheel", c, !0);
    k.addEventListener("mousewheel", c, !0);
    this.root = k;
    b.title && (e = document.createElement("div"), e.className = "litemenu-title", e.innerHTML = b.title, k.appendChild(e));
    for (var g = e = 0; g < a.length; g++) {
      var f = a.constructor == Array ? a[g] : g;
      null != f && f.constructor !== String && (f = void 0 === f.content ? String(f) : f.content);
      this.addItem(f, a[g], b);
      e++;
    }
    k.addEventListener("mouseleave", function(a) {
      d.lock || (k.closing_timer && clearTimeout(k.closing_timer), k.closing_timer = setTimeout(d.close.bind(d, a), 500));
    });
    k.addEventListener("mouseenter", function(a) {
      k.closing_timer && clearTimeout(k.closing_timer);
    });
    a = document;
    b.event && (a = b.event.target.ownerDocument);
    a || (a = document);
    a.fullscreenElement ? a.fullscreenElement.appendChild(k) : a.body.appendChild(k);
    e = b.left || 0;
    a = b.top || 0;
    b.event && (e = b.event.clientX - 10, a = b.event.clientY - 10, b.title && (a -= 20), b.parentMenu && (e = b.parentMenu.root.getBoundingClientRect(), e = e.left + e.width), g = document.body.getBoundingClientRect(), f = k.getBoundingClientRect(), 0 == g.height && console.error("document.body height is 0. That is dangerous, set html,body { height: 100%; }"), g.width && e > g.width - f.width - 10 && (e = g.width - f.width - 10), g.height && a > g.height - f.height - 10 && (a = g.height - f.height - 
    10));
    k.style.left = e + "px";
    k.style.top = a + "px";
    b.scale && (k.style.transform = "scale(" + b.scale + ")");
  }
  function I(a) {
    this.points = a;
    this.nearest = this.selected = -1;
    this.size = null;
    this.must_update = !0;
    this.margin = 5;
  }
  var f = r.LiteGraph = {VERSION:0.4, CANVAS_GRID_SIZE:10, NODE_TITLE_HEIGHT:30, NODE_TITLE_TEXT_Y:20, NODE_SLOT_HEIGHT:20, NODE_WIDGET_HEIGHT:20, NODE_WIDTH:140, NODE_MIN_WIDTH:50, NODE_COLLAPSED_RADIUS:10, NODE_COLLAPSED_WIDTH:80, NODE_TITLE_COLOR:"#999", NODE_SELECTED_TITLE_COLOR:"#FFF", NODE_TEXT_SIZE:14, NODE_TEXT_COLOR:"#AAA", NODE_SUBTEXT_SIZE:12, NODE_DEFAULT_COLOR:"#333", NODE_DEFAULT_BGCOLOR:"#353535", NODE_DEFAULT_BOXCOLOR:"#666", NODE_DEFAULT_SHAPE:"box", NODE_BOX_OUTLINE_COLOR:"#FFF", 
  DEFAULT_SHADOW_COLOR:"rgba(0,0,0,0.5)", DEFAULT_GROUP_FONT:24, WIDGET_BGCOLOR:"#222", WIDGET_OUTLINE_COLOR:"#666", WIDGET_TEXT_COLOR:"#DDD", WIDGET_SECONDARY_TEXT_COLOR:"#999", LINK_COLOR:"#9A9", EVENT_LINK_COLOR:"#A86", CONNECTING_LINK_COLOR:"#AFA", MAX_NUMBER_OF_NODES:1000, DEFAULT_POSITION:[100, 100], VALID_SHAPES:["default", "box", "round", "card"], BOX_SHAPE:1, ROUND_SHAPE:2, CIRCLE_SHAPE:3, CARD_SHAPE:4, ARROW_SHAPE:5, INPUT:1, OUTPUT:2, EVENT:-1, ACTION:-1, ALWAYS:0, ON_EVENT:1, NEVER:2, 
  ON_TRIGGER:3, UP:1, DOWN:2, LEFT:3, RIGHT:4, CENTER:5, STRAIGHT_LINK:0, LINEAR_LINK:1, SPLINE_LINK:2, NORMAL_TITLE:0, NO_TITLE:1, TRANSPARENT_TITLE:2, AUTOHIDE_TITLE:3, ASYNC:!1, proxy:null, node_images_path:"", debug:!1, catch_exceptions:!0, throw_errors:!0, allow_scripts:!1, registered_node_types:{}, node_types_by_file_extension:{}, Nodes:{}, Globals:{}, searchbox_extras:{}, registerNodeType:function(a, b) {
    if (!b.prototype) {
      throw "Cannot register a simple object, it must be a class with a prototype";
    }
    b.type = a;
    f.debug && console.log("Node registered: " + a);
    a.split("/");
    var c = b.name, d = a.lastIndexOf("/");
    b.category = a.substr(0, d);
    b.title || (b.title = c);
    if (b.prototype) {
      for (var e in n.prototype) {
        b.prototype[e] || (b.prototype[e] = n.prototype[e]);
      }
    }
    if (d = this.registered_node_types[a]) {
      console.log("replacing node type: " + a);
    } else {
      if (Object.hasOwnProperty(b.prototype, "shape") || Object.defineProperty(b.prototype, "shape", {set:function(a) {
        switch(a) {
          case "default":
            delete this._shape;
            break;
          case "box":
            this._shape = f.BOX_SHAPE;
            break;
          case "round":
            this._shape = f.ROUND_SHAPE;
            break;
          case "circle":
            this._shape = f.CIRCLE_SHAPE;
            break;
          case "card":
            this._shape = f.CARD_SHAPE;
            break;
          default:
            this._shape = a;
        }
      }, get:function(a) {
        return this._shape;
      }, enumerable:!0, configurable:!0}), b.prototype.onPropertyChange && console.warn("LiteGraph node class " + a + " has onPropertyChange method, it must be called onPropertyChanged with d at the end"), b.supported_extensions) {
        for (e in b.supported_extensions) {
          var k = b.supported_extensions[e];
          k && k.constructor === String && (this.node_types_by_file_extension[k.toLowerCase()] = b);
        }
      }
    }
    this.registered_node_types[a] = b;
    b.constructor.name && (this.Nodes[c] = b);
    if (f.onNodeTypeRegistered) {
      f.onNodeTypeRegistered(a, b);
    }
    if (d && f.onNodeTypeReplaced) {
      f.onNodeTypeReplaced(a, b, d);
    }
    b.prototype.onPropertyChange && console.warn("LiteGraph node class " + a + " has onPropertyChange method, it must be called onPropertyChanged with d at the end");
    if (b.supported_extensions) {
      for (e = 0; e < b.supported_extensions.length; e++) {
        (k = b.supported_extensions[e]) && k.constructor === String && (this.node_types_by_file_extension[k.toLowerCase()] = b);
      }
    }
  }, unregisterNodeType:function(a) {
    var b = a.constructor === String ? this.registered_node_types[a] : a;
    if (!b) {
      throw "node type not found: " + a;
    }
    delete this.registered_node_types[b.type];
    b.constructor.name && delete this.Nodes[b.constructor.name];
  }, wrapFunctionAsNode:function(a, b, c, d, e) {
    for (var k = Array(b.length), g = "", p = f.getParameterNames(b), h = 0; h < p.length; ++h) {
      g += "this.addInput('" + p[h] + "'," + (c && c[h] ? "'" + c[h] + "'" : "0") + ");\n";
    }
    g += "this.addOutput('out'," + (d ? "'" + d + "'" : 0) + ");\n";
    e && (g += "this.properties = " + JSON.stringify(e) + ";\n");
    c = Function(g);
    c.title = a.split("/").pop();
    c.desc = "Generated from " + b.name;
    c.prototype.onExecute = function() {
      for (var a = 0; a < k.length; ++a) {
        k[a] = this.getInputData(a);
      }
      a = b.apply(this, k);
      this.setOutputData(0, a);
    };
    this.registerNodeType(a, c);
  }, clearRegisteredTypes:function() {
    this.registered_node_types = {};
    this.node_types_by_file_extension = {};
    this.Nodes = {};
    this.searchbox_extras = {};
  }, addNodeMethod:function(a, b) {
    n.prototype[a] = b;
    for (var c in this.registered_node_types) {
      var d = this.registered_node_types[c];
      d.prototype[a] && (d.prototype["_" + a] = d.prototype[a]);
      d.prototype[a] = b;
    }
  }, createNode:function(a, b, c) {
    var d = this.registered_node_types[a];
    if (!d) {
      return f.debug && console.log('GraphNode type "' + a + '" not registered.'), null;
    }
    b = b || d.title || a;
    var e = null;
    if (f.catch_exceptions) {
      try {
        e = new d(b);
      } catch (g) {
        return console.error(g), null;
      }
    } else {
      e = new d(b);
    }
    e.type = a;
    !e.title && b && (e.title = b);
    e.properties || (e.properties = {});
    e.properties_info || (e.properties_info = []);
    e.flags || (e.flags = {});
    e.size || (e.size = e.computeSize());
    e.pos || (e.pos = f.DEFAULT_POSITION.concat());
    e.mode || (e.mode = f.ALWAYS);
    if (c) {
      for (var k in c) {
        e[k] = c[k];
      }
    }
    return e;
  }, getNodeType:function(a) {
    return this.registered_node_types[a];
  }, getNodeTypesInCategory:function(a, b) {
    var c = [], d;
    for (d in this.registered_node_types) {
      var e = this.registered_node_types[d];
      e.filter == b && ("" == a ? null == e.category && c.push(e) : e.category == a && c.push(e));
    }
    return c;
  }, getNodeTypesCategories:function(a) {
    var b = {"":1}, c;
    for (c in this.registered_node_types) {
      var d = this.registered_node_types[c];
      d.category && !d.skip_list && d.filter == a && (b[d.category] = 1);
    }
    a = [];
    for (c in b) {
      a.push(c);
    }
    return a;
  }, reloadNodes:function(a) {
    for (var b = document.getElementsByTagName("script"), c = [], d = 0; d < b.length; d++) {
      c.push(b[d]);
    }
    b = document.getElementsByTagName("head")[0];
    a = document.location.href + a;
    for (d = 0; d < c.length; d++) {
      var e = c[d].src;
      if (e && e.substr(0, a.length) == a) {
        try {
          f.debug && console.log("Reloading: " + e);
          var k = document.createElement("script");
          k.type = "text/javascript";
          k.src = e;
          b.appendChild(k);
          b.removeChild(c[d]);
        } catch (g) {
          if (f.throw_errors) {
            throw g;
          }
          f.debug && console.log("Error while reloading " + e);
        }
      }
    }
    f.debug && console.log("Nodes reloaded");
  }, cloneObject:function(a, b) {
    if (null == a) {
      return null;
    }
    a = JSON.parse(JSON.stringify(a));
    if (!b) {
      return a;
    }
    for (var c in a) {
      b[c] = a[c];
    }
    return b;
  }, isValidConnection:function(a, b) {
    if (!a || !b || a == b || a == f.EVENT && b == f.ACTION) {
      return !0;
    }
    a = String(a);
    b = String(b);
    a = a.toLowerCase();
    b = b.toLowerCase();
    if (-1 == a.indexOf(",") && -1 == b.indexOf(",")) {
      return a == b;
    }
    a = a.split(",");
    b = b.split(",");
    for (var c = 0; c < a.length; ++c) {
      for (var d = 0; d < b.length; ++d) {
        if (a[c] == b[d]) {
          return !0;
        }
      }
    }
    return !1;
  }, registerSearchboxExtra:function(a, b, c) {
    this.searchbox_extras[b.toLowerCase()] = {type:a, desc:b, data:c};
  }, fetchFile:function(a, b, c, d) {
    if (!a) {
      return null;
    }
    b = b || "text";
    if (a.constructor === String) {
      return "http" == a.substr(0, 4) && f.proxy && (a = f.proxy + a.substr(a.indexOf(":") + 3)), fetch(a).then(function(a) {
        if (!a.ok) {
          throw Error("File not found");
        }
        if ("arraybuffer" == b) {
          return a.arrayBuffer();
        }
        if ("text" == b || "string" == b) {
          return a.text();
        }
        if ("json" == b) {
          return a.json();
        }
        if ("blob" == b) {
          return a.blob();
        }
      }).then(function(a) {
        c && c(a);
      }).catch(function(b) {
        console.error("error fetching file:", a);
        d && d(b);
      });
    }
    if (a.constructor === File || a.constructor === Blob) {
      var e = new FileReader;
      e.onload = function(a) {
        a = a.target.result;
        "json" == b && (a = JSON.parse(a));
        c && c(a);
      };
      if ("arraybuffer" == b) {
        return e.readAsArrayBuffer(a);
      }
      if ("text" == b || "json" == b) {
        return e.readAsText(a);
      }
      if ("blob" == b) {
        return e.readAsBinaryString(a);
      }
    }
    return null;
  }};
  f.getTime = "undefined" != typeof performance ? performance.now.bind(performance) : "undefined" != typeof Date && Date.now ? Date.now.bind(Date) : "undefined" != typeof process ? function() {
    var a = process.hrtime();
    return 0.001 * a[0] + 1e-6 * a[1];
  } : function() {
    return (new Date).getTime();
  };
  r.LGraph = f.LGraph = l;
  l.supported_types = ["number", "string", "boolean"];
  l.prototype.getSupportedTypes = function() {
    return this.supported_types || l.supported_types;
  };
  l.STATUS_STOPPED = 1;
  l.STATUS_RUNNING = 2;
  l.prototype.clear = function() {
    this.stop();
    this.status = l.STATUS_STOPPED;
    this.last_link_id = this.last_node_id = 0;
    this._version = -1;
    if (this._nodes) {
      for (var a = 0; a < this._nodes.length; ++a) {
        var b = this._nodes[a];
        if (b.onRemoved) {
          b.onRemoved();
        }
      }
    }
    this._nodes = [];
    this._nodes_by_id = {};
    this._nodes_in_order = [];
    this._nodes_executable = null;
    this._groups = [];
    this.links = {};
    this.iteration = 0;
    this.config = {};
    this.vars = {};
    this.extra = {};
    this.fixedtime = this.runningtime = this.globaltime = 0;
    this.elapsed_time = this.fixedtime_lapse = 0.01;
    this.starttime = this.last_update_time = 0;
    this.catch_errors = !0;
    this.inputs = {};
    this.outputs = {};
    this.change();
    this.sendActionToCanvas("clear");
  };
  l.prototype.attachCanvas = function(a) {
    if (a.constructor != h) {
      throw "attachCanvas expects a LGraphCanvas instance";
    }
    a.graph && a.graph != this && a.graph.detachCanvas(a);
    a.graph = this;
    this.list_of_graphcanvas || (this.list_of_graphcanvas = []);
    this.list_of_graphcanvas.push(a);
  };
  l.prototype.detachCanvas = function(a) {
    if (this.list_of_graphcanvas) {
      var b = this.list_of_graphcanvas.indexOf(a);
      -1 != b && (a.graph = null, this.list_of_graphcanvas.splice(b, 1));
    }
  };
  l.prototype.start = function(a) {
    return f.ASYNC ? this.asyncStart(a) : this.syncStart(a);
  };
  l.prototype.syncStart = function(a) {
    if (this.status != l.STATUS_RUNNING) {
      this.status = l.STATUS_RUNNING;
      if (this.onPlayEvent) {
        this.onPlayEvent();
      }
      this.sendEventToAllNodes("onStart");
      this.last_update_time = this.starttime = f.getTime();
      a = a || 0;
      var b = this;
      if (0 == a && "undefined" != typeof window && window.requestAnimationFrame) {
        var c = function() {
          if (-1 == b.execution_timer_id) {
            window.requestAnimationFrame(c);
            if (b.onBeforeStep) {
              b.onBeforeStep();
            }
            b.syncRunStep(1, !b.catch_errors);
            if (b.onAfterStep) {
              b.onAfterStep();
            }
          }
        };
        this.execution_timer_id = -1;
        c();
      } else {
        this.execution_timer_id = setInterval(function() {
          if (b.onBeforeStep) {
            b.onBeforeStep();
          }
          b.syncRunStep(1, !b.catch_errors);
          if (b.onAfterStep) {
            b.onAfterStep();
          }
        }, a);
      }
    }
  };
  l.prototype.asyncStart = function(a) {
    if (this.status != l.STATUS_RUNNING) {
      this.status = l.STATUS_RUNNING;
      if (this.onPlayEvent) {
        this.onPlayEvent();
      }
      this.sendEventToAllNodes("onStart");
      this.last_update_time = this.starttime = f.getTime();
      a = a || 0;
      var b = this;
      if (0 == a && "undefined" != typeof window && window.requestAnimationFrame) {
        var c = function() {
          return $jscomp.executeAsyncGenerator(function() {
            function a(a, d, k) {
              for (;;) {
                switch(e) {
                  case 0:
                    if (-1 == b.execution_timer_id) {
                      e = 1;
                      break;
                    }
                    e = -1;
                    return {value:void 0, done:!0};
                  case 1:
                    window.requestAnimationFrame(c);
                    if (b.onBeforeStep) {
                      b.onBeforeStep();
                    }
                    e = 2;
                    return {value:b.asyncRunStep(1, !b.catch_errors), done:!1};
                  case 2:
                    if (1 != a) {
                      e = 3;
                      break;
                    }
                    e = -1;
                    throw k;
                  case 3:
                    if (b.onAfterStep) {
                      b.onAfterStep();
                    }
                    e = -1;
                  default:
                    return {value:void 0, done:!0};
                }
              }
            }
            var e = 0, k = {next:function(b) {
              return a(0.0, b, void 0);
            }, throw:function(b) {
              return a(1.0, void 0, b);
            }, return:function(a) {
              throw Error("Not yet implemented");
            }};
            $jscomp.initSymbolIterator();
            k[Symbol.iterator] = function() {
              return this;
            };
            return k;
          }());
        };
        this.execution_timer_id = -1;
        c();
      } else {
        this.execution_timer_id = setInterval(function() {
          return $jscomp.executeAsyncGenerator(function() {
            function a(a, d, e) {
              for (;;) {
                switch(c) {
                  case 0:
                    if (b.onBeforeStep) {
                      b.onBeforeStep();
                    }
                    c = 1;
                    return {value:b.asyncRunStep(1, !b.catch_errors), done:!1};
                  case 1:
                    if (1 != a) {
                      c = 2;
                      break;
                    }
                    c = -1;
                    throw e;
                  case 2:
                    if (b.onAfterStep) {
                      b.onAfterStep();
                    }
                    c = -1;
                  default:
                    return {value:void 0, done:!0};
                }
              }
            }
            var c = 0, k = {next:function(b) {
              return a(0.0, b, void 0);
            }, throw:function(b) {
              return a(1.0, void 0, b);
            }, return:function(a) {
              throw Error("Not yet implemented");
            }};
            $jscomp.initSymbolIterator();
            k[Symbol.iterator] = function() {
              return this;
            };
            return k;
          }());
        }, a);
      }
    }
  };
  l.prototype.stop = function() {
    if (this.status != l.STATUS_STOPPED) {
      this.status = l.STATUS_STOPPED;
      if (this.onStopEvent) {
        this.onStopEvent();
      }
      null != this.execution_timer_id && (-1 != this.execution_timer_id && clearInterval(this.execution_timer_id), this.execution_timer_id = null);
      this.sendEventToAllNodes("onStop");
    }
  };
  l.prototype.runStep = function(a, b, c, d) {
    return f.ASYNC ? this.asyncRunStep(a, b, c, d) : this.syncRunStep(a, b, c, d);
  };
  l.prototype.syncRunStep = function(a, b, c, d) {
    a = a || 1;
    d = f.getTime();
    this.globaltime = 0.001 * (d - this.starttime);
    var e = this._nodes_executable ? this._nodes_executable : this._nodes;
    if (e) {
      c = c || e.length;
      if (b) {
        for (var k = 0; k < a; k++) {
          for (var g = 0; g < c; ++g) {
            var p = e[g];
            if (p.mode == f.ALWAYS && p.onExecute) {
              p.onExecute();
            }
          }
          this.fixedtime += this.fixedtime_lapse;
          if (this.onExecuteStep) {
            this.onExecuteStep();
          }
        }
        if (this.onAfterExecute) {
          this.onAfterExecute();
        }
      } else {
        try {
          for (k = 0; k < a; k++) {
            for (g = 0; g < c; ++g) {
              if (p = e[g], p.mode == f.ALWAYS && p.onExecute) {
                p.onExecute();
              }
            }
            this.fixedtime += this.fixedtime_lapse;
            if (this.onExecuteStep) {
              this.onExecuteStep();
            }
          }
          if (this.onAfterExecute) {
            this.onAfterExecute();
          }
          this.errors_in_execution = !1;
        } catch (x) {
          this.errors_in_execution = !0;
          if (f.throw_errors) {
            throw x;
          }
          f.debug && console.log("Error during execution: " + x);
          this.stop();
        }
      }
      a = f.getTime();
      b = a - d;
      0 == b && (b = 1);
      this.execution_time = 0.001 * b;
      this.globaltime += 0.001 * b;
      this.iteration += 1;
      this.elapsed_time = 0.001 * (a - this.last_update_time);
      this.last_update_time = a;
    }
  };
  l.prototype.asyncRunStep = function(a, b, c, d) {
    var e = this;
    return $jscomp.executeAsyncGenerator(function() {
      function d(d, k, x) {
        for (;;) {
          switch(g) {
            case 0:
              a = a || 1;
              r = f.getTime();
              e.globaltime = 0.001 * (r - e.starttime);
              if (n = e._nodes_executable ? e._nodes_executable : e._nodes) {
                g = 1;
                break;
              }
              g = -1;
              return {value:void 0, done:!0};
            case 1:
              c = c || n.length;
              if (!b) {
                g = 2;
                break;
              }
              l = 0;
            case 4:
              if (!(l < a)) {
                g = 6;
                break;
              }
              m = 0;
            case 7:
              if (!(m < c)) {
                g = 9;
                break;
              }
              q = n[m];
              if (q.mode != f.ALWAYS || !q.onExecute) {
                g = 10;
                break;
              }
              g = 11;
              return {value:q.onExecute(), done:!1};
            case 11:
              if (1 != d) {
                g = 12;
                break;
              }
              g = -1;
              throw x;
            case 12:
            case 10:
            case 8:
              ++m;
              g = 7;
              break;
            case 9:
              if (e.fixedtime += e.fixedtime_lapse, e.onExecuteStep) {
                e.onExecuteStep();
              }
            case 5:
              l++;
              g = 4;
              break;
            case 6:
              if (e.onAfterExecute) {
                e.onAfterExecute();
              }
              g = 3;
              break;
            case 2:
              try {
                l = 0;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 15:
              try {
                if (!(l < a)) {
                  g = 17;
                  break;
                }
                m = 0;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 18:
              try {
                if (!(m < c)) {
                  g = 20;
                  break;
                }
                q = n[m];
                if (q.mode != f.ALWAYS || !q.onExecute) {
                  g = 21;
                  break;
                }
                g = 22;
                return {value:q.onExecute(), done:!1};
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 22:
              try {
                if (1 != d) {
                  g = 23;
                  break;
                }
                g = -1;
                throw x;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 23:
            case 21:
            case 19:
              try {
                ++m;
                g = 18;
                break;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 20:
              try {
                if (e.fixedtime += e.fixedtime_lapse, e.onExecuteStep) {
                  e.onExecuteStep();
                }
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 16:
              try {
                l++;
                g = 15;
                break;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 17:
              try {
                if (e.onAfterExecute) {
                  e.onAfterExecute();
                }
                e.errors_in_execution = !1;
                g = 14;
                break;
              } catch (D) {
                v = D;
                g = 13;
                break;
              }
            case 13:
              t = v;
              e.errors_in_execution = !0;
              if (!f.throw_errors) {
                g = 24;
                break;
              }
              g = -1;
              throw t;
            case 24:
              f.debug && console.log("Error during execution: " + t), e.stop();
            case 14:
            case 3:
              h = f.getTime(), p = h - r, 0 == p && (p = 1), e.execution_time = 0.001 * p, e.globaltime += 0.001 * p, e.iteration += 1, e.elapsed_time = 0.001 * (h - e.last_update_time), e.last_update_time = h, g = -1;
            default:
              return {value:void 0, done:!0};
          }
        }
      }
      var g = 0, p, h, t, q, m, l, v, n, r, A = {next:function(a) {
        return d(0.0, a, void 0);
      }, throw:function(a) {
        return d(1.0, void 0, a);
      }, return:function(a) {
        throw Error("Not yet implemented");
      }};
      $jscomp.initSymbolIterator();
      A[Symbol.iterator] = function() {
        return this;
      };
      return A;
    }());
  };
  l.prototype.updateExecutionOrder = function() {
    this._nodes_in_order = this.computeExecutionOrder(!1);
    this._nodes_executable = [];
    for (var a = 0; a < this._nodes_in_order.length; ++a) {
      this._nodes_in_order[a].onExecute && this._nodes_executable.push(this._nodes_in_order[a]);
    }
  };
  l.prototype.computeExecutionOrder = function(a, b) {
    for (var c = [], d = [], e = {}, k = {}, g = {}, p = 0, h = this._nodes.length; p < h; ++p) {
      var t = this._nodes[p];
      if (!a || t.onExecute) {
        e[t.id] = t;
        var q = 0;
        if (t.inputs) {
          for (var m = 0, l = t.inputs.length; m < l; m++) {
            t.inputs[m] && null != t.inputs[m].link && (q += 1);
          }
        }
        0 == q ? (d.push(t), b && (t._level = 1)) : (b && (t._level = 0), g[t.id] = q);
      }
    }
    for (; 0 != d.length;) {
      if (t = d.shift(), c.push(t), delete e[t.id], t.outputs) {
        for (p = 0; p < t.outputs.length; p++) {
          if (a = t.outputs[p], null != a && null != a.links && 0 != a.links.length) {
            for (m = 0; m < a.links.length; m++) {
              (h = this.links[a.links[m]]) && !k[h.id] && (q = this.getNodeById(h.target_id), null == q ? k[h.id] = !0 : (b && (!q._level || q._level <= t._level) && (q._level = t._level + 1), k[h.id] = !0, --g[q.id], 0 == g[q.id] && d.push(q)));
            }
          }
        }
      }
    }
    for (p in e) {
      c.push(e[p]);
    }
    c.length != this._nodes.length && f.debug && console.warn("something went wrong, nodes missing");
    h = c.length;
    for (p = 0; p < h; ++p) {
      c[p].order = p;
    }
    c = c.sort(function(a, b) {
      var c = a.constructor.priority || a.priority || 0, d = b.constructor.priority || b.priority || 0;
      return c == d ? a.order - b.order : c - d;
    });
    for (p = 0; p < h; ++p) {
      c[p].order = p;
    }
    return c;
  };
  l.prototype.getAncestors = function(a) {
    for (var b = [], c = [a], d = {}; c.length;) {
      var e = c.shift();
      if (e.inputs) {
        d[e.id] || e == a || (d[e.id] = !0, b.push(e));
        for (var k = 0; k < e.inputs.length; ++k) {
          var g = e.getInputNode(k);
          g && -1 == b.indexOf(g) && c.push(g);
        }
      }
    }
    b.sort(function(a, b) {
      return a.order - b.order;
    });
    return b;
  };
  l.prototype.arrange = function(a) {
    a = a || 100;
    for (var b = this.computeExecutionOrder(!1, !0), c = [], d = 0; d < b.length; ++d) {
      var e = b[d], k = e._level || 1;
      c[k] || (c[k] = []);
      c[k].push(e);
    }
    b = a;
    for (d = 0; d < c.length; ++d) {
      if (k = c[d]) {
        for (var g = 100, p = a + f.NODE_TITLE_HEIGHT, h = 0; h < k.length; ++h) {
          e = k[h], e.pos[0] = b, e.pos[1] = p, e.size[0] > g && (g = e.size[0]), p += e.size[1] + a + f.NODE_TITLE_HEIGHT;
        }
        b += g + a;
      }
    }
    this.setDirtyCanvas(!0, !0);
  };
  l.prototype.getTime = function() {
    return this.globaltime;
  };
  l.prototype.getFixedTime = function() {
    return this.fixedtime;
  };
  l.prototype.getElapsedTime = function() {
    return this.elapsed_time;
  };
  l.prototype.sendEventToAllNodes = function(a, b, c) {
    c = c || f.ALWAYS;
    var d = this._nodes_in_order ? this._nodes_in_order : this._nodes;
    if (d) {
      for (var e = 0, k = d.length; e < k; ++e) {
        var g = d[e];
        if (g.constructor === f.Subgraph && "onExecute" != a) {
          g.mode == c && g.sendEventToAllNodes(a, b, c);
        } else {
          if (g[a] && g.mode == c) {
            if (void 0 === b) {
              g[a]();
            } else {
              if (b && b.constructor === Array) {
                g[a].apply(g, b);
              } else {
                g[a](b);
              }
            }
          }
        }
      }
    }
  };
  l.prototype.sendActionToCanvas = function(a, b) {
    if (this.list_of_graphcanvas) {
      for (var c = 0; c < this.list_of_graphcanvas.length; ++c) {
        var d = this.list_of_graphcanvas[c];
        d[a] && d[a].apply(d, b);
      }
    }
  };
  l.prototype.add = function(a, b) {
    if (a) {
      if (a.constructor === u) {
        this._groups.push(a), this.setDirtyCanvas(!0), this.change(), a.graph = this, this._version++;
      } else {
        -1 != a.id && null != this._nodes_by_id[a.id] && (console.warn("LiteGraph: there is already a node with this ID, changing it"), a.id = ++this.last_node_id);
        if (this._nodes.length >= f.MAX_NUMBER_OF_NODES) {
          throw "LiteGraph: max number of nodes in a graph reached";
        }
        null == a.id || -1 == a.id ? a.id = ++this.last_node_id : this.last_node_id < a.id && (this.last_node_id = a.id);
        a.graph = this;
        this._version++;
        this._nodes.push(a);
        this._nodes_by_id[a.id] = a;
        if (a.onAdded) {
          a.onAdded(this);
        }
        this.config.align_to_grid && a.alignToGrid();
        b || this.updateExecutionOrder();
        if (this.onNodeAdded) {
          this.onNodeAdded(a);
        }
        this.setDirtyCanvas(!0);
        this.change();
        return a;
      }
    }
  };
  l.prototype.remove = function(a) {
    if (a.constructor === f.LGraphGroup) {
      var b = this._groups.indexOf(a);
      -1 != b && this._groups.splice(b, 1);
      a.graph = null;
      this._version++;
      this.setDirtyCanvas(!0, !0);
      this.change();
    } else {
      if (null != this._nodes_by_id[a.id] && !a.ignore_remove) {
        if (a.inputs) {
          for (b = 0; b < a.inputs.length; b++) {
            var c = a.inputs[b];
            null != c.link && a.disconnectInput(b);
          }
        }
        if (a.outputs) {
          for (b = 0; b < a.outputs.length; b++) {
            c = a.outputs[b], null != c.links && c.links.length && a.disconnectOutput(b);
          }
        }
        if (a.onRemoved) {
          a.onRemoved();
        }
        a.graph = null;
        this._version++;
        if (this.list_of_graphcanvas) {
          for (b = 0; b < this.list_of_graphcanvas.length; ++b) {
            c = this.list_of_graphcanvas[b], c.selected_nodes[a.id] && delete c.selected_nodes[a.id], c.node_dragged == a && (c.node_dragged = null);
          }
        }
        b = this._nodes.indexOf(a);
        -1 != b && this._nodes.splice(b, 1);
        delete this._nodes_by_id[a.id];
        if (this.onNodeRemoved) {
          this.onNodeRemoved(a);
        }
        this.sendActionToCanvas("checkPanels");
        this.setDirtyCanvas(!0, !0);
        this.change();
        this.updateExecutionOrder();
      }
    }
  };
  l.prototype.getNodeById = function(a) {
    return null == a ? null : this._nodes_by_id[a];
  };
  l.prototype.findNodesByClass = function(a, b) {
    b = b || [];
    for (var c = b.length = 0, d = this._nodes.length; c < d; ++c) {
      this._nodes[c].constructor === a && b.push(this._nodes[c]);
    }
    return b;
  };
  l.prototype.findNodesByType = function(a, b) {
    a = a.toLowerCase();
    b = b || [];
    for (var c = b.length = 0, d = this._nodes.length; c < d; ++c) {
      this._nodes[c].type.toLowerCase() == a && b.push(this._nodes[c]);
    }
    return b;
  };
  l.prototype.findNodeByTitle = function(a) {
    for (var b = 0, c = this._nodes.length; b < c; ++b) {
      if (this._nodes[b].title == a) {
        return this._nodes[b];
      }
    }
    return null;
  };
  l.prototype.findNodesByTitle = function(a) {
    for (var b = [], c = 0, d = this._nodes.length; c < d; ++c) {
      this._nodes[c].title == a && b.push(this._nodes[c]);
    }
    return b;
  };
  l.prototype.getNodeOnPos = function(a, b, c, d) {
    c = c || this._nodes;
    for (var e = c.length - 1; 0 <= e; e--) {
      var k = c[e];
      if (k.isPointInside(a, b, d)) {
        return k;
      }
    }
    return null;
  };
  l.prototype.getGroupOnPos = function(a, b) {
    for (var c = this._groups.length - 1; 0 <= c; c--) {
      var d = this._groups[c];
      if (d.isPointInside(a, b, 2, !0)) {
        return d;
      }
    }
    return null;
  };
  l.prototype.checkNodeTypes = function() {
    for (var a = 0; a < this._nodes.length; a++) {
      var b = this._nodes[a];
      if (b.constructor != f.registered_node_types[b.type]) {
        console.log("node being replaced by newer version: " + b.type);
        var c = f.createNode(b.type);
        this._nodes[a] = c;
        c.configure(b.serialize());
        c.graph = this;
        this._nodes_by_id[c.id] = c;
        b.inputs && (c.inputs = b.inputs.concat());
        b.outputs && (c.outputs = b.outputs.concat());
      }
    }
    this.updateExecutionOrder();
  };
  l.prototype.onAction = function(a, b) {
    this._input_nodes = this.findNodesByClass(f.GraphInput, this._input_nodes);
    for (var c = 0; c < this._input_nodes.length; ++c) {
      var d = this._input_nodes[c];
      if (d.properties.name == a) {
        d.onAction(a, b);
        break;
      }
    }
  };
  l.prototype.trigger = function(a, b) {
    if (this.onTrigger) {
      this.onTrigger(a, b);
    }
  };
  l.prototype.addInput = function(a, b, c) {
    if (!this.inputs[a]) {
      this.beforeChange();
      this.inputs[a] = {name:a, type:b, value:c};
      this._version++;
      this.afterChange();
      if (this.onInputAdded) {
        this.onInputAdded(a, b);
      }
      if (this.onInputsOutputsChange) {
        this.onInputsOutputsChange();
      }
    }
  };
  l.prototype.setInputData = function(a, b) {
    if (a = this.inputs[a]) {
      a.value = b;
    }
  };
  l.prototype.getInputData = function(a) {
    return (a = this.inputs[a]) ? a.value : null;
  };
  l.prototype.renameInput = function(a, b) {
    if (b != a) {
      if (!this.inputs[a]) {
        return !1;
      }
      if (this.inputs[b]) {
        return console.error("there is already one input with that name"), !1;
      }
      this.inputs[b] = this.inputs[a];
      delete this.inputs[a];
      this._version++;
      if (this.onInputRenamed) {
        this.onInputRenamed(a, b);
      }
      if (this.onInputsOutputsChange) {
        this.onInputsOutputsChange();
      }
    }
  };
  l.prototype.changeInputType = function(a, b) {
    if (!this.inputs[a]) {
      return !1;
    }
    if (!this.inputs[a].type || String(this.inputs[a].type).toLowerCase() != String(b).toLowerCase()) {
      if (this.inputs[a].type = b, this._version++, this.onInputTypeChanged) {
        this.onInputTypeChanged(a, b);
      }
    }
  };
  l.prototype.removeInput = function(a) {
    if (!this.inputs[a]) {
      return !1;
    }
    delete this.inputs[a];
    this._version++;
    if (this.onInputRemoved) {
      this.onInputRemoved(a);
    }
    if (this.onInputsOutputsChange) {
      this.onInputsOutputsChange();
    }
    return !0;
  };
  l.prototype.addOutput = function(a, b, c) {
    this.outputs[a] = {name:a, type:b, value:c};
    this._version++;
    if (this.onOutputAdded) {
      this.onOutputAdded(a, b);
    }
    if (this.onInputsOutputsChange) {
      this.onInputsOutputsChange();
    }
  };
  l.prototype.setOutputData = function(a, b) {
    if (a = this.outputs[a]) {
      a.value = b;
    }
  };
  l.prototype.getOutputData = function(a) {
    return (a = this.outputs[a]) ? a.value : null;
  };
  l.prototype.renameOutput = function(a, b) {
    if (!this.outputs[a]) {
      return !1;
    }
    if (this.outputs[b]) {
      return console.error("there is already one output with that name"), !1;
    }
    this.outputs[b] = this.outputs[a];
    delete this.outputs[a];
    this._version++;
    if (this.onOutputRenamed) {
      this.onOutputRenamed(a, b);
    }
    if (this.onInputsOutputsChange) {
      this.onInputsOutputsChange();
    }
  };
  l.prototype.changeOutputType = function(a, b) {
    if (!this.outputs[a]) {
      return !1;
    }
    if (!this.outputs[a].type || String(this.outputs[a].type).toLowerCase() != String(b).toLowerCase()) {
      if (this.outputs[a].type = b, this._version++, this.onOutputTypeChanged) {
        this.onOutputTypeChanged(a, b);
      }
    }
  };
  l.prototype.removeOutput = function(a) {
    if (!this.outputs[a]) {
      return !1;
    }
    delete this.outputs[a];
    this._version++;
    if (this.onOutputRemoved) {
      this.onOutputRemoved(a);
    }
    if (this.onInputsOutputsChange) {
      this.onInputsOutputsChange();
    }
    return !0;
  };
  l.prototype.triggerInput = function(a, b) {
    a = this.findNodesByTitle(a);
    for (var c = 0; c < a.length; ++c) {
      a[c].onTrigger(b);
    }
  };
  l.prototype.setCallback = function(a, b) {
    a = this.findNodesByTitle(a);
    for (var c = 0; c < a.length; ++c) {
      a[c].setTrigger(b);
    }
  };
  l.prototype.beforeChange = function(a) {
    if (this.onBeforeChange) {
      this.onBeforeChange(this, a);
    }
    this.sendActionToCanvas("onBeforeChange", this);
  };
  l.prototype.afterChange = function(a) {
    if (this.onAfterChange) {
      this.onAfterChange(this, a);
    }
    this.sendActionToCanvas("onAfterChange", this);
  };
  l.prototype.connectionChange = function(a, b) {
    this.updateExecutionOrder();
    if (this.onConnectionChange) {
      this.onConnectionChange(a);
    }
    this._version++;
    this.sendActionToCanvas("onConnectionChange");
  };
  l.prototype.isLive = function() {
    if (!this.list_of_graphcanvas) {
      return !1;
    }
    for (var a = 0; a < this.list_of_graphcanvas.length; ++a) {
      if (this.list_of_graphcanvas[a].live_mode) {
        return !0;
      }
    }
    return !1;
  };
  l.prototype.clearTriggeredSlots = function() {
    for (var a in this.links) {
      var b = this.links[a];
      b && b._last_time && (b._last_time = 0);
    }
  };
  l.prototype.change = function() {
    f.debug && console.log("Graph changed");
    this.sendActionToCanvas("setDirty", [!0, !0]);
    if (this.on_change) {
      this.on_change(this);
    }
  };
  l.prototype.setDirtyCanvas = function(a, b) {
    this.sendActionToCanvas("setDirty", [a, b]);
  };
  l.prototype.removeLink = function(a) {
    if (a = this.links[a]) {
      var b = this.getNodeById(a.target_id);
      b && b.disconnectInput(a.target_slot);
    }
  };
  l.prototype.serialize = function() {
    for (var a = [], b = 0, c = this._nodes.length; b < c; ++b) {
      a.push(this._nodes[b].serialize());
    }
    c = [];
    for (b in this.links) {
      var d = this.links[b];
      if (!d.serialize) {
        console.warn("weird LLink bug, link info is not a LLink but a regular object");
        var e = new y;
        for (k in d) {
          e[k] = d[k];
        }
        d = this.links[b] = e;
      }
      c.push(d.serialize());
    }
    var k = [];
    for (b = 0; b < this._groups.length; ++b) {
      k.push(this._groups[b].serialize());
    }
    a = {last_node_id:this.last_node_id, last_link_id:this.last_link_id, nodes:a, links:c, groups:k, config:this.config, extra:this.extra, version:f.VERSION};
    if (this.onSerialize) {
      this.onSerialize(a);
    }
    return a;
  };
  l.prototype.configure = function(a, b) {
    if (a) {
      b || this.clear();
      b = a.nodes;
      if (a.links && a.links.constructor === Array) {
        for (var c = [], d = 0; d < a.links.length; ++d) {
          var e = a.links[d];
          if (e) {
            var k = new y;
            k.configure(e);
            c[k.id] = k;
          } else {
            console.warn("serialized graph link data contains errors, skipping.");
          }
        }
        a.links = c;
      }
      for (d in a) {
        "nodes" != d && "groups" != d && (this[d] = a[d]);
      }
      c = !1;
      this._nodes = [];
      if (b) {
        d = 0;
        for (e = b.length; d < e; ++d) {
          k = b[d];
          var g = f.createNode(k.type, k.title);
          g || (f.debug && console.log("Node not found or has errors: " + k.type), g = new n, g.last_serialization = k, c = g.has_errors = !0);
          g.id = k.id;
          this.add(g, !0);
        }
        d = 0;
        for (e = b.length; d < e; ++d) {
          k = b[d], (g = this.getNodeById(k.id)) && g.configure(k);
        }
      }
      this._groups.length = 0;
      if (a.groups) {
        for (d = 0; d < a.groups.length; ++d) {
          b = new f.LGraphGroup, b.configure(a.groups[d]), this.add(b);
        }
      }
      this.updateExecutionOrder();
      this.extra = a.extra || {};
      if (this.onConfigure) {
        this.onConfigure(a);
      }
      this._version++;
      this.setDirtyCanvas(!0, !0);
      return c;
    }
  };
  l.prototype.load = function(a, b) {
    var c = this;
    if (a.constructor === File || a.constructor === Blob) {
      var d = new FileReader;
      d.addEventListener("load", function(a) {
        a = JSON.parse(a.target.result);
        c.configure(a);
        b && b();
      });
      d.readAsText(a);
    } else {
      var e = new XMLHttpRequest;
      e.open("GET", a, !0);
      e.send(null);
      e.onload = function(a) {
        200 !== e.status ? console.error("Error loading graph:", e.status, e.response) : (a = JSON.parse(e.response), c.configure(a), b && b());
      };
      e.onerror = function(a) {
        console.error("Error loading graph:", a);
      };
    }
  };
  l.prototype.onNodeTrace = function(a, b, c) {
  };
  y.prototype.configure = function(a) {
    a.constructor === Array ? (this.id = a[0], this.origin_id = a[1], this.origin_slot = a[2], this.target_id = a[3], this.target_slot = a[4], this.type = a[5]) : (this.id = a.id, this.type = a.type, this.origin_id = a.origin_id, this.origin_slot = a.origin_slot, this.target_id = a.target_id, this.target_slot = a.target_slot);
  };
  y.prototype.serialize = function() {
    return [this.id, this.origin_id, this.origin_slot, this.target_id, this.target_slot, this.type];
  };
  f.LLink = y;
  r.LGraphNode = f.LGraphNode = n;
  n.prototype._ctor = function(a) {
    this.title = a || "Unnamed";
    this.size = [f.NODE_WIDTH, 60];
    this.graph = null;
    this._pos = new Float32Array(10, 10);
    Object.defineProperty(this, "pos", {set:function(a) {
      !a || 2 > a.length || (this._pos[0] = a[0], this._pos[1] = a[1]);
    }, get:function() {
      return this._pos;
    }, enumerable:!0});
    this.id = -1;
    this.type = null;
    this.inputs = [];
    this.outputs = [];
    this.connections = [];
    this.properties = {};
    this.properties_info = [];
    this.flags = {};
  };
  n.prototype.configure = function(a) {
    this.graph && this.graph._version++;
    for (var b in a) {
      if ("properties" == b) {
        for (var c in a.properties) {
          if (this.properties[c] = a.properties[c], this.onPropertyChanged) {
            this.onPropertyChanged(c, a.properties[c]);
          }
        }
      } else {
        null != a[b] && ("object" == typeof a[b] ? this[b] && this[b].configure ? this[b].configure(a[b]) : this[b] = f.cloneObject(a[b], this[b]) : this[b] = a[b]);
      }
    }
    a.title || (this.title = this.constructor.title);
    if (this.onConnectionsChange) {
      if (this.inputs) {
        for (c = 0; c < this.inputs.length; ++c) {
          b = this.inputs[c];
          var d = this.graph ? this.graph.links[b.link] : null;
          this.onConnectionsChange(f.INPUT, c, !0, d, b);
        }
      }
      if (this.outputs) {
        for (c = 0; c < this.outputs.length; ++c) {
          var e = this.outputs[c];
          if (e.links) {
            for (b = 0; b < e.links.length; ++b) {
              d = this.graph ? this.graph.links[e.links[b]] : null, this.onConnectionsChange(f.OUTPUT, c, !0, d, e);
            }
          }
        }
      }
    }
    if (this.widgets) {
      for (c = 0; c < this.widgets.length; ++c) {
        (b = this.widgets[c]) && b.options && b.options.property && this.properties[b.options.property] && (b.value = JSON.parse(JSON.stringify(this.properties[b.options.property])));
      }
      if (a.widgets_values) {
        for (c = 0; c < a.widgets_values.length; ++c) {
          this.widgets[c] && (this.widgets[c].value = a.widgets_values[c]);
        }
      }
    }
    if (this.onConfigure) {
      this.onConfigure(a);
    }
  };
  n.prototype.serialize = function() {
    var a = {id:this.id, type:this.type, pos:this.pos, size:this.size, flags:f.cloneObject(this.flags), order:this.order, mode:this.mode};
    if (this.constructor === n && this.last_serialization) {
      return this.last_serialization;
    }
    this.inputs && (a.inputs = this.inputs);
    if (this.outputs) {
      for (var b = 0; b < this.outputs.length; b++) {
        delete this.outputs[b]._data;
      }
      a.outputs = this.outputs;
    }
    this.title && this.title != this.constructor.title && (a.title = this.title);
    this.properties && (a.properties = f.cloneObject(this.properties));
    if (this.widgets && this.serialize_widgets) {
      for (a.widgets_values = [], b = 0; b < this.widgets.length; ++b) {
        a.widgets_values[b] = this.widgets[b] ? this.widgets[b].value : null;
      }
    }
    a.type || (a.type = this.constructor.type);
    this.color && (a.color = this.color);
    this.bgcolor && (a.bgcolor = this.bgcolor);
    this.boxcolor && (a.boxcolor = this.boxcolor);
    this.shape && (a.shape = this.shape);
    this.onSerialize && this.onSerialize(a) && console.warn("node onSerialize shouldnt return anything, data should be stored in the object pass in the first parameter");
    return a;
  };
  n.prototype.clone = function() {
    var a = f.createNode(this.type);
    if (!a) {
      return null;
    }
    var b = f.cloneObject(this.serialize());
    if (b.inputs) {
      for (var c = 0; c < b.inputs.length; ++c) {
        b.inputs[c].link = null;
      }
    }
    if (b.outputs) {
      for (c = 0; c < b.outputs.length; ++c) {
        b.outputs[c].links && (b.outputs[c].links.length = 0);
      }
    }
    delete b.id;
    a.configure(b);
    return a;
  };
  n.prototype.toString = function() {
    return JSON.stringify(this.serialize());
  };
  n.prototype.getTitle = function() {
    return this.title || this.constructor.title;
  };
  n.prototype.setProperty = function(a, b) {
    this.properties || (this.properties = {});
    if (b !== this.properties[a]) {
      var c = this.properties[a];
      this.properties[a] = b;
      this.onPropertyChanged && !1 === this.onPropertyChanged(a, b, c) && (this.properties[a] = c);
      if (this.widgets) {
        for (c = 0; c < this.widgets.length; ++c) {
          var d = this.widgets[c];
          if (d && d.options.property == a) {
            d.value = b;
            break;
          }
        }
      }
    }
  };
  n.prototype.setOutputData = function(a, b) {
    if (this.outputs && !(-1 == a || a >= this.outputs.length)) {
      var c = this.outputs[a];
      if (c && (c._data = b, this.outputs[a].links)) {
        for (c = 0; c < this.outputs[a].links.length; c++) {
          var d = this.graph.links[this.outputs[a].links[c]];
          d && (d.data = b);
        }
      }
    }
  };
  n.prototype.setOutputDataType = function(a, b) {
    if (this.outputs && !(-1 == a || a >= this.outputs.length)) {
      var c = this.outputs[a];
      if (c && (c.type = b, this.outputs[a].links)) {
        for (c = 0; c < this.outputs[a].links.length; c++) {
          this.graph.links[this.outputs[a].links[c]].type = b;
        }
      }
    }
  };
  n.prototype.getInputData = function(a, b) {
    if (this.inputs && !(a >= this.inputs.length || null == this.inputs[a].link)) {
      a = this.graph.links[this.inputs[a].link];
      if (!a) {
        return null;
      }
      if (!b) {
        return a.data;
      }
      b = this.graph.getNodeById(a.origin_id);
      if (!b) {
        return a.data;
      }
      if (b.updateOutputData) {
        b.updateOutputData(a.origin_slot);
      } else {
        if (b.onExecute) {
          b.onExecute();
        }
      }
      return a.data;
    }
  };
  n.prototype.getInputDataType = function(a) {
    if (!this.inputs || a >= this.inputs.length || null == this.inputs[a].link) {
      return null;
    }
    a = this.graph.links[this.inputs[a].link];
    if (!a) {
      return null;
    }
    var b = this.graph.getNodeById(a.origin_id);
    return b ? (a = b.outputs[a.origin_slot]) ? a.type : null : a.type;
  };
  n.prototype.getInputDataByName = function(a, b) {
    a = this.findInputSlot(a);
    return -1 == a ? null : this.getInputData(a, b);
  };
  n.prototype.isInputConnected = function(a) {
    return this.inputs ? a < this.inputs.length && null != this.inputs[a].link : !1;
  };
  n.prototype.getInputInfo = function(a) {
    return this.inputs ? a < this.inputs.length ? this.inputs[a] : null : null;
  };
  n.prototype.getInputLink = function(a) {
    return this.inputs ? a < this.inputs.length ? this.graph.links[this.inputs[a].link] : null : null;
  };
  n.prototype.getInputNode = function(a) {
    if (!this.inputs || a >= this.inputs.length) {
      return null;
    }
    a = this.inputs[a];
    return a && null !== a.link ? (a = this.graph.links[a.link]) ? this.graph.getNodeById(a.origin_id) : null : null;
  };
  n.prototype.getInputOrProperty = function(a) {
    if (!this.inputs || !this.inputs.length) {
      return this.properties ? this.properties[a] : null;
    }
    for (var b = 0, c = this.inputs.length; b < c; ++b) {
      var d = this.inputs[b];
      if (a == d.name && null != d.link && (d = this.graph.links[d.link])) {
        return d.data;
      }
    }
    return this.properties[a];
  };
  n.prototype.getOutputData = function(a) {
    return !this.outputs || a >= this.outputs.length ? null : this.outputs[a]._data;
  };
  n.prototype.getOutputInfo = function(a) {
    return this.outputs ? a < this.outputs.length ? this.outputs[a] : null : null;
  };
  n.prototype.isOutputConnected = function(a) {
    return this.outputs ? a < this.outputs.length && this.outputs[a].links && this.outputs[a].links.length : !1;
  };
  n.prototype.isAnyOutputConnected = function() {
    if (!this.outputs) {
      return !1;
    }
    for (var a = 0; a < this.outputs.length; ++a) {
      if (this.outputs[a].links && this.outputs[a].links.length) {
        return !0;
      }
    }
    return !1;
  };
  n.prototype.getOutputNodes = function(a) {
    if (!this.outputs || 0 == this.outputs.length || a >= this.outputs.length) {
      return null;
    }
    a = this.outputs[a];
    if (!a.links || 0 == a.links.length) {
      return null;
    }
    for (var b = [], c = 0; c < a.links.length; c++) {
      var d = this.graph.links[a.links[c]];
      d && (d = this.graph.getNodeById(d.target_id)) && b.push(d);
    }
    return b;
  };
  n.prototype.trigger = function(a, b) {
    if (this.outputs && this.outputs.length) {
      this.graph && (this.graph._last_trigger_time = f.getTime());
      for (var c = 0; c < this.outputs.length; ++c) {
        var d = this.outputs[c];
        !d || d.type !== f.EVENT || a && d.name != a || this.triggerSlot(c, b);
      }
    }
  };
  n.prototype.triggerSlot = function(a, b, c) {
    if (this.outputs && (a = this.outputs[a]) && (a = a.links) && a.length) {
      this.graph && (this.graph._last_trigger_time = f.getTime());
      for (var d = 0; d < a.length; ++d) {
        var e = a[d];
        if (null == c || c == e) {
          var k = this.graph.links[a[d]];
          if (k && (k._last_time = f.getTime(), e = this.graph.getNodeById(k.target_id))) {
            if (k = e.inputs[k.target_slot], e.mode === f.ON_TRIGGER) {
              if (e.onExecute) {
                e.onExecute(b);
              }
            } else {
              if (e.onAction) {
                e.onAction(k.name, b);
              }
            }
          }
        }
      }
    }
  };
  n.prototype.clearTriggeredSlot = function(a, b) {
    if (this.outputs && (a = this.outputs[a]) && (a = a.links) && a.length) {
      for (var c = 0; c < a.length; ++c) {
        var d = a[c];
        if (null == b || b == d) {
          if (d = this.graph.links[a[c]]) {
            d._last_time = 0;
          }
        }
      }
    }
  };
  n.prototype.setSize = function(a) {
    this.size = a;
    if (this.onResize) {
      this.onResize(this.size);
    }
  };
  n.prototype.addProperty = function(a, b, c, d) {
    c = {name:a, type:c, default_value:b};
    if (d) {
      for (var e in d) {
        c[e] = d[e];
      }
    }
    this.properties_info || (this.properties_info = []);
    this.properties_info.push(c);
    this.properties || (this.properties = {});
    this.properties[a] = b;
    return c;
  };
  n.prototype.addOutput = function(a, b, c) {
    a = {name:a, type:b, links:null};
    if (c) {
      for (var d in c) {
        a[d] = c[d];
      }
    }
    this.outputs || (this.outputs = []);
    this.outputs.push(a);
    if (this.onOutputAdded) {
      this.onOutputAdded(a);
    }
    this.setSize(this.computeSize());
    this.setDirtyCanvas(!0, !0);
    return a;
  };
  n.prototype.addOutputs = function(a) {
    for (var b = 0; b < a.length; ++b) {
      var c = a[b], d = {name:c[0], type:c[1], link:null};
      if (a[2]) {
        for (var e in c[2]) {
          d[e] = c[2][e];
        }
      }
      this.outputs || (this.outputs = []);
      this.outputs.push(d);
      if (this.onOutputAdded) {
        this.onOutputAdded(d);
      }
    }
    this.setSize(this.computeSize());
    this.setDirtyCanvas(!0, !0);
  };
  n.prototype.removeOutput = function(a) {
    this.disconnectOutput(a);
    this.outputs.splice(a, 1);
    for (var b = a; b < this.outputs.length; ++b) {
      if (this.outputs[b] && this.outputs[b].links) {
        for (var c = this.outputs[b].links, d = 0; d < c.length; ++d) {
          var e = this.graph.links[c[d]];
          e && --e.origin_slot;
        }
      }
    }
    this.setSize(this.computeSize());
    if (this.onOutputRemoved) {
      this.onOutputRemoved(a);
    }
    this.setDirtyCanvas(!0, !0);
  };
  n.prototype.addInput = function(a, b, c) {
    a = {name:a, type:b || 0, link:null};
    if (c) {
      for (var d in c) {
        a[d] = c[d];
      }
    }
    this.inputs || (this.inputs = []);
    this.inputs.push(a);
    this.setSize(this.computeSize());
    if (this.onInputAdded) {
      this.onInputAdded(a);
    }
    this.setDirtyCanvas(!0, !0);
    return a;
  };
  n.prototype.addInputs = function(a) {
    for (var b = 0; b < a.length; ++b) {
      var c = a[b], d = {name:c[0], type:c[1], link:null};
      if (a[2]) {
        for (var e in c[2]) {
          d[e] = c[2][e];
        }
      }
      this.inputs || (this.inputs = []);
      this.inputs.push(d);
      if (this.onInputAdded) {
        this.onInputAdded(d);
      }
    }
    this.setSize(this.computeSize());
    this.setDirtyCanvas(!0, !0);
  };
  n.prototype.removeInput = function(a) {
    this.disconnectInput(a);
    for (var b = this.inputs.splice(a, 1), c = a; c < this.inputs.length; ++c) {
      if (this.inputs[c]) {
        var d = this.graph.links[this.inputs[c].link];
        d && --d.target_slot;
      }
    }
    this.setSize(this.computeSize());
    if (this.onInputRemoved) {
      this.onInputRemoved(a, b[0]);
    }
    this.setDirtyCanvas(!0, !0);
  };
  n.prototype.addConnection = function(a, b, c, d) {
    a = {name:a, type:b, pos:c, direction:d, links:null};
    this.connections.push(a);
    return a;
  };
  n.prototype.computeSize = function(a) {
    function b(a) {
      return a ? d * a.length * 0.6 : 0;
    }
    if (this.constructor.size) {
      return this.constructor.size.concat();
    }
    var c = Math.max(this.inputs ? this.inputs.length : 1, this.outputs ? this.outputs.length : 1);
    a = a || new Float32Array([0, 0]);
    c = Math.max(c, 1);
    var d = f.NODE_TEXT_SIZE, e = b(this.title), k = 0, g = 0;
    if (this.inputs) {
      for (var p = 0, h = this.inputs.length; p < h; ++p) {
        var t = this.inputs[p];
        t = t.label || t.name || "";
        t = b(t);
        k < t && (k = t);
      }
    }
    if (this.outputs) {
      for (p = 0, h = this.outputs.length; p < h; ++p) {
        t = this.outputs[p], t = t.label || t.name || "", t = b(t), g < t && (g = t);
      }
    }
    a[0] = Math.max(k + g + 10, e);
    a[0] = Math.max(a[0], f.NODE_WIDTH);
    this.widgets && this.widgets.length && (a[0] = Math.max(a[0], 1.5 * f.NODE_WIDTH));
    a[1] = (this.constructor.slot_start_y || 0) + c * f.NODE_SLOT_HEIGHT;
    c = 0;
    if (this.widgets && this.widgets.length) {
      p = 0;
      for (h = this.widgets.length; p < h; ++p) {
        c = this.widgets[p].computeSize ? c + (this.widgets[p].computeSize(a[0])[1] + 4) : c + (f.NODE_WIDGET_HEIGHT + 4);
      }
      c += 8;
    }
    a[1] = this.widgets_up ? Math.max(a[1], c) : null != this.widgets_start_y ? Math.max(a[1], c + this.widgets_start_y) : a[1] + c;
    this.constructor.min_height && a[1] < this.constructor.min_height && (a[1] = this.constructor.min_height);
    a[1] += 6;
    return a;
  };
  n.prototype.getPropertyInfo = function(a) {
    var b = null;
    if (this.properties_info) {
      for (var c = 0; c < this.properties_info.length; ++c) {
        if (this.properties_info[c].name == a) {
          b = this.properties_info[c];
          break;
        }
      }
    }
    this.constructor["@" + a] && (b = this.constructor["@" + a]);
    this.constructor.widgets_info && this.constructor.widgets_info[a] && (b = this.constructor.widgets_info[a]);
    !b && this.onGetPropertyInfo && (b = this.onGetPropertyInfo(a));
    b || (b = {});
    b.type || (b.type = typeof this.properties[a]);
    "combo" == b.widget && (b.type = "enum");
    return b;
  };
  n.prototype.addWidget = function(a, b, c, d, e) {
    this.widgets || (this.widgets = []);
    !e && d && d.constructor === Object && (e = d, d = null);
    e && e.constructor === String && (e = {property:e});
    d && d.constructor === String && (e || (e = {}), e.property = d, d = null);
    d && d.constructor !== Function && (console.warn("addWidget: callback must be a function"), d = null);
    b = {type:a.toLowerCase(), name:b, value:c, callback:d, options:e || {}};
    void 0 !== b.options.y && (b.y = b.options.y);
    d || b.options.callback || b.options.property || console.warn("LiteGraph addWidget(...) without a callback or property assigned");
    if ("combo" == a && !b.options.values) {
      throw "LiteGraph addWidget('combo',...) requires to pass values in options: { values:['red','blue'] }";
    }
    this.widgets.push(b);
    this.setSize(this.computeSize());
    return b;
  };
  n.prototype.addCustomWidget = function(a) {
    this.widgets || (this.widgets = []);
    this.widgets.push(a);
    return a;
  };
  n.prototype.getBounding = function(a) {
    a = a || new Float32Array(4);
    a[0] = this.pos[0] - 4;
    a[1] = this.pos[1] - f.NODE_TITLE_HEIGHT;
    a[2] = this.size[0] + 4;
    a[3] = this.size[1] + f.NODE_TITLE_HEIGHT;
    if (this.onBounding) {
      this.onBounding(a);
    }
    return a;
  };
  n.prototype.isPointInside = function(a, b, c, d) {
    c = c || 0;
    var e = this.graph && this.graph.isLive() ? 0 : f.NODE_TITLE_HEIGHT;
    d && (e = 0);
    if (this.flags && this.flags.collapsed) {
      if (C(a, b, this.pos[0] - c, this.pos[1] - f.NODE_TITLE_HEIGHT - c, (this._collapsed_width || f.NODE_COLLAPSED_WIDTH) + 2 * c, f.NODE_TITLE_HEIGHT + 2 * c)) {
        return !0;
      }
    } else {
      if (this.pos[0] - 4 - c < a && this.pos[0] + this.size[0] + 4 + c > a && this.pos[1] - e - c < b && this.pos[1] + this.size[1] + c > b) {
        return !0;
      }
    }
    return !1;
  };
  n.prototype.getSlotInPosition = function(a, b) {
    var c = new Float32Array(2);
    if (this.inputs) {
      for (var d = 0, e = this.inputs.length; d < e; ++d) {
        var k = this.inputs[d];
        this.getConnectionPos(!0, d, c);
        if (C(a, b, c[0] - 10, c[1] - 5, 20, 10)) {
          return {input:k, slot:d, link_pos:c};
        }
      }
    }
    if (this.outputs) {
      for (d = 0, e = this.outputs.length; d < e; ++d) {
        if (k = this.outputs[d], this.getConnectionPos(!1, d, c), C(a, b, c[0] - 10, c[1] - 5, 20, 10)) {
          return {output:k, slot:d, link_pos:c};
        }
      }
    }
    return null;
  };
  n.prototype.findInputSlot = function(a) {
    if (!this.inputs) {
      return -1;
    }
    for (var b = 0, c = this.inputs.length; b < c; ++b) {
      if (a == this.inputs[b].name) {
        return b;
      }
    }
    return -1;
  };
  n.prototype.findOutputSlot = function(a) {
    if (!this.outputs) {
      return -1;
    }
    for (var b = 0, c = this.outputs.length; b < c; ++b) {
      if (a == this.outputs[b].name) {
        return b;
      }
    }
    return -1;
  };
  n.prototype.connect = function(a, b, c) {
    c = c || 0;
    if (!this.graph) {
      return console.log("Connect: Error, node doesn't belong to any graph. Nodes must be added first to a graph before connecting them."), null;
    }
    if (a.constructor === String) {
      if (a = this.findOutputSlot(a), -1 == a) {
        return f.debug && console.log("Connect: Error, no slot of name " + a), null;
      }
    } else {
      if (!this.outputs || a >= this.outputs.length) {
        return f.debug && console.log("Connect: Error, slot number not found"), null;
      }
    }
    b && b.constructor === Number && (b = this.graph.getNodeById(b));
    if (!b) {
      throw "target node is null";
    }
    if (b == this) {
      return null;
    }
    if (c.constructor === String) {
      if (c = b.findInputSlot(c), -1 == c) {
        return f.debug && console.log("Connect: Error, no slot of name " + c), null;
      }
    } else {
      if (c === f.EVENT) {
        return null;
      }
      if (!b.inputs || c >= b.inputs.length) {
        return f.debug && console.log("Connect: Error, slot number not found"), null;
      }
    }
    var d = !1;
    null != b.inputs[c].link && (this.graph.beforeChange(), b.disconnectInput(c), d = !0);
    var e = this.outputs[a];
    if (b.onConnectInput && !1 === b.onConnectInput(c, e.type, e, this, a)) {
      return null;
    }
    var k = b.inputs[c], g = null;
    if (!f.isValidConnection(e.type, k.type)) {
      return this.setDirtyCanvas(!1, !0), d && this.graph.connectionChange(this, g), null;
    }
    d || this.graph.beforeChange();
    g = new y(++this.graph.last_link_id, k.type, this.id, a, b.id, c);
    this.graph.links[g.id] = g;
    null == e.links && (e.links = []);
    e.links.push(g.id);
    b.inputs[c].link = g.id;
    this.graph && this.graph._version++;
    if (this.onConnectionsChange) {
      this.onConnectionsChange(f.OUTPUT, a, !0, g, e);
    }
    if (b.onConnectionsChange) {
      b.onConnectionsChange(f.INPUT, c, !0, g, k);
    }
    this.graph && this.graph.onNodeConnectionChange && (this.graph.onNodeConnectionChange(f.INPUT, b, c, this, a), this.graph.onNodeConnectionChange(f.OUTPUT, this, a, b, c));
    this.setDirtyCanvas(!1, !0);
    this.graph.afterChange();
    this.graph.connectionChange(this, g);
    return g;
  };
  n.prototype.disconnectOutput = function(a, b) {
    if (a.constructor === String) {
      if (a = this.findOutputSlot(a), -1 == a) {
        return f.debug && console.log("Connect: Error, no slot of name " + a), !1;
      }
    } else {
      if (!this.outputs || a >= this.outputs.length) {
        return f.debug && console.log("Connect: Error, slot number not found"), !1;
      }
    }
    var c = this.outputs[a];
    if (!c || !c.links || 0 == c.links.length) {
      return !1;
    }
    if (b) {
      b.constructor === Number && (b = this.graph.getNodeById(b));
      if (!b) {
        throw "Target Node not found";
      }
      for (var d = 0, e = c.links.length; d < e; d++) {
        var k = c.links[d], g = this.graph.links[k];
        if (g.target_id == b.id) {
          c.links.splice(d, 1);
          var p = b.inputs[g.target_slot];
          p.link = null;
          delete this.graph.links[k];
          this.graph && this.graph._version++;
          if (b.onConnectionsChange) {
            b.onConnectionsChange(f.INPUT, g.target_slot, !1, g, p);
          }
          if (this.onConnectionsChange) {
            this.onConnectionsChange(f.OUTPUT, a, !1, g, c);
          }
          if (this.graph && this.graph.onNodeConnectionChange) {
            this.graph.onNodeConnectionChange(f.OUTPUT, this, a);
          }
          this.graph && this.graph.onNodeConnectionChange && (this.graph.onNodeConnectionChange(f.OUTPUT, this, a), this.graph.onNodeConnectionChange(f.INPUT, b, g.target_slot));
          break;
        }
      }
    } else {
      d = 0;
      for (e = c.links.length; d < e; d++) {
        if (k = c.links[d], g = this.graph.links[k]) {
          b = this.graph.getNodeById(g.target_id);
          this.graph && this.graph._version++;
          if (b) {
            p = b.inputs[g.target_slot];
            p.link = null;
            if (b.onConnectionsChange) {
              b.onConnectionsChange(f.INPUT, g.target_slot, !1, g, p);
            }
            if (this.graph && this.graph.onNodeConnectionChange) {
              this.graph.onNodeConnectionChange(f.INPUT, b, g.target_slot);
            }
          }
          delete this.graph.links[k];
          if (this.onConnectionsChange) {
            this.onConnectionsChange(f.OUTPUT, a, !1, g, c);
          }
          this.graph && this.graph.onNodeConnectionChange && (this.graph.onNodeConnectionChange(f.OUTPUT, this, a), this.graph.onNodeConnectionChange(f.INPUT, b, g.target_slot));
        }
      }
      c.links = null;
    }
    this.setDirtyCanvas(!1, !0);
    this.graph.connectionChange(this);
    return !0;
  };
  n.prototype.disconnectInput = function(a) {
    if (a.constructor === String) {
      if (a = this.findInputSlot(a), -1 == a) {
        return f.debug && console.log("Connect: Error, no slot of name " + a), !1;
      }
    } else {
      if (!this.inputs || a >= this.inputs.length) {
        return f.debug && console.log("Connect: Error, slot number not found"), !1;
      }
    }
    var b = this.inputs[a];
    if (!b) {
      return !1;
    }
    var c = this.inputs[a].link;
    if (null != c) {
      this.inputs[a].link = null;
      var d = this.graph.links[c];
      if (d) {
        var e = this.graph.getNodeById(d.origin_id);
        if (!e) {
          return !1;
        }
        var k = e.outputs[d.origin_slot];
        if (!k || !k.links || 0 == k.links.length) {
          return !1;
        }
        for (var g = 0, p = k.links.length; g < p; g++) {
          if (k.links[g] == c) {
            k.links.splice(g, 1);
            break;
          }
        }
        delete this.graph.links[c];
        this.graph && this.graph._version++;
        if (this.onConnectionsChange) {
          this.onConnectionsChange(f.INPUT, a, !1, d, b);
        }
        if (e.onConnectionsChange) {
          e.onConnectionsChange(f.OUTPUT, g, !1, d, k);
        }
        this.graph && this.graph.onNodeConnectionChange && (this.graph.onNodeConnectionChange(f.OUTPUT, e, g), this.graph.onNodeConnectionChange(f.INPUT, this, a));
      }
    }
    this.setDirtyCanvas(!1, !0);
    this.graph && this.graph.connectionChange(this);
    return !0;
  };
  n.prototype.getConnectionPos = function(a, b, c) {
    c = c || new Float32Array(2);
    var d = 0;
    a && this.inputs && (d = this.inputs.length);
    !a && this.outputs && (d = this.outputs.length);
    var e = 0.5 * f.NODE_SLOT_HEIGHT;
    if (this.flags.collapsed) {
      return b = this._collapsed_width || f.NODE_COLLAPSED_WIDTH, this.horizontal ? (c[0] = this.pos[0] + 0.5 * b, c[1] = a ? this.pos[1] - f.NODE_TITLE_HEIGHT : this.pos[1]) : (c[0] = a ? this.pos[0] : this.pos[0] + b, c[1] = this.pos[1] - 0.5 * f.NODE_TITLE_HEIGHT), c;
    }
    if (a && -1 == b) {
      return c[0] = this.pos[0] + 0.5 * f.NODE_TITLE_HEIGHT, c[1] = this.pos[1] + 0.5 * f.NODE_TITLE_HEIGHT, c;
    }
    if (a && d > b && this.inputs[b].pos) {
      return c[0] = this.pos[0] + this.inputs[b].pos[0], c[1] = this.pos[1] + this.inputs[b].pos[1], c;
    }
    if (!a && d > b && this.outputs[b].pos) {
      return c[0] = this.pos[0] + this.outputs[b].pos[0], c[1] = this.pos[1] + this.outputs[b].pos[1], c;
    }
    if (this.horizontal) {
      return c[0] = this.pos[0] + this.size[0] / d * (b + 0.5), c[1] = a ? this.pos[1] - f.NODE_TITLE_HEIGHT : this.pos[1] + this.size[1], c;
    }
    c[0] = a ? this.pos[0] + e : this.pos[0] + this.size[0] + 1 - e;
    c[1] = this.pos[1] + (b + 0.7) * f.NODE_SLOT_HEIGHT + (this.constructor.slot_start_y || 0);
    return c;
  };
  n.prototype.alignToGrid = function() {
    this.pos[0] = f.CANVAS_GRID_SIZE * Math.round(this.pos[0] / f.CANVAS_GRID_SIZE);
    this.pos[1] = f.CANVAS_GRID_SIZE * Math.round(this.pos[1] / f.CANVAS_GRID_SIZE);
  };
  n.prototype.trace = function(a) {
    this.console || (this.console = []);
    this.console.push(a);
    this.console.length > n.MAX_CONSOLE && this.console.shift();
    if (this.graph.onNodeTrace) {
      this.graph.onNodeTrace(this, a);
    }
  };
  n.prototype.setDirtyCanvas = function(a, b) {
    this.graph && this.graph.sendActionToCanvas("setDirty", [a, b]);
  };
  n.prototype.loadImage = function(a) {
    var b = new Image;
    b.src = f.node_images_path + a;
    b.ready = !1;
    var c = this;
    b.onload = function() {
      this.ready = !0;
      c.setDirtyCanvas(!0);
    };
    return b;
  };
  n.prototype.captureInput = function(a) {
    if (this.graph && this.graph.list_of_graphcanvas) {
      for (var b = this.graph.list_of_graphcanvas, c = 0; c < b.length; ++c) {
        var d = b[c];
        if (a || d.node_capturing_input == this) {
          d.node_capturing_input = a ? this : null;
        }
      }
    }
  };
  n.prototype.collapse = function(a) {
    this.graph._version++;
    if (!1 !== this.constructor.collapsable || a) {
      this.flags.collapsed = this.flags.collapsed ? !1 : !0, this.setDirtyCanvas(!0, !0);
    }
  };
  n.prototype.pin = function(a) {
    this.graph._version++;
    this.flags.pinned = void 0 === a ? !this.flags.pinned : a;
  };
  n.prototype.localToScreen = function(a, b, c) {
    return [(a + this.pos[0]) * c.scale + c.offset[0], (b + this.pos[1]) * c.scale + c.offset[1]];
  };
  r.LGraphGroup = f.LGraphGroup = u;
  u.prototype._ctor = function(a) {
    this.title = a || "Group";
    this.font_size = 24;
    this.color = h.node_colors.pale_blue ? h.node_colors.pale_blue.groupcolor : "#AAA";
    this._bounding = new Float32Array([10, 10, 140, 80]);
    this._pos = this._bounding.subarray(0, 2);
    this._size = this._bounding.subarray(2, 4);
    this._nodes = [];
    this.graph = null;
    Object.defineProperty(this, "pos", {set:function(a) {
      !a || 2 > a.length || (this._pos[0] = a[0], this._pos[1] = a[1]);
    }, get:function() {
      return this._pos;
    }, enumerable:!0});
    Object.defineProperty(this, "size", {set:function(a) {
      !a || 2 > a.length || (this._size[0] = Math.max(140, a[0]), this._size[1] = Math.max(80, a[1]));
    }, get:function() {
      return this._size;
    }, enumerable:!0});
  };
  u.prototype.configure = function(a) {
    this.title = a.title;
    this._bounding.set(a.bounding);
    this.color = a.color;
    this.font = a.font;
  };
  u.prototype.serialize = function() {
    var a = this._bounding;
    return {title:this.title, bounding:[Math.round(a[0]), Math.round(a[1]), Math.round(a[2]), Math.round(a[3])], color:this.color, font:this.font};
  };
  u.prototype.move = function(a, b, c) {
    this._pos[0] += a;
    this._pos[1] += b;
    if (!c) {
      for (c = 0; c < this._nodes.length; ++c) {
        var d = this._nodes[c];
        d.pos[0] += a;
        d.pos[1] += b;
      }
    }
  };
  u.prototype.recomputeInsideNodes = function() {
    this._nodes.length = 0;
    for (var a = this.graph._nodes, b = new Float32Array(4), c = 0; c < a.length; ++c) {
      var d = a[c];
      d.getBounding(b);
      F(this._bounding, b) && this._nodes.push(d);
    }
  };
  u.prototype.isPointInside = n.prototype.isPointInside;
  u.prototype.setDirtyCanvas = n.prototype.setDirtyCanvas;
  f.DragAndScale = z;
  z.prototype.bindEvents = function(a) {
    this.last_mouse = new Float32Array(2);
    this._binded_mouse_callback = this.onMouse.bind(this);
    a.addEventListener("mousedown", this._binded_mouse_callback);
    a.addEventListener("mousemove", this._binded_mouse_callback);
    a.addEventListener("mousewheel", this._binded_mouse_callback, !1);
    a.addEventListener("wheel", this._binded_mouse_callback, !1);
  };
  z.prototype.computeVisibleArea = function() {
    if (this.element) {
      var a = -this.offset[0], b = -this.offset[1], c = a + this.element.width / this.scale, d = b + this.element.height / this.scale;
      this.visible_area[0] = a;
      this.visible_area[1] = b;
      this.visible_area[2] = c - a;
      this.visible_area[3] = d - b;
    } else {
      this.visible_area[0] = this.visible_area[1] = this.visible_area[2] = this.visible_area[3] = 0;
    }
  };
  z.prototype.onMouse = function(a) {
    if (this.enabled) {
      var b = this.element, c = b.getBoundingClientRect(), d = a.clientX - c.left;
      c = a.clientY - c.top;
      a.canvasx = d;
      a.canvasy = c;
      a.dragging = this.dragging;
      var e = !1;
      this.onmouse && (e = this.onmouse(a));
      if ("mousedown" == a.type) {
        this.dragging = !0, b.removeEventListener("mousemove", this._binded_mouse_callback), document.body.addEventListener("mousemove", this._binded_mouse_callback), document.body.addEventListener("mouseup", this._binded_mouse_callback);
      } else {
        if ("mousemove" == a.type) {
          e || (b = d - this.last_mouse[0], e = c - this.last_mouse[1], this.dragging && this.mouseDrag(b, e));
        } else {
          if ("mouseup" == a.type) {
            this.dragging = !1, document.body.removeEventListener("mousemove", this._binded_mouse_callback), document.body.removeEventListener("mouseup", this._binded_mouse_callback), b.addEventListener("mousemove", this._binded_mouse_callback);
          } else {
            if ("mousewheel" == a.type || "wheel" == a.type || "DOMMouseScroll" == a.type) {
              a.eventType = "mousewheel", a.wheel = "wheel" == a.type ? -a.deltaY : null != a.wheelDeltaY ? a.wheelDeltaY : -60 * a.detail, a.delta = a.wheelDelta ? a.wheelDelta / 40 : a.deltaY ? -a.deltaY / 3 : 0, this.changeDeltaScale(1.0 + 0.05 * a.delta);
            }
          }
        }
      }
      this.last_mouse[0] = d;
      this.last_mouse[1] = c;
      a.preventDefault();
      a.stopPropagation();
      return !1;
    }
  };
  z.prototype.toCanvasContext = function(a) {
    a.scale(this.scale, this.scale);
    a.translate(this.offset[0], this.offset[1]);
  };
  z.prototype.convertOffsetToCanvas = function(a) {
    return [(a[0] + this.offset[0]) * this.scale, (a[1] + this.offset[1]) * this.scale];
  };
  z.prototype.convertCanvasToOffset = function(a, b) {
    b = b || [0, 0];
    b[0] = a[0] / this.scale - this.offset[0];
    b[1] = a[1] / this.scale - this.offset[1];
    return b;
  };
  z.prototype.mouseDrag = function(a, b) {
    this.offset[0] += a / this.scale;
    this.offset[1] += b / this.scale;
    if (this.onredraw) {
      this.onredraw(this);
    }
  };
  z.prototype.changeScale = function(a, b) {
    a < this.min_scale ? a = this.min_scale : a > this.max_scale && (a = this.max_scale);
    if (a != this.scale && this.element) {
      var c = this.element.getBoundingClientRect();
      if (c && (b = b || [0.5 * c.width, 0.5 * c.height], c = this.convertCanvasToOffset(b), this.scale = a, 0.01 > Math.abs(this.scale - 1) && (this.scale = 1), a = this.convertCanvasToOffset(b), a = [a[0] - c[0], a[1] - c[1]], this.offset[0] += a[0], this.offset[1] += a[1], this.onredraw)) {
        this.onredraw(this);
      }
    }
  };
  z.prototype.changeDeltaScale = function(a, b) {
    this.changeScale(this.scale * a, b);
  };
  z.prototype.reset = function() {
    this.scale = 1;
    this.offset[0] = 0;
    this.offset[1] = 0;
  };
  r.LGraphCanvas = f.LGraphCanvas = h;
  h.DEFAULT_BACKGROUND_IMAGE = "data:image/png;base64,";
  h.link_type_colors = {"-1":f.EVENT_LINK_COLOR, number:"#AAA", node:"#DCA"};
  h.gradients = {};
  h.prototype.clear = function() {
    this.fps = this.render_time = this.last_draw_time = this.frame = 0;
    this.dragging_rectangle = null;
    this.selected_nodes = {};
    this.selected_group = null;
    this.visible_nodes = [];
    this.connecting_node = this.node_capturing_input = this.node_over = this.node_dragged = null;
    this.highlighted_links = {};
    this.dragging_canvas = !1;
    this.dirty_bgcanvas = this.dirty_canvas = !0;
    this.node_widget = this.node_in_panel = this.dirty_area = null;
    this.last_mouse = [0, 0];
    this.last_mouseclick = 0;
    this.visible_area.set([0, 0, 0, 0]);
    if (this.onClear) {
      this.onClear();
    }
  };
  h.prototype.setGraph = function(a, b) {
    this.graph != a && (b || this.clear(), !a && this.graph ? this.graph.detachCanvas(this) : (a.attachCanvas(this), this._graph_stack && (this._graph_stack = null), this.setDirty(!0, !0)));
  };
  h.prototype.getTopGraph = function() {
    return this._graph_stack.length ? this._graph_stack[0] : this.graph;
  };
  h.prototype.openSubgraph = function(a) {
    if (!a) {
      throw "graph cannot be null";
    }
    if (this.graph == a) {
      throw "graph cannot be the same";
    }
    this.clear();
    this.graph && (this._graph_stack || (this._graph_stack = []), this._graph_stack.push(this.graph));
    a.attachCanvas(this);
    this.checkPanels();
    this.setDirty(!0, !0);
  };
  h.prototype.closeSubgraph = function() {
    if (this._graph_stack && 0 != this._graph_stack.length) {
      var a = this.graph._subgraph_node, b = this._graph_stack.pop();
      this.selected_nodes = {};
      this.highlighted_links = {};
      b.attachCanvas(this);
      this.setDirty(!0, !0);
      a && (this.centerOnNode(a), this.selectNodes([a]));
    }
  };
  h.prototype.getCurrentGraph = function() {
    return this.graph;
  };
  h.prototype.setCanvas = function(a, b) {
    if (a && a.constructor === String && (a = document.getElementById(a), !a)) {
      throw "Error creating LiteGraph canvas: Canvas not found";
    }
    if (a !== this.canvas && (!a && this.canvas && (b || this.unbindEvents()), this.canvas = a, this.ds.element = a)) {
      a.className += " lgraphcanvas";
      a.data = this;
      a.tabindex = "1";
      this.bgcanvas = null;
      this.bgcanvas || (this.bgcanvas = document.createElement("canvas"), this.bgcanvas.width = this.canvas.width, this.bgcanvas.height = this.canvas.height);
      if (null == a.getContext) {
        if ("canvas" != a.localName) {
          throw "Element supplied for LGraphCanvas must be a <canvas> element, you passed a " + a.localName;
        }
        throw "This browser doesn't support Canvas";
      }
      null == (this.ctx = a.getContext("2d")) && (a.webgl_enabled || console.warn("This canvas seems to be WebGL, enabling WebGL renderer"), this.enableWebGL());
      this._mousemove_callback = this.processMouseMove.bind(this);
      this._mouseup_callback = this.processMouseUp.bind(this);
      b || this.bindEvents();
    }
  };
  h.prototype._doNothing = function(a) {
    a.preventDefault();
    return !1;
  };
  h.prototype._doReturnTrue = function(a) {
    a.preventDefault();
    return !0;
  };
  h.prototype.bindEvents = function() {
    if (this._events_binded) {
      console.warn("LGraphCanvas: events already binded");
    } else {
      var a = this.canvas, b = this.getCanvasWindow().document;
      this._mousedown_callback = this.processMouseDown.bind(this);
      this._mousewheel_callback = this.processMouseWheel.bind(this);
      a.addEventListener("mousedown", this._mousedown_callback, !0);
      a.addEventListener("mousemove", this._mousemove_callback);
      a.addEventListener("mousewheel", this._mousewheel_callback, !1);
      a.addEventListener("contextmenu", this._doNothing);
      a.addEventListener("DOMMouseScroll", this._mousewheel_callback, !1);
      a.addEventListener("touchstart", this.touchHandler, !0);
      a.addEventListener("touchmove", this.touchHandler, !0);
      a.addEventListener("touchend", this.touchHandler, !0);
      a.addEventListener("touchcancel", this.touchHandler, !0);
      this._key_callback = this.processKey.bind(this);
      a.addEventListener("keydown", this._key_callback, !0);
      b.addEventListener("keyup", this._key_callback, !0);
      this._ondrop_callback = this.processDrop.bind(this);
      a.addEventListener("dragover", this._doNothing, !1);
      a.addEventListener("dragend", this._doNothing, !1);
      a.addEventListener("drop", this._ondrop_callback, !1);
      a.addEventListener("dragenter", this._doReturnTrue, !1);
      this._events_binded = !0;
    }
  };
  h.prototype.unbindEvents = function() {
    if (this._events_binded) {
      var a = this.getCanvasWindow().document;
      this.canvas.removeEventListener("mousedown", this._mousedown_callback);
      this.canvas.removeEventListener("mousewheel", this._mousewheel_callback);
      this.canvas.removeEventListener("DOMMouseScroll", this._mousewheel_callback);
      this.canvas.removeEventListener("keydown", this._key_callback);
      a.removeEventListener("keyup", this._key_callback);
      this.canvas.removeEventListener("contextmenu", this._doNothing);
      this.canvas.removeEventListener("drop", this._ondrop_callback);
      this.canvas.removeEventListener("dragenter", this._doReturnTrue);
      this.canvas.removeEventListener("touchstart", this.touchHandler);
      this.canvas.removeEventListener("touchmove", this.touchHandler);
      this.canvas.removeEventListener("touchend", this.touchHandler);
      this.canvas.removeEventListener("touchcancel", this.touchHandler);
      this._ondrop_callback = this._key_callback = this._mousewheel_callback = this._mousedown_callback = null;
      this._events_binded = !1;
    } else {
      console.warn("LGraphCanvas: no events binded");
    }
  };
  h.getFileExtension = function(a) {
    var b = a.indexOf("?");
    -1 != b && (a = a.substr(0, b));
    b = a.lastIndexOf(".");
    return -1 == b ? "" : a.substr(b + 1).toLowerCase();
  };
  h.prototype.enableWebGL = function() {
    this.gl = this.ctx = enableWebGLCanvas(this.canvas);
    this.ctx.webgl = !0;
    this.bgcanvas = this.canvas;
    this.bgctx = this.gl;
    this.canvas.webgl_enabled = !0;
  };
  h.prototype.setDirty = function(a, b) {
    a && (this.dirty_canvas = !0);
    b && (this.dirty_bgcanvas = !0);
  };
  h.prototype.getCanvasWindow = function() {
    if (!this.canvas) {
      return window;
    }
    var a = this.canvas.ownerDocument;
    return a.defaultView || a.parentWindow;
  };
  h.prototype.startRendering = function() {
    function a() {
      this.pause_rendering || this.draw();
      var b = this.getCanvasWindow();
      this.is_rendering && b.requestAnimationFrame(a.bind(this));
    }
    this.is_rendering || (this.is_rendering = !0, a.call(this));
  };
  h.prototype.stopRendering = function() {
    this.is_rendering = !1;
  };
  h.prototype.blockClick = function() {
    this.block_click = !0;
    this.last_mouseclick = 0;
  };
  h.prototype.processMouseDown = function(a) {
    this.set_canvas_dirty_on_mouse_event && (this.dirty_canvas = !0);
    if (this.graph) {
      this.adjustMouseEvent(a);
      var b = this.getCanvasWindow();
      h.active_canvas = this;
      var c = this;
      this.canvas.removeEventListener("mousemove", this._mousemove_callback);
      b.document.addEventListener("mousemove", this._mousemove_callback, !0);
      b.document.addEventListener("mouseup", this._mouseup_callback, !0);
      var d = this.graph.getNodeOnPos(a.canvasX, a.canvasY, this.visible_nodes, 5), e = !1, k = 300 > f.getTime() - this.last_mouseclick;
      this.mouse[0] = a.localX;
      this.mouse[1] = a.localY;
      this.graph_mouse[0] = a.canvasX;
      this.graph_mouse[1] = a.canvasY;
      this.last_click_position = [this.mouse[0], this.mouse[1]];
      this.canvas.focus();
      f.closeAllContextMenus(b);
      if (!this.onMouse || 1 != this.onMouse(a)) {
        if (1 == a.which) {
          a.ctrlKey && (this.dragging_rectangle = new Float32Array(4), this.dragging_rectangle[0] = a.canvasX, this.dragging_rectangle[1] = a.canvasY, this.dragging_rectangle[2] = 1, this.dragging_rectangle[3] = 1, e = !0);
          var g = !1;
          if (d && this.allow_interaction && !e && !this.read_only) {
            this.live_mode || d.flags.pinned || this.bringToFront(d);
            if (!this.connecting_node && !d.flags.collapsed && !this.live_mode) {
              if (!e && !1 !== d.resizable && C(a.canvasX, a.canvasY, d.pos[0] + d.size[0] - 5, d.pos[1] + d.size[1] - 5, 10, 10)) {
                this.graph.beforeChange(), this.resizing_node = d, this.canvas.style.cursor = "se-resize", e = !0;
              } else {
                if (d.outputs) {
                  for (var p = 0, x = d.outputs.length; p < x; ++p) {
                    var t = d.outputs[p], q = d.getConnectionPos(!1, p);
                    if (C(a.canvasX, a.canvasY, q[0] - 15, q[1] - 10, 30, 20)) {
                      this.connecting_node = d;
                      this.connecting_output = t;
                      this.connecting_pos = d.getConnectionPos(!1, p);
                      this.connecting_slot = p;
                      a.shiftKey && d.disconnectOutput(p);
                      if (k) {
                        if (d.onOutputDblClick) {
                          d.onOutputDblClick(p, a);
                        }
                      } else {
                        if (d.onOutputClick) {
                          d.onOutputClick(p, a);
                        }
                      }
                      e = !0;
                      break;
                    }
                  }
                }
                if (d.inputs) {
                  for (p = 0, x = d.inputs.length; p < x; ++p) {
                    if (t = d.inputs[p], q = d.getConnectionPos(!0, p), C(a.canvasX, a.canvasY, q[0] - 15, q[1] - 10, 30, 20)) {
                      if (k) {
                        if (d.onInputDblClick) {
                          d.onInputDblClick(p, a);
                        }
                      } else {
                        if (d.onInputClick) {
                          d.onInputClick(p, a);
                        }
                      }
                      if (null !== t.link) {
                        e = this.graph.links[t.link];
                        d.disconnectInput(p);
                        if (this.allow_reconnect_links || a.shiftKey) {
                          this.connecting_node = this.graph._nodes_by_id[e.origin_id], this.connecting_slot = e.origin_slot, this.connecting_output = this.connecting_node.outputs[this.connecting_slot], this.connecting_pos = this.connecting_node.getConnectionPos(!1, this.connecting_slot);
                        }
                        e = this.dirty_bgcanvas = !0;
                      }
                    }
                  }
                }
              }
            }
            if (!e) {
              p = !1;
              x = [a.canvasX - d.pos[0], a.canvasY - d.pos[1]];
              if (q = this.processNodeWidgets(d, this.graph_mouse, a)) {
                p = !0, this.node_widget = [d, q];
              }
              if (k && this.selected_nodes[d.id]) {
                if (d.onDblClick) {
                  d.onDblClick(a, x, this);
                }
                this.processNodeDblClicked(d);
                p = !0;
              }
              d.onMouseDown && d.onMouseDown(a, x, this) ? p = !0 : (d.subgraph && !d.skip_subgraph_button && !d.flags.collapsed && x[0] > d.size[0] - f.NODE_TITLE_HEIGHT && 0 > x[1] && (c = this, setTimeout(function() {
                c.openSubgraph(d.subgraph);
              }, 10)), this.live_mode && (p = g = !0));
              p || (this.allow_dragnodes && (this.graph.beforeChange(), this.node_dragged = d), this.selected_nodes[d.id] || this.processNodeSelected(d, a));
              this.dirty_canvas = !0;
            }
          } else {
            if (!this.read_only) {
              for (p = 0; p < this.visible_links.length; ++p) {
                if (g = this.visible_links[p], x = g._pos, !(!x || a.canvasX < x[0] - 4 || a.canvasX > x[0] + 4 || a.canvasY < x[1] - 4 || a.canvasY > x[1] + 4)) {
                  this.showLinkMenu(g, a);
                  this.over_link_center = null;
                  break;
                }
              }
            }
            this.selected_group = this.graph.getGroupOnPos(a.canvasX, a.canvasY);
            this.selected_group_resizing = !1;
            this.selected_group && !this.read_only && (a.ctrlKey && (this.dragging_rectangle = null), 10 > K([a.canvasX, a.canvasY], [this.selected_group.pos[0] + this.selected_group.size[0], this.selected_group.pos[1] + this.selected_group.size[1]]) * this.ds.scale ? this.selected_group_resizing = !0 : this.selected_group.recomputeInsideNodes());
            k && !this.read_only && this.allow_searchbox && this.showSearchBox(a);
            g = !0;
          }
          !e && g && this.allow_dragcanvas && (this.dragging_canvas = !0);
        } else {
          2 != a.which && 3 == a.which && (this.read_only || this.processContextMenu(d, a));
        }
        this.last_mouse[0] = a.localX;
        this.last_mouse[1] = a.localY;
        this.last_mouseclick = f.getTime();
        this.last_mouse_dragging = !0;
        this.graph.change();
        (!b.document.activeElement || "input" != b.document.activeElement.nodeName.toLowerCase() && "textarea" != b.document.activeElement.nodeName.toLowerCase()) && a.preventDefault();
        a.stopPropagation();
        if (this.onMouseDown) {
          this.onMouseDown(a);
        }
        return !1;
      }
    }
  };
  h.prototype.processMouseMove = function(a) {
    this.autoresize && this.resize();
    this.set_canvas_dirty_on_mouse_event && (this.dirty_canvas = !0);
    if (this.graph) {
      h.active_canvas = this;
      this.adjustMouseEvent(a);
      var b = [a.localX, a.localY];
      this.mouse[0] = b[0];
      this.mouse[1] = b[1];
      var c = [b[0] - this.last_mouse[0], b[1] - this.last_mouse[1]];
      this.last_mouse = b;
      this.graph_mouse[0] = a.canvasX;
      this.graph_mouse[1] = a.canvasY;
      if (this.block_click) {
        return a.preventDefault(), !1;
      }
      a.dragging = this.last_mouse_dragging;
      this.node_widget && (this.processNodeWidgets(this.node_widget[0], this.graph_mouse, a, this.node_widget[1]), this.dirty_canvas = !0);
      if (this.dragging_rectangle) {
        this.dragging_rectangle[2] = a.canvasX - this.dragging_rectangle[0], this.dragging_rectangle[3] = a.canvasY - this.dragging_rectangle[1], this.dirty_canvas = !0;
      } else {
        if (this.selected_group && !this.read_only) {
          this.selected_group_resizing ? this.selected_group.size = [a.canvasX - this.selected_group.pos[0], a.canvasY - this.selected_group.pos[1]] : (this.selected_group.move(c[0] / this.ds.scale, c[1] / this.ds.scale, a.ctrlKey), this.selected_group._nodes.length && (this.dirty_canvas = !0)), this.dirty_bgcanvas = !0;
        } else {
          if (this.dragging_canvas) {
            this.ds.offset[0] += c[0] / this.ds.scale, this.ds.offset[1] += c[1] / this.ds.scale, this.dirty_bgcanvas = this.dirty_canvas = !0;
          } else {
            if (this.allow_interaction && !this.read_only) {
              this.connecting_node && (this.dirty_canvas = !0);
              var d = this.graph.getNodeOnPos(a.canvasX, a.canvasY, this.visible_nodes);
              b = 0;
              for (var e = this.graph._nodes.length; b < e; ++b) {
                if (this.graph._nodes[b].mouseOver && d != this.graph._nodes[b]) {
                  this.graph._nodes[b].mouseOver = !1;
                  if (this.node_over && this.node_over.onMouseLeave) {
                    this.node_over.onMouseLeave(a);
                  }
                  this.node_over = null;
                  this.dirty_canvas = !0;
                }
              }
              if (d) {
                d.redraw_on_mouse && (this.dirty_canvas = !0);
                if (!d.mouseOver && (d.mouseOver = !0, this.node_over = d, this.dirty_canvas = !0, d.onMouseEnter)) {
                  d.onMouseEnter(a);
                }
                if (d.onMouseMove) {
                  d.onMouseMove(a, [a.canvasX - d.pos[0], a.canvasY - d.pos[1]], this);
                }
                if (this.connecting_node && (e = this._highlight_input || [0, 0], !this.isOverNodeBox(d, a.canvasX, a.canvasY))) {
                  var k = this.isOverNodeInput(d, a.canvasX, a.canvasY, e);
                  -1 != k && d.inputs[k] ? f.isValidConnection(this.connecting_output.type, d.inputs[k].type) && (this._highlight_input = e) : this._highlight_input = null;
                }
                this.canvas && (C(a.canvasX, a.canvasY, d.pos[0] + d.size[0] - 5, d.pos[1] + d.size[1] - 5, 5, 5) ? this.canvas.style.cursor = "se-resize" : this.canvas.style.cursor = "crosshair");
              } else {
                e = null;
                for (b = 0; b < this.visible_links.length; ++b) {
                  k = this.visible_links[b];
                  var g = k._pos;
                  if (!(!g || a.canvasX < g[0] - 4 || a.canvasX > g[0] + 4 || a.canvasY < g[1] - 4 || a.canvasY > g[1] + 4)) {
                    e = k;
                    break;
                  }
                }
                e != this.over_link_center && (this.over_link_center = e, this.dirty_canvas = !0);
                this.canvas && (this.canvas.style.cursor = "");
              }
              if (this.node_capturing_input && this.node_capturing_input != d && this.node_capturing_input.onMouseMove) {
                this.node_capturing_input.onMouseMove(a, [a.canvasX - this.node_capturing_input.pos[0], a.canvasY - this.node_capturing_input.pos[1]], this);
              }
              if (this.node_dragged && !this.live_mode) {
                for (b in this.selected_nodes) {
                  d = this.selected_nodes[b], d.pos[0] += c[0] / this.ds.scale, d.pos[1] += c[1] / this.ds.scale;
                }
                this.dirty_bgcanvas = this.dirty_canvas = !0;
              }
              this.resizing_node && !this.live_mode && (c = [a.canvasX - this.resizing_node.pos[0], a.canvasY - this.resizing_node.pos[1]], b = this.resizing_node.computeSize(), c[0] = Math.max(b[0], c[0]), c[1] = Math.max(b[1], c[1]), this.resizing_node.setSize(c), this.canvas.style.cursor = "se-resize", this.dirty_bgcanvas = this.dirty_canvas = !0);
            }
          }
        }
      }
      a.preventDefault();
      return !1;
    }
  };
  h.prototype.processMouseUp = function(a) {
    this.set_canvas_dirty_on_mouse_event && (this.dirty_canvas = !0);
    if (this.graph) {
      var b = this.getCanvasWindow().document;
      h.active_canvas = this;
      b.removeEventListener("mousemove", this._mousemove_callback, !0);
      this.canvas.addEventListener("mousemove", this._mousemove_callback, !0);
      b.removeEventListener("mouseup", this._mouseup_callback, !0);
      this.adjustMouseEvent(a);
      b = f.getTime();
      a.click_time = b - this.last_mouseclick;
      this.last_mouse_dragging = !1;
      this.last_click_position = null;
      this.block_click && (console.log("foo"), this.block_click = !1);
      if (1 == a.which) {
        if (this.node_widget && this.processNodeWidgets(this.node_widget[0], this.graph_mouse, a), this.node_widget = null, this.selected_group && (this.selected_group.move(this.selected_group.pos[0] - Math.round(this.selected_group.pos[0]), this.selected_group.pos[1] - Math.round(this.selected_group.pos[1]), a.ctrlKey), this.selected_group.pos[0] = Math.round(this.selected_group.pos[0]), this.selected_group.pos[1] = Math.round(this.selected_group.pos[1]), this.selected_group._nodes.length && (this.dirty_canvas = 
        !0), this.selected_group = null), this.selected_group_resizing = !1, this.dragging_rectangle) {
          if (this.graph) {
            b = this.graph._nodes;
            var c = new Float32Array(4);
            this.deselectAllNodes();
            var d = Math.abs(this.dragging_rectangle[2]), e = Math.abs(this.dragging_rectangle[3]), k = 0 > this.dragging_rectangle[3] ? this.dragging_rectangle[1] - e : this.dragging_rectangle[1];
            this.dragging_rectangle[0] = 0 > this.dragging_rectangle[2] ? this.dragging_rectangle[0] - d : this.dragging_rectangle[0];
            this.dragging_rectangle[1] = k;
            this.dragging_rectangle[2] = d;
            this.dragging_rectangle[3] = e;
            e = [];
            for (k = 0; k < b.length; ++k) {
              d = b[k], d.getBounding(c), F(this.dragging_rectangle, c) && e.push(d);
            }
            e.length && this.selectNodes(e);
          }
          this.dragging_rectangle = null;
        } else {
          if (this.connecting_node) {
            this.dirty_bgcanvas = this.dirty_canvas = !0;
            if (d = this.graph.getNodeOnPos(a.canvasX, a.canvasY, this.visible_nodes)) {
              this.connecting_output.type == f.EVENT && this.isOverNodeBox(d, a.canvasX, a.canvasY) ? this.connecting_node.connect(this.connecting_slot, d, f.EVENT) : (b = this.isOverNodeInput(d, a.canvasX, a.canvasY), -1 != b ? this.connecting_node.connect(this.connecting_slot, d, b) : (b = d.getInputInfo(0), this.connecting_output.type == f.EVENT ? this.connecting_node.connect(this.connecting_slot, d, f.EVENT) : b && !b.link && f.isValidConnection(b.type && this.connecting_output.type) && this.connecting_node.connect(this.connecting_slot, 
              d, 0)));
            }
            this.connecting_node = this.connecting_pos = this.connecting_output = null;
            this.connecting_slot = -1;
          } else {
            if (this.resizing_node) {
              this.dirty_bgcanvas = this.dirty_canvas = !0, this.graph.afterChange(this.resizing_node), this.resizing_node = null;
            } else {
              if (this.node_dragged) {
                (d = this.node_dragged) && 300 > a.click_time && C(a.canvasX, a.canvasY, d.pos[0], d.pos[1] - f.NODE_TITLE_HEIGHT, f.NODE_TITLE_HEIGHT, f.NODE_TITLE_HEIGHT) && d.collapse();
                this.dirty_bgcanvas = this.dirty_canvas = !0;
                this.node_dragged.pos[0] = Math.round(this.node_dragged.pos[0]);
                this.node_dragged.pos[1] = Math.round(this.node_dragged.pos[1]);
                this.graph.config.align_to_grid && this.node_dragged.alignToGrid();
                if (this.onNodeMoved) {
                  this.onNodeMoved(this.node_dragged);
                }
                this.graph.afterChange(this.node_dragged);
                this.node_dragged = null;
              } else {
                d = this.graph.getNodeOnPos(a.canvasX, a.canvasY, this.visible_nodes);
                !d && 300 > a.click_time && this.deselectAllNodes();
                this.dirty_canvas = !0;
                this.dragging_canvas = !1;
                if (this.node_over && this.node_over.onMouseUp) {
                  this.node_over.onMouseUp(a, [a.canvasX - this.node_over.pos[0], a.canvasY - this.node_over.pos[1]], this);
                }
                if (this.node_capturing_input && this.node_capturing_input.onMouseUp) {
                  this.node_capturing_input.onMouseUp(a, [a.canvasX - this.node_capturing_input.pos[0], a.canvasY - this.node_capturing_input.pos[1]]);
                }
              }
            }
          }
        }
      } else {
        2 == a.which ? (this.dirty_canvas = !0, this.dragging_canvas = !1) : 3 == a.which && (this.dirty_canvas = !0, this.dragging_canvas = !1);
      }
      this.graph.change();
      a.stopPropagation();
      a.preventDefault();
      return !1;
    }
  };
  h.prototype.processMouseWheel = function(a) {
    if (this.graph && this.allow_dragcanvas) {
      var b = null != a.wheelDeltaY ? a.wheelDeltaY : -60 * a.detail;
      this.adjustMouseEvent(a);
      var c = this.ds.scale;
      0 < b ? c *= 1.1 : 0 > b && (c *= 1 / 1.1);
      this.ds.changeScale(c, [a.localX, a.localY]);
      this.graph.change();
      a.preventDefault();
      return !1;
    }
  };
  h.prototype.isOverNodeBox = function(a, b, c) {
    var d = f.NODE_TITLE_HEIGHT;
    return C(b, c, a.pos[0] + 2, a.pos[1] + 2 - d, d - 4, d - 4) ? !0 : !1;
  };
  h.prototype.isOverNodeInput = function(a, b, c, d) {
    if (a.inputs) {
      for (var e = 0, k = a.inputs.length; e < k; ++e) {
        var g = a.getConnectionPos(!0, e);
        if (a.horizontal ? C(b, c, g[0] - 5, g[1] - 10, 10, 20) : C(b, c, g[0] - 10, g[1] - 5, 40, 10)) {
          return d && (d[0] = g[0], d[1] = g[1]), e;
        }
      }
    }
    return -1;
  };
  h.prototype.processKey = function(a) {
    if (this.graph) {
      var b = !1;
      if ("input" != a.target.localName) {
        if ("keydown" == a.type) {
          if (32 == a.keyCode && (b = this.dragging_canvas = !0), 65 == a.keyCode && a.ctrlKey && (this.selectNodes(), b = !0), "KeyC" == a.code && (a.metaKey || a.ctrlKey) && !a.shiftKey && this.selected_nodes && (this.copyToClipboard(), b = !0), "KeyV" != a.code || !a.metaKey && !a.ctrlKey || a.shiftKey || this.pasteFromClipboard(), 46 != a.keyCode && 8 != a.keyCode || "input" == a.target.localName || "textarea" == a.target.localName || (this.deleteSelectedNodes(), b = !0), this.selected_nodes) {
            for (var c in this.selected_nodes) {
              if (this.selected_nodes[c].onKeyDown) {
                this.selected_nodes[c].onKeyDown(a);
              }
            }
          }
        } else {
          if ("keyup" == a.type && (32 == a.keyCode && (this.dragging_canvas = !1), this.selected_nodes)) {
            for (c in this.selected_nodes) {
              if (this.selected_nodes[c].onKeyUp) {
                this.selected_nodes[c].onKeyUp(a);
              }
            }
          }
        }
        this.graph.change();
        if (b) {
          return a.preventDefault(), a.stopImmediatePropagation(), !1;
        }
      }
    }
  };
  h.prototype.copyToClipboard = function() {
    var a = {nodes:[], links:[]}, b = 0, c = [], d;
    for (d in this.selected_nodes) {
      var e = this.selected_nodes[d];
      e._relative_id = b;
      c.push(e);
      b += 1;
    }
    for (d = 0; d < c.length; ++d) {
      if (e = c[d], b = e.clone()) {
        if (a.nodes.push(b.serialize()), e.inputs && e.inputs.length) {
          for (b = 0; b < e.inputs.length; ++b) {
            var k = e.inputs[b];
            if (k && null != k.link && (k = this.graph.links[k.link])) {
              var g = this.graph.getNodeById(k.origin_id);
              g && this.selected_nodes[g.id] && a.links.push([g._relative_id, k.origin_slot, e._relative_id, k.target_slot]);
            }
          }
        }
      } else {
        console.warn("node type not found: " + e.type);
      }
    }
    localStorage.setItem("litegrapheditor_clipboard", JSON.stringify(a));
  };
  h.prototype.pasteFromClipboard = function() {
    var a = localStorage.getItem("litegrapheditor_clipboard");
    if (a) {
      this.graph.beforeChange();
      a = JSON.parse(a);
      for (var b = [], c = 0; c < a.nodes.length; ++c) {
        var d = a.nodes[c], e = f.createNode(d.type);
        e && (e.configure(d), e.pos[0] += 5, e.pos[1] += 5, this.graph.add(e), b.push(e));
      }
      for (c = 0; c < a.links.length; ++c) {
        d = a.links[c];
        e = b[d[0]];
        var k = b[d[2]];
        e && k ? e.connect(d[1], k, d[3]) : console.warn("Warning, nodes missing on pasting");
      }
      this.selectNodes(b);
      this.graph.afterChange();
    }
  };
  h.prototype.processDrop = function(a) {
    a.preventDefault();
    this.adjustMouseEvent(a);
    var b = [a.canvasX, a.canvasY], c = this.graph ? this.graph.getNodeOnPos(b[0], b[1]) : null;
    if (c) {
      if ((c.onDropFile || c.onDropData) && (b = a.dataTransfer.files) && b.length) {
        for (var d = 0; d < b.length; d++) {
          var e = a.dataTransfer.files[0], k = e.name;
          h.getFileExtension(k);
          if (c.onDropFile) {
            c.onDropFile(e);
          }
          if (c.onDropData) {
            var g = new FileReader;
            g.onload = function(a) {
              c.onDropData(a.target.result, k, e);
            };
            var f = e.type.split("/")[0];
            "text" == f || "" == f ? g.readAsText(e) : "image" == f ? g.readAsDataURL(e) : g.readAsArrayBuffer(e);
          }
        }
      }
      return c.onDropItem && c.onDropItem(event) ? !0 : this.onDropItem ? this.onDropItem(event) : !1;
    }
    b = null;
    this.onDropItem && (b = this.onDropItem(event));
    b || this.checkDropItem(a);
  };
  h.prototype.checkDropItem = function(a) {
    if (a.dataTransfer.files.length) {
      var b = a.dataTransfer.files[0], c = h.getFileExtension(b.name).toLowerCase();
      if (c = f.node_types_by_file_extension[c]) {
        this.graph.beforeChange();
        c = f.createNode(c.type);
        c.pos = [a.canvasX, a.canvasY];
        this.graph.add(c);
        if (c.onDropFile) {
          c.onDropFile(b);
        }
        this.graph.afterChange();
      }
    }
  };
  h.prototype.processNodeDblClicked = function(a) {
    if (this.onShowNodePanel) {
      this.onShowNodePanel(a);
    } else {
      this.showShowNodePanel(a);
    }
    if (this.onNodeDblClicked) {
      this.onNodeDblClicked(a);
    }
    this.setDirty(!0);
  };
  h.prototype.processNodeSelected = function(a, b) {
    this.selectNode(a, b && b.shiftKey);
    if (this.onNodeSelected) {
      this.onNodeSelected(a);
    }
  };
  h.prototype.selectNode = function(a, b) {
    null == a ? this.deselectAllNodes() : this.selectNodes([a], b);
  };
  h.prototype.selectNodes = function(a, b) {
    b || this.deselectAllNodes();
    a = a || this.graph._nodes;
    for (b = 0; b < a.length; ++b) {
      var c = a[b];
      if (!c.is_selected) {
        if (!c.is_selected && c.onSelected) {
          c.onSelected();
        }
        c.is_selected = !0;
        this.selected_nodes[c.id] = c;
        if (c.inputs) {
          for (var d = 0; d < c.inputs.length; ++d) {
            this.highlighted_links[c.inputs[d].link] = !0;
          }
        }
        if (c.outputs) {
          for (d = 0; d < c.outputs.length; ++d) {
            var e = c.outputs[d];
            if (e.links) {
              for (var k = 0; k < e.links.length; ++k) {
                this.highlighted_links[e.links[k]] = !0;
              }
            }
          }
        }
      }
    }
    if (this.onSelectionChange) {
      this.onSelectionChange(this.selected_nodes);
    }
    this.setDirty(!0);
  };
  h.prototype.deselectNode = function(a) {
    if (a.is_selected) {
      if (a.onDeselected) {
        a.onDeselected();
      }
      a.is_selected = !1;
      if (this.onNodeDeselected) {
        this.onNodeDeselected(a);
      }
      if (a.inputs) {
        for (var b = 0; b < a.inputs.length; ++b) {
          delete this.highlighted_links[a.inputs[b].link];
        }
      }
      if (a.outputs) {
        for (b = 0; b < a.outputs.length; ++b) {
          var c = a.outputs[b];
          if (c.links) {
            for (var d = 0; d < c.links.length; ++d) {
              delete this.highlighted_links[c.links[d]];
            }
          }
        }
      }
    }
  };
  h.prototype.deselectAllNodes = function() {
    if (this.graph) {
      for (var a = this.graph._nodes, b = 0, c = a.length; b < c; ++b) {
        var d = a[b];
        if (d.is_selected) {
          if (d.onDeselected) {
            d.onDeselected();
          }
          d.is_selected = !1;
          if (this.onNodeDeselected) {
            this.onNodeDeselected(d);
          }
        }
      }
      this.selected_nodes = {};
      this.current_node = null;
      this.highlighted_links = {};
      if (this.onSelectionChange) {
        this.onSelectionChange(this.selected_nodes);
      }
      this.setDirty(!0);
    }
  };
  h.prototype.deleteSelectedNodes = function() {
    this.graph.beforeChange();
    for (var a in this.selected_nodes) {
      var b = this.selected_nodes[a];
      if (!b.block_delete) {
        if (b.inputs && b.inputs.length && b.outputs && b.outputs.length && f.isValidConnection(b.inputs[0].type, b.outputs[0].type) && b.inputs[0].link && b.outputs[0].links && b.outputs[0].links.length) {
          var c = b.graph.links[b.inputs[0].link], d = b.graph.links[b.outputs[0].links[0]], e = b.getInputNode(0), k = b.getOutputNodes(0)[0];
          e && k && e.connect(c.origin_slot, k, d.target_slot);
        }
        this.graph.remove(b);
        if (this.onNodeDeselected) {
          this.onNodeDeselected(b);
        }
      }
    }
    this.selected_nodes = {};
    this.current_node = null;
    this.highlighted_links = {};
    this.setDirty(!0);
    this.graph.afterChange();
  };
  h.prototype.centerOnNode = function(a) {
    this.ds.offset[0] = -a.pos[0] - 0.5 * a.size[0] + 0.5 * this.canvas.width / this.ds.scale;
    this.ds.offset[1] = -a.pos[1] - 0.5 * a.size[1] + 0.5 * this.canvas.height / this.ds.scale;
    this.setDirty(!0, !0);
  };
  h.prototype.adjustMouseEvent = function(a) {
    if (this.canvas) {
      var b = this.canvas.getBoundingClientRect();
      a.localX = a.clientX - b.left;
      a.localY = a.clientY - b.top;
    } else {
      a.localX = a.clientX, a.localY = a.clientY;
    }
    a.deltaX = a.localX - this.last_mouse_position[0];
    a.deltaY = a.localY - this.last_mouse_position[1];
    this.last_mouse_position[0] = a.localX;
    this.last_mouse_position[1] = a.localY;
    a.canvasX = a.localX / this.ds.scale - this.ds.offset[0];
    a.canvasY = a.localY / this.ds.scale - this.ds.offset[1];
  };
  h.prototype.setZoom = function(a, b) {
    this.ds.changeScale(a, b);
    this.dirty_bgcanvas = this.dirty_canvas = !0;
  };
  h.prototype.convertOffsetToCanvas = function(a, b) {
    return this.ds.convertOffsetToCanvas(a, b);
  };
  h.prototype.convertCanvasToOffset = function(a, b) {
    return this.ds.convertCanvasToOffset(a, b);
  };
  h.prototype.convertEventToCanvasOffset = function(a) {
    var b = this.canvas.getBoundingClientRect();
    return this.convertCanvasToOffset([a.clientX - b.left, a.clientY - b.top]);
  };
  h.prototype.bringToFront = function(a) {
    var b = this.graph._nodes.indexOf(a);
    -1 != b && (this.graph._nodes.splice(b, 1), this.graph._nodes.push(a));
  };
  h.prototype.sendToBack = function(a) {
    var b = this.graph._nodes.indexOf(a);
    -1 != b && (this.graph._nodes.splice(b, 1), this.graph._nodes.unshift(a));
  };
  var L = new Float32Array(4);
  h.prototype.computeVisibleNodes = function(a, b) {
    b = b || [];
    b.length = 0;
    a = a || this.graph._nodes;
    for (var c = 0, d = a.length; c < d; ++c) {
      var e = a[c];
      (!this.live_mode || e.onDrawBackground || e.onDrawForeground) && F(this.visible_area, e.getBounding(L)) && b.push(e);
    }
    return b;
  };
  h.prototype.draw = function(a, b) {
    if (this.canvas && 0 != this.canvas.width && 0 != this.canvas.height) {
      var c = f.getTime();
      this.render_time = 0.001 * (c - this.last_draw_time);
      this.last_draw_time = c;
      this.graph && this.ds.computeVisibleArea();
      (this.dirty_bgcanvas || b || this.always_render_background || this.graph && this.graph._last_trigger_time && 1000 > c - this.graph._last_trigger_time) && this.drawBackCanvas();
      (this.dirty_canvas || a) && this.drawFrontCanvas();
      this.fps = this.render_time ? 1.0 / this.render_time : 0;
      this.frame += 1;
    }
  };
  h.prototype.drawFrontCanvas = function() {
    this.dirty_canvas = !1;
    this.ctx || (this.ctx = this.bgcanvas.getContext("2d"));
    var a = this.ctx;
    if (a) {
      a.start2D && a.start2D();
      var b = this.canvas;
      a.restore();
      a.setTransform(1, 0, 0, 1, 0, 0);
      this.dirty_area && (a.save(), a.beginPath(), a.rect(this.dirty_area[0], this.dirty_area[1], this.dirty_area[2], this.dirty_area[3]), a.clip());
      this.clear_background && a.clearRect(0, 0, b.width, b.height);
      this.bgcanvas == this.canvas ? this.drawBackCanvas() : a.drawImage(this.bgcanvas, 0, 0);
      if (this.onRender) {
        this.onRender(b, a);
      }
      this.show_info && this.renderInfo(a);
      if (this.graph) {
        a.save();
        this.ds.toCanvasContext(a);
        b = this.computeVisibleNodes(null, this.visible_nodes);
        for (var c = 0; c < b.length; ++c) {
          var d = b[c];
          a.save();
          a.translate(d.pos[0], d.pos[1]);
          this.drawNode(d, a);
          a.restore();
        }
        this.render_execution_order && this.drawExecutionOrder(a);
        this.graph.config.links_ontop && (this.live_mode || this.drawConnections(a));
        if (null != this.connecting_pos) {
          a.lineWidth = this.connections_width;
          switch(this.connecting_output.type) {
            case f.EVENT:
              b = f.EVENT_LINK_COLOR;
              break;
            default:
              b = f.CONNECTING_LINK_COLOR;
          }
          this.renderLink(a, this.connecting_pos, [this.graph_mouse[0], this.graph_mouse[1]], null, !1, null, b, this.connecting_output.dir || (this.connecting_node.horizontal ? f.DOWN : f.RIGHT), f.CENTER);
          a.beginPath();
          this.connecting_output.type === f.EVENT || this.connecting_output.shape === f.BOX_SHAPE ? a.rect(this.connecting_pos[0] - 6 + 0.5, this.connecting_pos[1] - 5 + 0.5, 14, 10) : a.arc(this.connecting_pos[0], this.connecting_pos[1], 4, 0, 2 * Math.PI);
          a.fill();
          a.fillStyle = "#ffcc00";
          this._highlight_input && (a.beginPath(), a.arc(this._highlight_input[0], this._highlight_input[1], 6, 0, 2 * Math.PI), a.fill());
        }
        this.dragging_rectangle && (a.strokeStyle = "#FFF", a.strokeRect(this.dragging_rectangle[0], this.dragging_rectangle[1], this.dragging_rectangle[2], this.dragging_rectangle[3]));
        if (this.over_link_center && this.render_link_tooltip) {
          this.drawLinkTooltip(a, this.over_link_center);
        } else {
          if (this.onDrawLinkTooltip) {
            this.onDrawLinkTooltip(a, null);
          }
        }
        if (this.onDrawForeground) {
          this.onDrawForeground(a, this.visible_rect);
        }
        a.restore();
      }
      this._graph_stack && this._graph_stack.length && this.drawSubgraphPanel(a);
      if (this.onDrawOverlay) {
        this.onDrawOverlay(a);
      }
      this.dirty_area && a.restore();
      a.finish2D && a.finish2D();
    }
  };
  h.prototype.drawSubgraphPanel = function(a) {
    var b = this.graph, c = b._subgraph_node;
    if (c) {
      var d = c.inputs ? c.inputs.length : 0, e = Math.floor(1.6 * f.NODE_SLOT_HEIGHT);
      a.fillStyle = "#111";
      a.globalAlpha = 0.8;
      a.beginPath();
      a.roundRect(10, 10, 300, (d + 1) * e + 50, 8);
      a.fill();
      a.globalAlpha = 1;
      a.fillStyle = "#888";
      a.font = "14px Arial";
      a.textAlign = "left";
      a.fillText("Graph Inputs", 20, 34);
      if (this.drawButton(280, 20, 20, 20, "X", "#151515")) {
        this.closeSubgraph();
      } else {
        d = 50;
        a.font = "20px Arial";
        if (c.inputs) {
          for (var k = 0; k < c.inputs.length; ++k) {
            var g = c.inputs[k];
            if (!g.not_subgraph_input) {
              if (this.drawButton(20, d + 2, 280, e - 2)) {
                var p = c.constructor.input_node_type || "graph/input";
                this.graph.beforeChange();
                var h = f.createNode(p);
                h ? (b.add(h), this.block_click = !1, this.last_click_position = null, this.selectNodes([h]), this.node_dragged = h, this.dragging_canvas = !1, h.setProperty("name", g.name), h.setProperty("type", g.type), this.node_dragged.pos[0] = this.graph_mouse[0] - 5, this.node_dragged.pos[1] = this.graph_mouse[1] - 5, this.graph.afterChange()) : console.error("graph input node not found:", p);
              }
              a.fillStyle = "#9C9";
              a.beginPath();
              a.arc(284, d + 0.5 * e, 5, 0, 2 * Math.PI);
              a.fill();
              a.fillStyle = "#AAA";
              a.fillText(g.name, 50, d + 0.75 * e);
              p = a.measureText(g.name);
              a.fillStyle = "#777";
              a.fillText(g.type, 50 + p.width + 10, d + 0.75 * e);
              d += e;
            }
          }
        }
        this.drawButton(20, d + 2, 280, e - 2, "+", "#151515", "#222") && this.showSubgraphPropertiesDialog(c);
      }
    } else {
      console.warn("subgraph without subnode");
    }
  };
  h.prototype.drawButton = function(a, b, c, d, e, k, g, p) {
    var h = this.ctx;
    k = k || f.NODE_DEFAULT_COLOR;
    g = g || "#555";
    p = p || f.NODE_TEXT_COLOR;
    var t = this.mouse, q = f.isInsideRectangle(t[0], t[1], a, b, c, d);
    t = (t = this.last_click_position) && f.isInsideRectangle(t[0], t[1], a, b, c, d);
    h.fillStyle = q ? g : k;
    t && (h.fillStyle = "#AAA");
    h.beginPath();
    h.roundRect(a, b, c, d, 4);
    h.fill();
    null != e && e.constructor == String && (h.fillStyle = p, h.textAlign = "center", h.font = (0.65 * d | 0) + "px Arial", h.fillText(e, a + 0.5 * c, b + 0.75 * d), h.textAlign = "left");
    a = t && !this.block_click;
    t && this.blockClick();
    return a;
  };
  h.prototype.isAreaClicked = function(a, b, c, d, e) {
    var k = this.mouse;
    f.isInsideRectangle(k[0], k[1], a, b, c, d);
    b = (a = (k = this.last_click_position) && f.isInsideRectangle(k[0], k[1], a, b, c, d)) && !this.block_click;
    a && e && this.blockClick();
    return b;
  };
  h.prototype.renderInfo = function(a, b, c) {
    b = b || 10;
    c = c || this.canvas.height - 80;
    a.save();
    a.translate(b, c);
    a.font = "10px Arial";
    a.fillStyle = "#888";
    this.graph ? (a.fillText("T: " + this.graph.globaltime.toFixed(2) + "s", 5, 13), a.fillText("I: " + this.graph.iteration, 5, 26), a.fillText("N: " + this.graph._nodes.length + " [" + this.visible_nodes.length + "]", 5, 39), a.fillText("V: " + this.graph._version, 5, 52), a.fillText("FPS:" + this.fps.toFixed(2), 5, 65)) : a.fillText("No graph selected", 5, 13);
    a.restore();
  };
  h.prototype.drawBackCanvas = function() {
    var a = this.bgcanvas;
    if (a.width != this.canvas.width || a.height != this.canvas.height) {
      a.width = this.canvas.width, a.height = this.canvas.height;
    }
    this.bgctx || (this.bgctx = this.bgcanvas.getContext("2d"));
    var b = this.bgctx;
    b.start && b.start();
    this.clear_background && b.clearRect(0, 0, a.width, a.height);
    if (this._graph_stack && this._graph_stack.length) {
      b.save();
      var c = this.graph._subgraph_node;
      b.strokeStyle = c.bgcolor;
      b.lineWidth = 10;
      b.strokeRect(1, 1, a.width - 2, a.height - 2);
      b.lineWidth = 1;
      b.font = "40px Arial";
      b.textAlign = "center";
      b.fillStyle = c.bgcolor || "#AAA";
      for (var d = "", e = 1; e < this._graph_stack.length; ++e) {
        d += this._graph_stack[e]._subgraph_node.getTitle() + " >> ";
      }
      b.fillText(d + c.getTitle(), 0.5 * a.width, 40);
      b.restore();
    }
    c = !1;
    this.onRenderBackground && (c = this.onRenderBackground(a, b));
    b.restore();
    b.setTransform(1, 0, 0, 1, 0, 0);
    this.visible_links.length = 0;
    if (this.graph) {
      b.save();
      this.ds.toCanvasContext(b);
      if (this.background_image && 0.5 < this.ds.scale && !c) {
        b.globalAlpha = this.zoom_modify_alpha ? (1.0 - 0.5 / this.ds.scale) * this.editor_alpha : this.editor_alpha;
        b.imageSmoothingEnabled = b.mozImageSmoothingEnabled = b.imageSmoothingEnabled = !1;
        if (!this._bg_img || this._bg_img.name != this.background_image) {
          this._bg_img = new Image;
          this._bg_img.name = this.background_image;
          this._bg_img.src = this.background_image;
          var k = this;
          this._bg_img.onload = function() {
            k.draw(!0, !0);
          };
        }
        c = null;
        null == this._pattern && 0 < this._bg_img.width ? (c = b.createPattern(this._bg_img, "repeat"), this._pattern_img = this._bg_img, this._pattern = c) : c = this._pattern;
        c && (b.fillStyle = c, b.fillRect(this.visible_area[0], this.visible_area[1], this.visible_area[2], this.visible_area[3]), b.fillStyle = "transparent");
        b.globalAlpha = 1.0;
        b.imageSmoothingEnabled = b.mozImageSmoothingEnabled = b.imageSmoothingEnabled = !0;
      }
      this.graph._groups.length && !this.live_mode && this.drawGroups(a, b);
      if (this.onDrawBackground) {
        this.onDrawBackground(b, this.visible_area);
      }
      this.onBackgroundRender && (console.error("WARNING! onBackgroundRender deprecated, now is named onDrawBackground "), this.onBackgroundRender = null);
      this.render_canvas_border && (b.strokeStyle = "#235", b.strokeRect(0, 0, a.width, a.height));
      this.render_connections_shadows ? (b.shadowColor = "#000", b.shadowOffsetX = 0, b.shadowOffsetY = 0, b.shadowBlur = 6) : b.shadowColor = "rgba(0,0,0,0)";
      this.live_mode || this.drawConnections(b);
      b.shadowColor = "rgba(0,0,0,0)";
      b.restore();
    }
    b.finish && b.finish();
    this.dirty_bgcanvas = !1;
    this.dirty_canvas = !0;
  };
  var E = new Float32Array(2);
  h.prototype.drawNode = function(a, b) {
    this.current_node = a;
    var c = a.color || a.constructor.color || f.NODE_DEFAULT_COLOR, d = a.bgcolor || a.constructor.bgcolor || f.NODE_DEFAULT_BGCOLOR, e = 0.6 > this.ds.scale;
    if (this.live_mode) {
      if (!a.flags.collapsed && (b.shadowColor = "transparent", a.onDrawForeground)) {
        a.onDrawForeground(b, this, this.canvas);
      }
    } else {
      var k = this.editor_alpha;
      b.globalAlpha = k;
      this.render_shadows && !e ? (b.shadowColor = f.DEFAULT_SHADOW_COLOR, b.shadowOffsetX = 2 * this.ds.scale, b.shadowOffsetY = 2 * this.ds.scale, b.shadowBlur = 3 * this.ds.scale) : b.shadowColor = "transparent";
      if (!a.flags.collapsed || !a.onDrawCollapsed || 1 != a.onDrawCollapsed(b, this)) {
        var g = a._shape || f.BOX_SHAPE;
        E.set(a.size);
        var p = a.horizontal;
        if (a.flags.collapsed) {
          b.font = this.inner_text_font;
          var h = a.getTitle ? a.getTitle() : a.title;
          null != h && (a._collapsed_width = Math.min(a.size[0], b.measureText(h).width + 2 * f.NODE_TITLE_HEIGHT), E[0] = a._collapsed_width, E[1] = 0);
        }
        a.clip_area && (b.save(), b.beginPath(), g == f.BOX_SHAPE ? b.rect(0, 0, E[0], E[1]) : g == f.ROUND_SHAPE ? b.roundRect(0, 0, E[0], E[1], 10) : g == f.CIRCLE_SHAPE && b.arc(0.5 * E[0], 0.5 * E[1], 0.5 * E[0], 0, 2 * Math.PI), b.clip());
        a.has_errors && (d = "red");
        this.drawNodeShape(a, b, E, c, d, a.is_selected, a.mouseOver);
        b.shadowColor = "transparent";
        if (a.onDrawForeground) {
          a.onDrawForeground(b, this, this.canvas);
        }
        b.textAlign = p ? "center" : "left";
        b.font = this.inner_text_font;
        d = !e;
        g = this.connecting_output;
        b.lineWidth = 1;
        h = 0;
        var t = new Float32Array(2);
        if (!a.flags.collapsed) {
          if (a.inputs) {
            for (c = 0; c < a.inputs.length; c++) {
              var q = a.inputs[c];
              b.globalAlpha = k;
              this.connecting_node && !f.isValidConnection(q.type, g.type) && (b.globalAlpha = 0.4 * k);
              b.fillStyle = null != q.link ? q.color_on || this.default_connection_color.input_on : q.color_off || this.default_connection_color.input_off;
              var m = a.getConnectionPos(!0, c, t);
              m[0] -= a.pos[0];
              m[1] -= a.pos[1];
              h < m[1] + 0.5 * f.NODE_SLOT_HEIGHT && (h = m[1] + 0.5 * f.NODE_SLOT_HEIGHT);
              b.beginPath();
              q.type === f.EVENT || q.shape === f.BOX_SHAPE ? p ? b.rect(m[0] - 5 + 0.5, m[1] - 8 + 0.5, 10, 14) : b.rect(m[0] - 6 + 0.5, m[1] - 5 + 0.5, 14, 10) : q.shape === f.ARROW_SHAPE ? (b.moveTo(m[0] + 8, m[1] + 0.5), b.lineTo(m[0] - 4, m[1] + 6 + 0.5), b.lineTo(m[0] - 4, m[1] - 6 + 0.5), b.closePath()) : e ? b.rect(m[0] - 4, m[1] - 4, 8, 8) : b.arc(m[0], m[1], 4, 0, 2 * Math.PI);
              b.fill();
              if (d) {
                var l = null != q.label ? q.label : q.name;
                l && (b.fillStyle = f.NODE_TEXT_COLOR, p || q.dir == f.UP ? b.fillText(l, m[0], m[1] - 10) : b.fillText(l, m[0] + 10, m[1] + 5));
              }
            }
          }
          this.connecting_node && (b.globalAlpha = 0.4 * k);
          b.textAlign = p ? "center" : "right";
          b.strokeStyle = "black";
          if (a.outputs) {
            for (c = 0; c < a.outputs.length; c++) {
              if (q = a.outputs[c], m = a.getConnectionPos(!1, c, t), m[0] -= a.pos[0], m[1] -= a.pos[1], h < m[1] + 0.5 * f.NODE_SLOT_HEIGHT && (h = m[1] + 0.5 * f.NODE_SLOT_HEIGHT), b.fillStyle = q.links && q.links.length ? q.color_on || this.default_connection_color.output_on : q.color_off || this.default_connection_color.output_off, b.beginPath(), q.type === f.EVENT || q.shape === f.BOX_SHAPE ? p ? b.rect(m[0] - 5 + 0.5, m[1] - 8 + 0.5, 10, 14) : b.rect(m[0] - 6 + 0.5, m[1] - 5 + 0.5, 14, 10) : 
              q.shape === f.ARROW_SHAPE ? (b.moveTo(m[0] + 8, m[1] + 0.5), b.lineTo(m[0] - 4, m[1] + 6 + 0.5), b.lineTo(m[0] - 4, m[1] - 6 + 0.5), b.closePath()) : e ? b.rect(m[0] - 4, m[1] - 4, 8, 8) : b.arc(m[0], m[1], 4, 0, 2 * Math.PI), b.fill(), e || b.stroke(), d && (l = null != q.label ? q.label : q.name)) {
                b.fillStyle = f.NODE_TEXT_COLOR, p || q.dir == f.DOWN ? b.fillText(l, m[0], m[1] - 8) : b.fillText(l, m[0] - 10, m[1] + 5);
              }
            }
          }
          b.textAlign = "left";
          b.globalAlpha = 1;
          if (a.widgets) {
            q = h;
            if (p || a.widgets_up) {
              q = 2;
            }
            null != a.widgets_start_y && (q = a.widgets_start_y);
            this.drawNodeWidgets(a, q, b, this.node_widget && this.node_widget[0] == a ? this.node_widget[1] : null);
          }
        } else {
          if (this.render_collapsed_slots) {
            e = k = null;
            if (a.inputs) {
              for (c = 0; c < a.inputs.length; c++) {
                if (q = a.inputs[c], null != q.link) {
                  k = q;
                  break;
                }
              }
            }
            if (a.outputs) {
              for (c = 0; c < a.outputs.length; c++) {
                q = a.outputs[c], q.links && q.links.length && (e = q);
              }
            }
            k && (k = 0, c = -0.5 * f.NODE_TITLE_HEIGHT, p && (k = 0.5 * a._collapsed_width, c = -f.NODE_TITLE_HEIGHT), b.fillStyle = "#686", b.beginPath(), q.type === f.EVENT || q.shape === f.BOX_SHAPE ? b.rect(k - 7 + 0.5, c - 4, 14, 8) : q.shape === f.ARROW_SHAPE ? (b.moveTo(k + 8, c), b.lineTo(k + -4, c - 4), b.lineTo(k + -4, c + 4), b.closePath()) : b.arc(k, c, 4, 0, 2 * Math.PI), b.fill());
            e && (k = a._collapsed_width, c = -0.5 * f.NODE_TITLE_HEIGHT, p && (k = 0.5 * a._collapsed_width, c = 0), b.fillStyle = "#686", b.strokeStyle = "black", b.beginPath(), q.type === f.EVENT || q.shape === f.BOX_SHAPE ? b.rect(k - 7 + 0.5, c - 4, 14, 8) : q.shape === f.ARROW_SHAPE ? (b.moveTo(k + 6, c), b.lineTo(k - 6, c - 4), b.lineTo(k - 6, c + 4), b.closePath()) : b.arc(k, c, 4, 0, 2 * Math.PI), b.fill());
          }
        }
        a.clip_area && b.restore();
        b.globalAlpha = 1.0;
      }
    }
  };
  h.prototype.drawLinkTooltip = function(a, b) {
    var c = b._pos;
    a.fillStyle = "black";
    a.beginPath();
    a.arc(c[0], c[1], 3, 0, 2 * Math.PI);
    a.fill();
    if (null != b.data && (!this.onDrawLinkTooltip || 1 != this.onDrawLinkTooltip(a, b, this)) && (b = b.data, b = b.constructor === Number ? b.toFixed(2) : b.constructor === String ? '"' + b + '"' : b.constructor === Boolean ? String(b) : b.toToolTip ? b.toToolTip() : "[" + b.constructor.name + "]", null != b)) {
      b = b.substr(0, 30);
      a.font = "14px Courier New";
      var d = a.measureText(b).width + 20;
      a.shadowColor = "black";
      a.shadowOffsetX = 2;
      a.shadowOffsetY = 2;
      a.shadowBlur = 3;
      a.fillStyle = "#454";
      a.beginPath();
      a.roundRect(c[0] - 0.5 * d, c[1] - 15 - 24, d, 24, 3, 3);
      a.moveTo(c[0] - 10, c[1] - 15);
      a.lineTo(c[0] + 10, c[1] - 15);
      a.lineTo(c[0], c[1] - 5);
      a.fill();
      a.shadowColor = "transparent";
      a.textAlign = "center";
      a.fillStyle = "#CEC";
      a.fillText(b, c[0], c[1] - 15 - 24 * 0.3);
    }
  };
  var w = new Float32Array(4);
  h.prototype.drawNodeShape = function(a, b, c, d, e, k, g) {
    b.strokeStyle = d;
    b.fillStyle = e;
    e = f.NODE_TITLE_HEIGHT;
    var p = 0.5 > this.ds.scale, l = a._shape || a.constructor.shape || f.ROUND_SHAPE, t = a.constructor.title_mode, q = !0;
    t == f.TRANSPARENT_TITLE ? q = !1 : t == f.AUTOHIDE_TITLE && g && (q = !0);
    w[0] = 0;
    w[1] = q ? -e : 0;
    w[2] = c[0] + 1;
    w[3] = q ? c[1] + e : c[1];
    g = b.globalAlpha;
    b.beginPath();
    l == f.BOX_SHAPE || p ? b.fillRect(w[0], w[1], w[2], w[3]) : l == f.ROUND_SHAPE || l == f.CARD_SHAPE ? b.roundRect(w[0], w[1], w[2], w[3], this.round_radius, l == f.CARD_SHAPE ? 0 : this.round_radius) : l == f.CIRCLE_SHAPE && b.arc(0.5 * c[0], 0.5 * c[1], 0.5 * c[0], 0, 2 * Math.PI);
    b.fill();
    a.flags.collapsed || (b.shadowColor = "transparent", b.fillStyle = "rgba(0,0,0,0.2)", b.fillRect(0, -1, w[2], 2));
    b.shadowColor = "transparent";
    if (a.onDrawBackground) {
      a.onDrawBackground(b, this, this.canvas, this.graph_mouse);
    }
    if (q || t == f.TRANSPARENT_TITLE) {
      if (a.onDrawTitleBar) {
        a.onDrawTitleBar(b, e, c, this.ds.scale, d);
      } else {
        if (t != f.TRANSPARENT_TITLE && (a.constructor.title_color || this.render_title_colored)) {
          q = a.constructor.title_color || d;
          a.flags.collapsed && (b.shadowColor = f.DEFAULT_SHADOW_COLOR);
          if (this.use_gradients) {
            var m = h.gradients[q];
            m || (m = h.gradients[q] = b.createLinearGradient(0, 0, 400, 0), m.addColorStop(0, q), m.addColorStop(1, "#000"));
            b.fillStyle = m;
          } else {
            b.fillStyle = q;
          }
          b.beginPath();
          l == f.BOX_SHAPE || p ? b.rect(0, -e, c[0] + 1, e) : (l == f.ROUND_SHAPE || l == f.CARD_SHAPE) && b.roundRect(0, -e, c[0] + 1, e, this.round_radius, a.flags.collapsed ? this.round_radius : 0);
          b.fill();
          b.shadowColor = "transparent";
        }
      }
      if (a.onDrawTitleBox) {
        a.onDrawTitleBox(b, e, c, this.ds.scale);
      } else {
        l == f.ROUND_SHAPE || l == f.CIRCLE_SHAPE || l == f.CARD_SHAPE ? (p && (b.fillStyle = "black", b.beginPath(), b.arc(0.5 * e, -0.5 * e, 6, 0, 2 * Math.PI), b.fill()), b.fillStyle = a.boxcolor || f.NODE_DEFAULT_BOXCOLOR, p ? b.fillRect(0.5 * e - 5, -0.5 * e - 5, 10, 10) : (b.beginPath(), b.arc(0.5 * e, -0.5 * e, 5, 0, 2 * Math.PI), b.fill())) : (p && (b.fillStyle = "black", b.fillRect(0.5 * (e - 10) - 1, -0.5 * (e + 10) - 1, 12, 12)), b.fillStyle = a.boxcolor || f.NODE_DEFAULT_BOXCOLOR, b.fillRect(0.5 * 
        (e - 10), -0.5 * (e + 10), 10, 10));
      }
      b.globalAlpha = g;
      if (a.onDrawTitleText) {
        a.onDrawTitleText(b, e, c, this.ds.scale, this.title_text_font, k);
      }
      !p && (b.font = this.title_text_font, g = String(a.getTitle())) && (b.fillStyle = k ? f.NODE_SELECTED_TITLE_COLOR : a.constructor.title_text_color || this.node_title_color, a.flags.collapsed ? (b.textAlign = "left", b.measureText(g), b.fillText(g.substr(0, 20), e, f.NODE_TITLE_TEXT_Y - e), b.textAlign = "left") : (b.textAlign = "left", b.fillText(g, e, f.NODE_TITLE_TEXT_Y - e)));
      a.flags.collapsed || !a.subgraph || a.skip_subgraph_button || (g = f.NODE_TITLE_HEIGHT, q = a.size[0] - g, m = f.isInsideRectangle(this.graph_mouse[0] - a.pos[0], this.graph_mouse[1] - a.pos[1], q + 2, -g + 2, g - 4, g - 4), b.fillStyle = m ? "#888" : "#555", l == f.BOX_SHAPE || p ? b.fillRect(q + 2, -g + 2, g - 4, g - 4) : (b.beginPath(), b.roundRect(q + 2, -g + 2, g - 4, g - 4, 4), b.fill()), b.fillStyle = "#333", b.beginPath(), b.moveTo(q + 0.2 * g, 0.6 * -g), b.lineTo(q + 0.8 * g, 0.6 * 
      -g), b.lineTo(q + 0.5 * g, 0.3 * -g), b.fill());
      if (a.onDrawTitle) {
        a.onDrawTitle(b);
      }
    }
    if (k) {
      if (a.onBounding) {
        a.onBounding(w);
      }
      t == f.TRANSPARENT_TITLE && (w[1] -= e, w[3] += e);
      b.lineWidth = 1;
      b.globalAlpha = 0.8;
      b.beginPath();
      l == f.BOX_SHAPE ? b.rect(-6 + w[0], -6 + w[1], 12 + w[2], 12 + w[3]) : l == f.ROUND_SHAPE || l == f.CARD_SHAPE && a.flags.collapsed ? b.roundRect(-6 + w[0], -6 + w[1], 12 + w[2], 12 + w[3], 2 * this.round_radius) : l == f.CARD_SHAPE ? b.roundRect(-6 + w[0], -6 + w[1], 12 + w[2], 12 + w[3], 2 * this.round_radius, 2) : l == f.CIRCLE_SHAPE && b.arc(0.5 * c[0], 0.5 * c[1], 0.5 * c[0] + 6, 0, 2 * Math.PI);
      b.strokeStyle = f.NODE_BOX_OUTLINE_COLOR;
      b.stroke();
      b.strokeStyle = d;
      b.globalAlpha = 1;
    }
  };
  var J = new Float32Array(4), B = new Float32Array(4), M = new Float32Array(2), N = new Float32Array(2);
  h.prototype.drawConnections = function(a) {
    var b = f.getTime(), c = this.visible_area;
    J[0] = c[0] - 20;
    J[1] = c[1] - 20;
    J[2] = c[2] + 40;
    J[3] = c[3] + 40;
    a.lineWidth = this.connections_width;
    a.fillStyle = "#AAA";
    a.strokeStyle = "#AAA";
    a.globalAlpha = this.editor_alpha;
    c = this.graph._nodes;
    for (var d = 0, e = c.length; d < e; ++d) {
      var k = c[d];
      if (k.inputs && k.inputs.length) {
        for (var g = 0; g < k.inputs.length; ++g) {
          var h = k.inputs[g];
          if (h && null != h.link && (h = this.graph.links[h.link])) {
            var l = this.graph.getNodeById(h.origin_id);
            if (null != l) {
              var t = h.origin_slot;
              var q = -1 == t ? [l.pos[0] + 10, l.pos[1] + 10] : l.getConnectionPos(!1, t, M);
              var m = k.getConnectionPos(!0, g, N);
              B[0] = q[0];
              B[1] = q[1];
              B[2] = m[0] - q[0];
              B[3] = m[1] - q[1];
              0 > B[2] && (B[0] += B[2], B[2] = Math.abs(B[2]));
              0 > B[3] && (B[1] += B[3], B[3] = Math.abs(B[3]));
              if (F(B, J)) {
                var n = l.outputs[t];
                t = k.inputs[g];
                if (n && t && (l = n.dir || (l.horizontal ? f.DOWN : f.RIGHT), t = t.dir || (k.horizontal ? f.UP : f.LEFT), this.renderLink(a, q, m, h, !1, 0, null, l, t), h && h._last_time && 1000 > b - h._last_time)) {
                  n = 2.0 - 0.002 * (b - h._last_time);
                  var v = a.globalAlpha;
                  a.globalAlpha = v * n;
                  this.renderLink(a, q, m, h, !0, n, "white", l, t);
                  a.globalAlpha = v;
                }
              }
            }
          }
        }
      }
    }
    a.globalAlpha = 1;
  };
  h.prototype.renderLink = function(a, b, c, d, e, k, g, p, l, t) {
    d && this.visible_links.push(d);
    !g && d && (g = d.color || h.link_type_colors[d.type]);
    g || (g = this.default_link_color);
    null != d && this.highlighted_links[d.id] && (g = "#FFF");
    p = p || f.RIGHT;
    l = l || f.LEFT;
    var q = K(b, c);
    this.render_connections_border && 0.6 < this.ds.scale && (a.lineWidth = this.connections_width + 4);
    a.lineJoin = "round";
    t = t || 1;
    1 < t && (a.lineWidth = 0.5);
    a.beginPath();
    for (var m = 0; m < t; m += 1) {
      var n = 5 * (m - 0.5 * (t - 1));
      if (this.links_render_mode == f.SPLINE_LINK) {
        a.moveTo(b[0], b[1] + n);
        var v = 0, x = 0, r = 0, A = 0;
        switch(p) {
          case f.LEFT:
            v = -0.25 * q;
            break;
          case f.RIGHT:
            v = 0.25 * q;
            break;
          case f.UP:
            x = -0.25 * q;
            break;
          case f.DOWN:
            x = 0.25 * q;
        }
        switch(l) {
          case f.LEFT:
            r = -0.25 * q;
            break;
          case f.RIGHT:
            r = 0.25 * q;
            break;
          case f.UP:
            A = -0.25 * q;
            break;
          case f.DOWN:
            A = 0.25 * q;
        }
        a.bezierCurveTo(b[0] + v, b[1] + x + n, c[0] + r, c[1] + A + n, c[0], c[1] + n);
      } else {
        if (this.links_render_mode == f.LINEAR_LINK) {
          a.moveTo(b[0], b[1] + n);
          A = r = x = v = 0;
          switch(p) {
            case f.LEFT:
              v = -1;
              break;
            case f.RIGHT:
              v = 1;
              break;
            case f.UP:
              x = -1;
              break;
            case f.DOWN:
              x = 1;
          }
          switch(l) {
            case f.LEFT:
              r = -1;
              break;
            case f.RIGHT:
              r = 1;
              break;
            case f.UP:
              A = -1;
              break;
            case f.DOWN:
              A = 1;
          }
          a.lineTo(b[0] + 15 * v, b[1] + 15 * x + n);
          a.lineTo(c[0] + 15 * r, c[1] + 15 * A + n);
          a.lineTo(c[0], c[1] + n);
        } else {
          if (this.links_render_mode == f.STRAIGHT_LINK) {
            a.moveTo(b[0], b[1]), n = b[0], v = b[1], x = c[0], r = c[1], p == f.RIGHT ? n += 10 : v += 10, l == f.LEFT ? x -= 10 : r -= 10, a.lineTo(n, v), a.lineTo(0.5 * (n + x), v), a.lineTo(0.5 * (n + x), r), a.lineTo(x, r), a.lineTo(c[0], c[1]);
          } else {
            return;
          }
        }
      }
    }
    this.render_connections_border && 0.6 < this.ds.scale && !e && (a.strokeStyle = "rgba(0,0,0,0.5)", a.stroke());
    a.lineWidth = this.connections_width;
    a.fillStyle = a.strokeStyle = g;
    a.stroke();
    e = this.computeConnectionPoint(b, c, 0.5, p, l);
    d && d._pos && (d._pos[0] = e[0], d._pos[1] = e[1]);
    0.6 <= this.ds.scale && this.highquality_render && l != f.CENTER && (this.render_connection_arrows && (m = this.computeConnectionPoint(b, c, 0.25, p, l), q = this.computeConnectionPoint(b, c, 0.26, p, l), d = this.computeConnectionPoint(b, c, 0.75, p, l), t = this.computeConnectionPoint(b, c, 0.76, p, l), this.render_curved_connections ? (q = -Math.atan2(q[0] - m[0], q[1] - m[1]), t = -Math.atan2(t[0] - d[0], t[1] - d[1])) : t = q = c[1] > b[1] ? 0 : Math.PI, a.save(), a.translate(m[0], m[1]), 
    a.rotate(q), a.beginPath(), a.moveTo(-5, -3), a.lineTo(0, 7), a.lineTo(5, -3), a.fill(), a.restore(), a.save(), a.translate(d[0], d[1]), a.rotate(t), a.beginPath(), a.moveTo(-5, -3), a.lineTo(0, 7), a.lineTo(5, -3), a.fill(), a.restore()), a.beginPath(), a.arc(e[0], e[1], 5, 0, 2 * Math.PI), a.fill());
    if (k) {
      for (a.fillStyle = g, m = 0; 5 > m; ++m) {
        k = (0.001 * f.getTime() + 0.2 * m) % 1, e = this.computeConnectionPoint(b, c, k, p, l), a.beginPath(), a.arc(e[0], e[1], 5, 0, 2 * Math.PI), a.fill();
      }
    }
  };
  h.prototype.computeConnectionPoint = function(a, b, c, d, e) {
    d = d || f.RIGHT;
    e = e || f.LEFT;
    var k = K(a, b), g = [a[0], a[1]], h = [b[0], b[1]];
    switch(d) {
      case f.LEFT:
        g[0] += -0.25 * k;
        break;
      case f.RIGHT:
        g[0] += 0.25 * k;
        break;
      case f.UP:
        g[1] += -0.25 * k;
        break;
      case f.DOWN:
        g[1] += 0.25 * k;
    }
    switch(e) {
      case f.LEFT:
        h[0] += -0.25 * k;
        break;
      case f.RIGHT:
        h[0] += 0.25 * k;
        break;
      case f.UP:
        h[1] += -0.25 * k;
        break;
      case f.DOWN:
        h[1] += 0.25 * k;
    }
    d = (1 - c) * (1 - c) * (1 - c);
    e = 3 * (1 - c) * (1 - c) * c;
    k = 3 * (1 - c) * c * c;
    c *= c * c;
    return [d * a[0] + e * g[0] + k * h[0] + c * b[0], d * a[1] + e * g[1] + k * h[1] + c * b[1]];
  };
  h.prototype.drawExecutionOrder = function(a) {
    a.shadowColor = "transparent";
    a.globalAlpha = 0.25;
    a.textAlign = "center";
    a.strokeStyle = "white";
    a.globalAlpha = 0.75;
    for (var b = this.visible_nodes, c = 0; c < b.length; ++c) {
      var d = b[c];
      a.fillStyle = "black";
      a.fillRect(d.pos[0] - f.NODE_TITLE_HEIGHT, d.pos[1] - f.NODE_TITLE_HEIGHT, f.NODE_TITLE_HEIGHT, f.NODE_TITLE_HEIGHT);
      0 == d.order && a.strokeRect(d.pos[0] - f.NODE_TITLE_HEIGHT + 0.5, d.pos[1] - f.NODE_TITLE_HEIGHT + 0.5, f.NODE_TITLE_HEIGHT, f.NODE_TITLE_HEIGHT);
      a.fillStyle = "#FFF";
      a.fillText(d.order, d.pos[0] + -0.5 * f.NODE_TITLE_HEIGHT, d.pos[1] - 6);
    }
    a.globalAlpha = 1;
  };
  h.prototype.drawNodeWidgets = function(a, b, c, d) {
    if (!a.widgets || !a.widgets.length) {
      return 0;
    }
    var e = a.size[0], k = a.widgets;
    b += 2;
    var g = f.NODE_WIDGET_HEIGHT, h = 0.5 < this.ds.scale;
    c.save();
    c.globalAlpha = this.editor_alpha;
    for (var l = f.WIDGET_OUTLINE_COLOR, t = f.WIDGET_BGCOLOR, q = f.WIDGET_TEXT_COLOR, m = f.WIDGET_SECONDARY_TEXT_COLOR, n = 0; n < k.length; ++n) {
      var v = k[n], r = b;
      v.y && (r = v.y);
      v.last_y = r;
      c.strokeStyle = l;
      c.fillStyle = "#222";
      c.textAlign = "left";
      v.disabled && (c.globalAlpha *= 0.5);
      var u = v.width || e;
      switch(v.type) {
        case "button":
          v.clicked && (c.fillStyle = "#AAA", v.clicked = !1, this.dirty_canvas = !0);
          c.fillRect(15, r, u - 30, g);
          h && !v.disabled && c.strokeRect(15, r, u - 30, g);
          h && (c.textAlign = "center", c.fillStyle = q, c.fillText(v.name, 0.5 * u, r + 0.7 * g));
          break;
        case "toggle":
          c.textAlign = "left";
          c.strokeStyle = l;
          c.fillStyle = t;
          c.beginPath();
          h ? c.roundRect(15, b, u - 30, g, 0.5 * g) : c.rect(15, b, u - 30, g);
          c.fill();
          h && !v.disabled && c.stroke();
          c.fillStyle = v.value ? "#89A" : "#333";
          c.beginPath();
          c.arc(u - 30, r + 0.5 * g, 0.36 * g, 0, 2 * Math.PI);
          c.fill();
          h && (c.fillStyle = m, null != v.name && c.fillText(v.name, 30, r + 0.7 * g), c.fillStyle = v.value ? q : m, c.textAlign = "right", c.fillText(v.value ? v.options.on || "true" : v.options.off || "false", u - 40, r + 0.7 * g));
          break;
        case "slider":
          c.fillStyle = t;
          c.fillRect(15, r, u - 30, g);
          var A = v.options.max - v.options.min, H = (v.value - v.options.min) / A;
          c.fillStyle = d == v ? "#89A" : "#678";
          c.fillRect(15, r, H * (u - 30), g);
          h && !v.disabled && c.strokeRect(15, r, u - 30, g);
          v.marker && (A = (v.marker - v.options.min) / A, c.fillStyle = "#AA9", c.fillRect(15 + A * (u - 30), r, 2, g));
          h && (c.textAlign = "center", c.fillStyle = q, c.fillText(v.name + "  " + Number(v.value).toFixed(3), 0.5 * u, r + 0.7 * g));
          break;
        case "number":
        case "combo":
          c.textAlign = "left";
          c.strokeStyle = l;
          c.fillStyle = t;
          c.beginPath();
          h ? c.roundRect(15, b, u - 30, g, 0.5 * g) : c.rect(15, b, u - 30, g);
          c.fill();
          h && (v.disabled || c.stroke(), c.fillStyle = q, v.disabled || (c.beginPath(), c.moveTo(31, b + 5), c.lineTo(21, b + 0.5 * g), c.lineTo(31, b + g - 5), c.fill(), c.beginPath(), c.moveTo(u - 15 - 16, b + 5), c.lineTo(u - 15 - 6, b + 0.5 * g), c.lineTo(u - 15 - 16, b + g - 5), c.fill()), c.fillStyle = m, c.fillText(v.name, 35, r + 0.7 * g), c.fillStyle = q, c.textAlign = "right", "number" == v.type ? c.fillText(Number(v.value).toFixed(void 0 !== v.options.precision ? v.options.precision : 
          3), u - 30 - 20, r + 0.7 * g) : (A = v.value, v.options.values && (H = v.options.values, H.constructor === Function && (H = H()), H && H.constructor !== Array && (A = H[v.value])), c.fillText(A, u - 30 - 20, r + 0.7 * g)));
          break;
        case "string":
        case "text":
          c.textAlign = "left";
          c.strokeStyle = l;
          c.fillStyle = t;
          c.beginPath();
          h ? c.roundRect(15, b, u - 30, g, 0.5 * g) : c.rect(15, b, u - 30, g);
          c.fill();
          h && (c.save(), c.beginPath(), c.rect(15, b, u - 30, g), c.clip(), c.stroke(), c.fillStyle = m, null != v.name && c.fillText(v.name, 30, r + 0.7 * g), c.fillStyle = q, c.textAlign = "right", c.fillText(String(v.value).substr(0, 30), u - 30, r + 0.7 * g), c.restore());
          break;
        default:
          v.draw && v.draw(c, a, u, r, g);
      }
      b += (v.computeSize ? v.computeSize(u)[1] : g) + 4;
      c.globalAlpha = this.editor_alpha;
    }
    c.restore();
    c.textAlign = "left";
  };
  h.prototype.processNodeWidgets = function(a, b, c, d) {
    function e(d, e) {
      d.value = e;
      d.options && d.options.property && void 0 !== a.properties[d.options.property] && a.setProperty(d.options.property, e);
      d.callback && d.callback(d.value, l, a, b, c);
    }
    if (!a.widgets || !a.widgets.length) {
      return null;
    }
    for (var k = b[0] - a.pos[0], g = b[1] - a.pos[1], h = a.size[0], l = this, t = this.getCanvasWindow(), q = 0; q < a.widgets.length; ++q) {
      var m = a.widgets[q];
      if (m && !m.disabled) {
        var n = m.computeSize ? m.computeSize(h)[1] : f.NODE_WIDGET_HEIGHT, r = m.width || h;
        if (m == d || !(6 > k || k > r - 12 || g < m.last_y || g > m.last_y + n)) {
          d = m.value;
          switch(m.type) {
            case "button":
              if ("mousemove" === c.type) {
                break;
              }
              m.callback && setTimeout(function() {
                m.callback(m, l, a, b, c);
              }, 20);
              this.dirty_canvas = m.clicked = !0;
              break;
            case "slider":
              t = Math.clamp((k - 10) / (r - 20), 0, 1);
              m.value = m.options.min + (m.options.max - m.options.min) * t;
              m.callback && setTimeout(function() {
                e(m, m.value);
              }, 20);
              this.dirty_canvas = !0;
              break;
            case "number":
            case "combo":
              d = m.value;
              if ("mousemove" == c.type && "number" == m.type) {
                m.value += 0.1 * c.deltaX * (m.options.step || 1), null != m.options.min && m.value < m.options.min && (m.value = m.options.min), null != m.options.max && m.value > m.options.max && (m.value = m.options.max);
              } else {
                if ("mousedown" == c.type) {
                  var u = m.options.values;
                  u && u.constructor === Function && (u = m.options.values(m, a));
                  var w = null;
                  "number" != m.type && (w = u.constructor === Array ? u : Object.keys(u));
                  k = 40 > k ? -1 : k > r - 40 ? 1 : 0;
                  if ("number" == m.type) {
                    m.value += 0.1 * k * (m.options.step || 1), null != m.options.min && m.value < m.options.min && (m.value = m.options.min), null != m.options.max && m.value > m.options.max && (m.value = m.options.max);
                  } else {
                    if (k) {
                      t = -1, this.last_mouseclick = 0, t = u.constructor === Object ? w.indexOf(String(m.value)) + k : w.indexOf(m.value) + k, t >= w.length && (t = w.length - 1), 0 > t && (t = 0), m.value = u.constructor === Array ? u[t] : t;
                    } else {
                      var A = u != w ? Object.values(u) : u;
                      new f.ContextMenu(A, {scale:Math.max(1, this.ds.scale), event:c, className:"dark", callback:function(a, b, c) {
                        u != w && (a = A.indexOf(a));
                        this.value = a;
                        e(this, a);
                        l.dirty_canvas = !0;
                        return !1;
                      }.bind(m)}, t);
                    }
                  }
                } else {
                  "mouseup" == c.type && "number" == m.type && (k = 40 > k ? -1 : k > r - 40 ? 1 : 0, 200 > c.click_time && 0 == k && this.prompt("Value", m.value, function(a) {
                    this.value = Number(a);
                    e(this, this.value);
                  }.bind(m), c));
                }
              }
              d != m.value && setTimeout(function() {
                e(this, this.value);
              }.bind(m), 20);
              this.dirty_canvas = !0;
              break;
            case "toggle":
              "mousedown" == c.type && (m.value = !m.value, setTimeout(function() {
                e(m, m.value);
              }, 20));
              break;
            case "string":
            case "text":
              "mousedown" == c.type && this.prompt("Value", m.value, function(a) {
                this.value = a;
                e(this, a);
              }.bind(m), c);
              break;
            default:
              m.mouse && (this.dirty_canvas = m.mouse(c, [k, g], a));
          }
          if (d != m.value) {
            if (a.onWidgetChanged) {
              a.onWidgetChanged(m.name, m.value, d, m);
            }
            a.graph._version++;
          }
          return m;
        }
      }
    }
    return null;
  };
  h.prototype.drawGroups = function(a, b) {
    if (this.graph) {
      a = this.graph._groups;
      b.save();
      b.globalAlpha = 0.5 * this.editor_alpha;
      for (var c = 0; c < a.length; ++c) {
        var d = a[c];
        if (F(this.visible_area, d._bounding)) {
          b.fillStyle = d.color || "#335";
          b.strokeStyle = d.color || "#335";
          var e = d._pos, k = d._size;
          b.globalAlpha = 0.25 * this.editor_alpha;
          b.beginPath();
          b.rect(e[0] + 0.5, e[1] + 0.5, k[0], k[1]);
          b.fill();
          b.globalAlpha = this.editor_alpha;
          b.stroke();
          b.beginPath();
          b.moveTo(e[0] + k[0], e[1] + k[1]);
          b.lineTo(e[0] + k[0] - 10, e[1] + k[1]);
          b.lineTo(e[0] + k[0], e[1] + k[1] - 10);
          b.fill();
          k = d.font_size || f.DEFAULT_GROUP_FONT_SIZE;
          b.font = k + "px Arial";
          b.fillText(d.title, e[0] + 4, e[1] + k);
        }
      }
      b.restore();
    }
  };
  h.prototype.adjustNodesSize = function() {
    for (var a = this.graph._nodes, b = 0; b < a.length; ++b) {
      a[b].size = a[b].computeSize();
    }
    this.setDirty(!0, !0);
  };
  h.prototype.resize = function(a, b) {
    a || b || (b = this.canvas.parentNode, a = b.offsetWidth, b = b.offsetHeight);
    if (this.canvas.width != a || this.canvas.height != b) {
      this.canvas.width = a, this.canvas.height = b, this.bgcanvas.width = this.canvas.width, this.bgcanvas.height = this.canvas.height, this.setDirty(!0, !0);
    }
  };
  h.prototype.switchLiveMode = function(a) {
    if (a) {
      var b = this, c = this.live_mode ? 1.1 : 0.9;
      this.live_mode && (this.live_mode = !1, this.editor_alpha = 0.1);
      var d = setInterval(function() {
        b.editor_alpha *= c;
        b.dirty_canvas = !0;
        b.dirty_bgcanvas = !0;
        1 > c && 0.01 > b.editor_alpha && (clearInterval(d), 1 > c && (b.live_mode = !0));
        1 < c && 0.99 < b.editor_alpha && (clearInterval(d), b.editor_alpha = 1);
      }, 1);
    } else {
      this.live_mode = !this.live_mode, this.dirty_bgcanvas = this.dirty_canvas = !0;
    }
  };
  h.prototype.onNodeSelectionChange = function(a) {
  };
  h.prototype.touchHandler = function(a) {
    var b = a.changedTouches[0];
    switch(a.type) {
      case "touchstart":
        var c = "mousedown";
        break;
      case "touchmove":
        c = "mousemove";
        break;
      case "touchend":
        c = "mouseup";
        break;
      default:
        return;
    }
    var d = this.getCanvasWindow(), e = d.document.createEvent("MouseEvent");
    e.initMouseEvent(c, !0, !0, d, 1, b.screenX, b.screenY, b.clientX, b.clientY, !1, !1, !1, !1, 0, null);
    b.target.dispatchEvent(e);
    a.preventDefault();
  };
  h.onGroupAdd = function(a, b, c) {
    a = h.active_canvas;
    a.getCanvasWindow();
    b = new f.LGraphGroup;
    b.pos = a.convertEventToCanvasOffset(c);
    a.graph.add(b);
  };
  h.onMenuAdd = function(a, b, c, d, e) {
    function k(a, b) {
      b = d.getFirstEvent();
      g.graph.beforeChange();
      if (a = f.createNode(a.value)) {
        a.pos = g.convertEventToCanvasOffset(b), g.graph.add(a);
      }
      e && e(a);
      g.graph.afterChange();
    }
    var g = h.active_canvas, p = g.getCanvasWindow(), l = g.graph;
    if (l) {
      a = f.getNodeTypesCategories(g.filter || l.filter);
      b = [];
      for (var t = 0; t < a.length; t++) {
        if (a[t]) {
          var q = a[t];
          -1 != q.indexOf("::") && (q = q.split("::")[1]);
          b.push({value:a[t], content:q, has_submenu:!0});
        }
      }
      var m = new f.ContextMenu(b, {event:c, callback:function(a, b, c) {
        a = f.getNodeTypesInCategory(a.value, g.filter || l.filter);
        b = [];
        for (var d = 0; d < a.length; d++) {
          a[d].skip_list || b.push({content:a[d].title, value:a[d].type});
        }
        new f.ContextMenu(b, {event:c, callback:k, parentMenu:m}, p);
        return !1;
      }, parentMenu:d}, p);
      return !1;
    }
  };
  h.onMenuCollapseAll = function() {
  };
  h.onMenuNodeEdit = function() {
  };
  h.showMenuNodeOptionalInputs = function(a, b, c, d, e) {
    if (e) {
      var k = this;
      a = h.active_canvas.getCanvasWindow();
      b = e.optional_inputs;
      e.onGetInputs && (b = e.onGetInputs());
      var g = [];
      if (b) {
        for (var p = 0; p < b.length; p++) {
          var l = b[p];
          if (l) {
            var t = l[0];
            l[2] && l[2].label && (t = l[2].label);
            t = {content:t, value:l};
            l[1] == f.ACTION && (t.className = "event");
            g.push(t);
          } else {
            g.push(null);
          }
        }
      }
      this.onMenuNodeInputs && (g = this.onMenuNodeInputs(g));
      if (g.length) {
        return new f.ContextMenu(g, {event:c, callback:function(a, b, c) {
          e && (a.callback && a.callback.call(k, e, a, b, c), a.value && (e.graph.beforeChange(), e.addInput(a.value[0], a.value[1], a.value[2]), e.setDirtyCanvas(!0, !0), e.graph.afterChange()));
        }, parentMenu:d, node:e}, a), !1;
      }
      console.log("no input entries");
    }
  };
  h.showMenuNodeOptionalOutputs = function(a, b, c, d, e) {
    function k(a, b, c) {
      if (e && (a.callback && a.callback.call(g, e, a, b, c), a.value)) {
        if (c = a.value[1], !c || c.constructor !== Object && c.constructor !== Array) {
          e.graph.beforeChange(), e.addOutput(a.value[0], a.value[1], a.value[2]), e.setDirtyCanvas(!0, !0), e.graph.afterChange();
        } else {
          a = [];
          for (var h in c) {
            a.push({content:h, value:c[h]});
          }
          new f.ContextMenu(a, {event:b, callback:k, parentMenu:d, node:e});
          return !1;
        }
      }
    }
    if (e) {
      var g = this;
      a = h.active_canvas.getCanvasWindow();
      b = e.optional_outputs;
      e.onGetOutputs && (b = e.onGetOutputs());
      var p = [];
      if (b) {
        for (var l = 0; l < b.length; l++) {
          var t = b[l];
          if (!t) {
            p.push(null);
          } else {
            if (!e.flags || !e.flags.skip_repeated_outputs || -1 == e.findOutputSlot(t[0])) {
              var q = t[0];
              t[2] && t[2].label && (q = t[2].label);
              q = {content:q, value:t};
              t[1] == f.EVENT && (q.className = "event");
              p.push(q);
            }
          }
        }
      }
      this.onMenuNodeOutputs && (p = this.onMenuNodeOutputs(p));
      if (p.length) {
        return new f.ContextMenu(p, {event:c, callback:k, parentMenu:d, node:e}, a), !1;
      }
    }
  };
  h.onShowMenuNodeProperties = function(a, b, c, d, e) {
    if (e && e.properties) {
      var k = h.active_canvas;
      b = k.getCanvasWindow();
      var g = [], p;
      for (p in e.properties) {
        a = void 0 !== e.properties[p] ? e.properties[p] : " ";
        "object" == typeof a && (a = JSON.stringify(a));
        var l = e.getPropertyInfo(p);
        if ("enum" == l.type || "combo" == l.type) {
          a = h.getPropertyPrintableValue(a, l.values);
        }
        a = h.decodeHTML(a);
        g.push({content:"<span class='property_name'>" + p + "</span><span class='property_value'>" + a + "</span>", value:p});
      }
      if (g.length) {
        return new f.ContextMenu(g, {event:c, callback:function(a, b, c, d) {
          e && (b = this.getBoundingClientRect(), k.showEditPropertyValue(e, a.value, {position:[b.left, b.top]}));
        }, parentMenu:d, allow_html:!0, node:e}, b), !1;
      }
    }
  };
  h.decodeHTML = function(a) {
    var b = document.createElement("div");
    b.innerText = a;
    return b.innerHTML;
  };
  h.onResizeNode = function(a, b, c, d, e) {
    if (e) {
      e.size = e.computeSize();
      if (e.onResize) {
        e.onResize(e.size);
      }
      e.setDirtyCanvas(!0, !0);
    }
  };
  h.prototype.showLinkMenu = function(a, b) {
    var c = this;
    console.log(a);
    var d = new f.ContextMenu(["Add Node", null, "Delete"], {event:b, title:null != a.data ? a.data.constructor.name : null, callback:function(b, k, g) {
      switch(b) {
        case "Add Node":
          h.onMenuAdd(null, null, g, d, function(b) {
            console.log("node autoconnect");
            var d = c.graph.getNodeById(a.origin_id), e = c.graph.getNodeById(a.target_id);
            b.inputs && b.inputs.length && b.outputs && b.outputs.length && d.outputs[a.origin_slot].type == b.inputs[0].type && b.outputs[0].type == e.inputs[0].type && (d.connect(a.origin_slot, b, 0), b.connect(0, e, a.target_slot), b.pos[0] -= 0.5 * b.size[0]);
          });
          break;
        case "Delete":
          c.graph.removeLink(a.id);
      }
    }});
    return !1;
  };
  h.onShowPropertyEditor = function(a, b, c, d, e) {
    function k() {
      var b = l.value;
      "Number" == a.type ? b = Number(b) : "Boolean" == a.type && (b = !!b);
      e[g] = b;
      f.parentNode && f.parentNode.removeChild(f);
      e.setDirtyCanvas(!0, !0);
    }
    var g = a.property || "title";
    b = e[g];
    var f = document.createElement("div");
    f.className = "graphdialog";
    f.innerHTML = "<span class='name'></span><input autofocus type='text' class='value'/><button>OK</button>";
    f.querySelector(".name").innerText = g;
    var l = f.querySelector("input");
    l && (l.value = b, l.addEventListener("blur", function(a) {
      this.focus();
    }), l.addEventListener("keydown", function(a) {
      13 == a.keyCode && (k(), a.preventDefault(), a.stopPropagation());
    }));
    b = h.active_canvas.canvas;
    c = b.getBoundingClientRect();
    var t = d = -20;
    c && (d -= c.left, t -= c.top);
    event ? (f.style.left = event.clientX + d + "px", f.style.top = event.clientY + t + "px") : (f.style.left = 0.5 * b.width + d + "px", f.style.top = 0.5 * b.height + t + "px");
    f.querySelector("button").addEventListener("click", k);
    b.parentNode.appendChild(f);
  };
  h.prototype.prompt = function(a, b, c, d) {
    var e = this;
    a = a || "";
    var f = !1, g = document.createElement("div");
    g.className = "graphdialog rounded";
    g.innerHTML = "<span class='name'></span> <input autofocus type='text' class='value'/><button class='rounded'>OK</button>";
    g.close = function() {
      e.prompt_box = null;
      g.parentNode && g.parentNode.removeChild(g);
    };
    1 < this.ds.scale && (g.style.transform = "scale(" + this.ds.scale + ")");
    g.addEventListener("mouseleave", function(a) {
      f || g.close();
    });
    e.prompt_box && e.prompt_box.close();
    e.prompt_box = g;
    g.querySelector(".name").innerText = a;
    g.querySelector(".value").value = b;
    var l = g.querySelector("input");
    l.addEventListener("keydown", function(a) {
      f = !0;
      if (27 == a.keyCode) {
        g.close();
      } else {
        if (13 == a.keyCode) {
          c && c(this.value), g.close();
        } else {
          return;
        }
      }
      a.preventDefault();
      a.stopPropagation();
    });
    g.querySelector("button").addEventListener("click", function(a) {
      c && c(l.value);
      e.setDirty(!0);
      g.close();
    });
    a = h.active_canvas.canvas;
    b = a.getBoundingClientRect();
    var n = -20, t = -20;
    b && (n -= b.left, t -= b.top);
    d ? (g.style.left = d.clientX + n + "px", g.style.top = d.clientY + t + "px") : (g.style.left = 0.5 * a.width + n + "px", g.style.top = 0.5 * a.height + t + "px");
    a.parentNode.appendChild(g);
    setTimeout(function() {
      l.focus();
    }, 10);
    return g;
  };
  h.search_limit = -1;
  h.prototype.showSearchBox = function(a) {
    function b(b) {
      if (b) {
        if (e.onSearchBoxSelection) {
          e.onSearchBoxSelection(b, a, k);
        } else {
          var c = f.searchbox_extras[b.toLowerCase()];
          c && (b = c.type);
          k.graph.beforeChange();
          if (b = f.createNode(b)) {
            b.pos = k.convertEventToCanvasOffset(a), k.graph.add(b);
          }
          if (c && c.data) {
            if (c.data.properties) {
              for (var d in c.data.properties) {
                b.addProperty(d, c.data.properties[d]);
              }
            }
            if (c.data.inputs) {
              for (d in b.inputs = [], c.data.inputs) {
                b.addOutput(c.data.inputs[d][0], c.data.inputs[d][1]);
              }
            }
            if (c.data.outputs) {
              for (d in b.outputs = [], c.data.outputs) {
                b.addOutput(c.data.outputs[d][0], c.data.outputs[d][1]);
              }
            }
            c.data.title && (b.title = c.data.title);
            c.data.json && b.configure(c.data.json);
            k.graph.afterChange();
          }
        }
      }
      n.close();
    }
    function c(a) {
      var b = v;
      v && v.classList.remove("selected");
      v ? (v = a ? v.nextSibling : v.previousSibling) || (v = b) : v = a ? q.childNodes[0] : q.childNodes[q.childNodes.length];
      v && (v.classList.add("selected"), v.scrollIntoView({block:"end", behavior:"smooth"}));
    }
    function d() {
      function a(a, c) {
        var d = document.createElement("div");
        m || (m = a);
        d.innerText = a;
        d.dataset.type = escape(a);
        d.className = "litegraph lite-search-item";
        c && (d.className += " " + c);
        d.addEventListener("click", function(a) {
          b(unescape(this.dataset.type));
        });
        q.appendChild(d);
      }
      r = null;
      var c = u.value;
      m = null;
      q.innerHTML = "";
      if (c) {
        if (e.onSearchBox) {
          var d = e.onSearchBox(q, c, k);
          if (d) {
            for (var g = 0; g < d.length; ++g) {
              a(d[g]);
            }
          }
        } else {
          d = function(a) {
            var b = f.registered_node_types[a];
            return p && b.filter != p ? !1 : -1 !== a.toLowerCase().indexOf(c);
          };
          var l = 0;
          c = c.toLowerCase();
          var p = k.filter || k.graph.filter;
          for (g in f.searchbox_extras) {
            var n = f.searchbox_extras[g];
            if (-1 !== n.desc.toLowerCase().indexOf(c)) {
              var t = f.registered_node_types[n.type];
              if (!t || t.filter == p) {
                if (a(n.desc, "searchbox_extra"), -1 !== h.search_limit && l++ > h.search_limit) {
                  break;
                }
              }
            }
          }
          n = null;
          if (Array.prototype.filter) {
            n = Object.keys(f.registered_node_types).filter(d);
          } else {
            for (g in n = [], f.registered_node_types) {
              d(g) && n.push(g);
            }
          }
          for (g = 0; g < n.length && !(a(n[g]), -1 !== h.search_limit && l++ > h.search_limit); g++) {
          }
        }
      }
    }
    var e = this, k = h.active_canvas, g = k.canvas, l = g.ownerDocument || document, n = document.createElement("div");
    n.className = "litegraph litesearchbox graphdialog rounded";
    n.innerHTML = "<span class='name'>Search</span> <input autofocus type='text' class='value rounded'/><div class='helper'></div>";
    n.close = function() {
      e.search_box = null;
      l.body.focus();
      l.body.style.overflow = "";
      setTimeout(function() {
        e.canvas.focus();
      }, 20);
      n.parentNode && n.parentNode.removeChild(n);
    };
    var t = null;
    1 < this.ds.scale && (n.style.transform = "scale(" + this.ds.scale + ")");
    n.addEventListener("mouseenter", function(a) {
      t && (clearTimeout(t), t = null);
    });
    n.addEventListener("mouseleave", function(a) {
      t = setTimeout(function() {
        n.close();
      }, 500);
    });
    e.search_box && e.search_box.close();
    e.search_box = n;
    var q = n.querySelector(".helper"), m = null, r = null, v = null, u = n.querySelector("input");
    u && (u.addEventListener("blur", function(a) {
      this.focus();
    }), u.addEventListener("keydown", function(a) {
      if (38 == a.keyCode) {
        c(!1);
      } else {
        if (40 == a.keyCode) {
          c(!0);
        } else {
          if (27 == a.keyCode) {
            n.close();
          } else {
            if (13 == a.keyCode) {
              v ? b(v.innerHTML) : m ? b(m) : n.close();
            } else {
              r && clearInterval(r);
              r = setTimeout(d, 10);
              return;
            }
          }
        }
      }
      a.preventDefault();
      a.stopPropagation();
      a.stopImmediatePropagation();
      return !0;
    }));
    l.fullscreenElement ? l.fullscreenElement.appendChild(n) : (l.body.appendChild(n), l.body.style.overflow = "hidden");
    g = g.getBoundingClientRect();
    var w = (a ? a.clientY : g.top + 0.5 * g.height) - 20;
    n.style.left = (a ? a.clientX : g.left + 0.5 * g.width) - 80 + "px";
    n.style.top = w + "px";
    a.layerY > g.height - 200 && (q.style.maxHeight = g.height - a.layerY - 20 + "px");
    u.focus();
    return n;
  };
  h.prototype.showEditPropertyValue = function(a, b, c) {
    function d() {
      e(m.value);
    }
    function e(d) {
      f && f.values && f.values.constructor === Object && void 0 != f.values[d] && (d = f.values[d]);
      "number" == typeof a.properties[b] && (d = Number(d));
      if ("array" == g || "object" == g) {
        d = JSON.parse(d);
      }
      a.properties[b] = d;
      a.graph && a.graph._version++;
      if (a.onPropertyChanged) {
        a.onPropertyChanged(b, d);
      }
      if (c.onclose) {
        c.onclose();
      }
      q.close();
      a.setDirtyCanvas(!0, !0);
    }
    if (a && void 0 !== a.properties[b]) {
      c = c || {};
      var f = a.getPropertyInfo(b), g = f.type, h = "";
      if ("string" == g || "number" == g || "array" == g || "object" == g) {
        h = "<input autofocus type='text' class='value'/>";
      } else {
        if ("enum" != g && "combo" != g || !f.values) {
          if ("boolean" == g) {
            h = "<input autofocus type='checkbox' class='value' " + (a.properties[b] ? "checked" : "") + "/>";
          } else {
            console.warn("unknown type: " + g);
            return;
          }
        } else {
          h = "<select autofocus type='text' class='value'>";
          for (var l in f.values) {
            var n = l;
            f.values.constructor === Array && (n = f.values[l]);
            h += "<option value='" + n + "' " + (n == a.properties[b] ? "selected" : "") + ">" + f.values[l] + "</option>";
          }
          h += "</select>";
        }
      }
      var q = this.createDialog("<span class='name'>" + b + "</span>" + h + "<button>OK</button>", c);
      if ("enum" != g && "combo" != g || !f.values) {
        if ("boolean" == g) {
          (m = q.querySelector("input")) && m.addEventListener("click", function(a) {
            e(!!m.checked);
          });
        } else {
          if (m = q.querySelector("input")) {
            m.addEventListener("blur", function(a) {
              this.focus();
            }), n = void 0 !== a.properties[b] ? a.properties[b] : "", n = JSON.stringify(n), m.value = n, m.addEventListener("keydown", function(a) {
              13 == a.keyCode && (d(), a.preventDefault(), a.stopPropagation());
            });
          }
        }
      } else {
        var m = q.querySelector("select");
        m.addEventListener("change", function(a) {
          e(a.target.value);
        });
      }
      q.querySelector("button").addEventListener("click", d);
      return q;
    }
  };
  h.prototype.createDialog = function(a, b) {
    b = b || {};
    var c = document.createElement("div");
    c.className = "graphdialog";
    c.innerHTML = a;
    a = this.canvas.getBoundingClientRect();
    var d = -20, e = -20;
    a && (d -= a.left, e -= a.top);
    b.position ? (d += b.position[0], e += b.position[1]) : b.event ? (d += b.event.clientX, e += b.event.clientY) : (d += 0.5 * this.canvas.width, e += 0.5 * this.canvas.height);
    c.style.left = d + "px";
    c.style.top = e + "px";
    this.canvas.parentNode.appendChild(c);
    c.close = function() {
      this.parentNode && this.parentNode.removeChild(this);
    };
    return c;
  };
  h.prototype.createPanel = function(a, b) {
    b = b || {};
    var c = b.window || window, d = document.createElement("div");
    d.className = "litegraph dialog";
    d.innerHTML = "<div class='dialog-header'><span class='dialog-title'></span></div><div class='dialog-content'></div><div class='dialog-footer'></div>";
    d.header = d.querySelector(".dialog-header");
    b.width && (d.style.width = b.width + (b.width.constructor === Number ? "px" : ""));
    b.height && (d.style.height = b.height + (b.height.constructor === Number ? "px" : ""));
    b.closable && (b = document.createElement("span"), b.innerHTML = "&#10005;", b.classList.add("close"), b.addEventListener("click", function() {
      d.close();
    }), d.header.appendChild(b));
    d.title_element = d.querySelector(".dialog-title");
    d.title_element.innerText = a;
    d.content = d.querySelector(".dialog-content");
    d.footer = d.querySelector(".dialog-footer");
    d.close = function() {
      this.parentNode.removeChild(this);
    };
    d.clear = function() {
      this.content.innerHTML = "";
    };
    d.addHTML = function(a, b, c) {
      var e = document.createElement("div");
      b && (e.className = b);
      e.innerHTML = a;
      c ? d.footer.appendChild(e) : d.content.appendChild(e);
      return e;
    };
    d.addButton = function(a, b, c) {
      var e = document.createElement("button");
      e.innerText = a;
      e.options = c;
      e.classList.add("btn");
      e.addEventListener("click", b);
      d.footer.appendChild(e);
      return e;
    };
    d.addSeparator = function() {
      var a = document.createElement("div");
      a.className = "separator";
      d.content.appendChild(a);
    };
    d.addWidget = function(a, b, g, l, n) {
      function e(a, b) {
        console.log("change", a, b);
        l.callback && l.callback(a, b);
        n && n(a, b);
      }
      l = l || {};
      var k = String(g);
      a = a.toLowerCase();
      "number" == a && (k = g.toFixed(3));
      var m = document.createElement("div");
      m.className = "property";
      m.innerHTML = "<span class='property_name'></span><span class='property_value'></span>";
      m.querySelector(".property_name").innerText = b;
      var p = m.querySelector(".property_value");
      p.innerText = k;
      m.dataset.property = b;
      m.dataset.type = l.type || a;
      m.options = l;
      m.value = g;
      if ("boolean" == a) {
        m.classList.add("boolean"), g && m.classList.add("bool-on"), m.addEventListener("click", function() {
          var a = this.dataset.property;
          this.value = !this.value;
          this.classList.toggle("bool-on");
          this.querySelector(".property_value").innerText = this.value ? "true" : "false";
          e(a, this.value);
        });
      } else {
        if ("string" == a || "number" == a) {
          p.setAttribute("contenteditable", !0), p.addEventListener("keydown", function(a) {
            "Enter" == a.code && (a.preventDefault(), this.blur());
          }), p.addEventListener("blur", function() {
            var a = this.innerText, b = this.parentNode.dataset.property;
            "number" == this.parentNode.dataset.type && (a = Number(a));
            e(b, a);
          });
        } else {
          if ("enum" == a || "combo" == a) {
            k = h.getPropertyPrintableValue(g, l.values);
          }
        }
      }
      p.innerText = k;
      p.addEventListener("click", function(a) {
        var b = this.parentNode.dataset.property, d = this;
        new f.ContextMenu(l.values || [], {event:a, className:"dark", callback:function(a, c, g) {
          d.innerText = a;
          e(b, a);
          return !1;
        }}, c);
      });
      d.content.appendChild(m);
      return m;
    };
    return d;
  };
  h.getPropertyPrintableValue = function(a, b) {
    if (!b || b.constructor === Array) {
      return String(a);
    }
    if (b.constructor === Object) {
      var c = "", d;
      for (d in b) {
        if (b[d] == a) {
          c = d;
          break;
        }
      }
      return String(a) + " (" + c + ")";
    }
  };
  h.prototype.showShowNodePanel = function(a) {
    window.SELECTED_NODE = a;
    var b = document.querySelector("#node-panel");
    b && b.close();
    var c = this.getCanvasWindow();
    b = this.createPanel(a.title || "", {closable:!0, window:c});
    b.id = "node-panel";
    b.node = a;
    b.classList.add("settings");
    var d = this;
    (function() {
      b.content.innerHTML = "";
      b.addHTML("<span class='node_type'>" + a.type + "</span><span class='node_desc'>" + (a.constructor.desc || "") + "</span><span class='separator'></span>");
      b.addHTML("<h3>Properties</h3>");
      for (var c in a.properties) {
        var f = a.properties[c], g = a.getPropertyInfo(c);
        a.onAddPropertyToPanel && a.onAddPropertyToPanel(c, b) || b.addWidget(g.widget || g.type, c, f, g, function(b, c) {
          d.graph.beforeChange(a);
          a.setProperty(b, c);
          d.graph.afterChange();
          d.dirty_canvas = !0;
        });
      }
      b.addSeparator();
      if (a.onShowCustomPanelInfo) {
        a.onShowCustomPanelInfo(b);
      }
      b.addButton("Delete", function() {
        a.block_delete || (a.graph.remove(a), b.close());
      }).classList.add("delete");
    })();
    this.canvas.parentNode.appendChild(b);
  };
  h.prototype.showSubgraphPropertiesDialog = function(a) {
    function b() {
      d.clear();
      if (a.inputs) {
        for (var c = 0; c < a.inputs.length; ++c) {
          var f = a.inputs[c];
          if (!f.not_subgraph_input) {
            var g = d.addHTML("<button>&#10005;</button> <span class='bullet_icon'></span><span class='name'></span><span class='type'></span>", "subgraph_property");
            g.dataset.name = f.name;
            g.dataset.slot = c;
            g.querySelector(".name").innerText = f.name;
            g.querySelector(".type").innerText = f.type;
            g.querySelector("button").addEventListener("click", function(c) {
              a.removeInput(Number(this.parentNode.dataset.slot));
              b();
            });
          }
        }
      }
    }
    console.log("showing subgraph properties dialog");
    var c = this.canvas.parentNode.querySelector(".subgraph_dialog");
    c && c.close();
    var d = this.createPanel("Subgraph Inputs", {closable:!0, width:500});
    d.node = a;
    d.classList.add("subgraph_dialog");
    d.addHTML(" + <span class='label'>Name</span><input class='name'/><span class='label'>Type</span><input class='type'></input><button>+</button>", "subgraph_property extra", !0).querySelector("button").addEventListener("click", function(c) {
      c = this.parentNode;
      var d = c.querySelector(".name").value, e = c.querySelector(".type").value;
      d && -1 == a.findInputSlot(d) && (a.addInput(d, e), c.querySelector(".name").value = "", c.querySelector(".type").value = "", b());
    });
    b();
    this.canvas.parentNode.appendChild(d);
    return d;
  };
  h.prototype.checkPanels = function() {
    if (this.canvas) {
      for (var a = this.canvas.parentNode.querySelectorAll(".litegraph.dialog"), b = 0; b < a.length; ++b) {
        var c = a[b];
        c.node && (c.node.graph && c.graph == this.graph || c.close());
      }
    }
  };
  h.onMenuNodeCollapse = function(a, b, c, d, e) {
    e.graph.beforeChange(e);
    e.collapse();
    e.graph.afterChange(e);
  };
  h.onMenuNodePin = function(a, b, c, d, e) {
    e.pin();
  };
  h.onMenuNodeMode = function(a, b, c, d, e) {
    new f.ContextMenu(["Always", "On Event", "On Trigger", "Never"], {event:c, callback:function(a) {
      if (e) {
        switch(a) {
          case "On Event":
            e.mode = f.ON_EVENT;
            break;
          case "On Trigger":
            e.mode = f.ON_TRIGGER;
            break;
          case "Never":
            e.mode = f.NEVER;
            break;
          default:
            e.mode = f.ALWAYS;
        }
      }
    }, parentMenu:d, node:e});
    return !1;
  };
  h.onMenuNodeColors = function(a, b, c, d, e) {
    if (!e) {
      throw "no node for color";
    }
    b = [];
    b.push({value:null, content:"<span style='display: block; padding-left: 4px;'>No color</span>"});
    for (var k in h.node_colors) {
      a = h.node_colors[k], a = {value:k, content:"<span style='display: block; color: #999; padding-left: 4px; border-left: 8px solid " + a.color + "; background-color:" + a.bgcolor + "'>" + k + "</span>"}, b.push(a);
    }
    new f.ContextMenu(b, {event:c, callback:function(a) {
      e && ((a = a.value ? h.node_colors[a.value] : null) ? e.constructor === f.LGraphGroup ? e.color = a.groupcolor : (e.color = a.color, e.bgcolor = a.bgcolor) : (delete e.color, delete e.bgcolor), e.setDirtyCanvas(!0, !0));
    }, parentMenu:d, node:e});
    return !1;
  };
  h.onMenuNodeShapes = function(a, b, c, d, e) {
    if (!e) {
      throw "no node passed";
    }
    new f.ContextMenu(f.VALID_SHAPES, {event:c, callback:function(a) {
      e && (e.graph.beforeChange(e), e.shape = a, e.graph.afterChange(e), e.setDirtyCanvas(!0));
    }, parentMenu:d, node:e});
    return !1;
  };
  h.onMenuNodeRemove = function(a, b, c, d, e) {
    if (!e) {
      throw "no node passed";
    }
    !1 !== e.removable && (a = e.graph, a.beforeChange(), a.remove(e), a.afterChange(), e.setDirtyCanvas(!0, !0));
  };
  h.onMenuNodeToSubgraph = function(a, b, c, d, e) {
    a = e.graph;
    if (b = h.active_canvas) {
      c = Object.values(b.selected_nodes || {}), c.length || (c = [e]), d = f.createNode("graph/subgraph"), d.pos = e.pos.concat(), a.add(d), d.buildFromNodes(c), b.deselectAllNodes(), e.setDirtyCanvas(!0, !0);
    }
  };
  h.onMenuNodeClone = function(a, b, c, d, e) {
    0 != e.clonable && (a = e.clone()) && (a.pos = [e.pos[0] + 5, e.pos[1] + 5], e.graph.beforeChange(), e.graph.add(a), e.graph.afterChange(), e.setDirtyCanvas(!0, !0));
  };
  h.node_colors = {red:{color:"#322", bgcolor:"#533", groupcolor:"#A88"}, brown:{color:"#332922", bgcolor:"#593930", groupcolor:"#b06634"}, green:{color:"#232", bgcolor:"#353", groupcolor:"#8A8"}, blue:{color:"#223", bgcolor:"#335", groupcolor:"#88A"}, pale_blue:{color:"#2a363b", bgcolor:"#3f5159", groupcolor:"#3f789e"}, cyan:{color:"#233", bgcolor:"#355", groupcolor:"#8AA"}, purple:{color:"#323", bgcolor:"#535", groupcolor:"#a1309b"}, yellow:{color:"#432", bgcolor:"#653", groupcolor:"#b58b2a"}, 
  black:{color:"#222", bgcolor:"#000", groupcolor:"#444"}};
  h.prototype.getCanvasMenuOptions = function() {
    if (this.getMenuOptions) {
      var a = this.getMenuOptions();
    } else {
      a = [{content:"Add Node", has_submenu:!0, callback:h.onMenuAdd}, {content:"Add Group", callback:h.onGroupAdd}], this._graph_stack && 0 < this._graph_stack.length && a.push(null, {content:"Close subgraph", callback:this.closeSubgraph.bind(this)});
    }
    if (this.getExtraMenuOptions) {
      var b = this.getExtraMenuOptions(this, a);
      b && (a = a.concat(b));
    }
    return a;
  };
  h.prototype.getNodeMenuOptions = function(a) {
    var b = null;
    b = a.getMenuOptions ? a.getMenuOptions(this) : [{content:"Inputs", has_submenu:!0, disabled:!0, callback:h.showMenuNodeOptionalInputs}, {content:"Outputs", has_submenu:!0, disabled:!0, callback:h.showMenuNodeOptionalOutputs}, null, {content:"Properties", has_submenu:!0, callback:h.onShowMenuNodeProperties}, null, {content:"Title", callback:h.onShowPropertyEditor}, {content:"Mode", has_submenu:!0, callback:h.onMenuNodeMode}, {content:"Resize", callback:function() {
      if (a.resizable) {
        return h.onResizeNode;
      }
    }}, {content:"Collapse", callback:h.onMenuNodeCollapse}, {content:"Pin", callback:h.onMenuNodePin}, {content:"Colors", has_submenu:!0, callback:h.onMenuNodeColors}, {content:"Shapes", has_submenu:!0, callback:h.onMenuNodeShapes}, null];
    if (a.onGetInputs) {
      var c = a.onGetInputs();
      c && c.length && (b[0].disabled = !1);
    }
    a.onGetOutputs && (c = a.onGetOutputs()) && c.length && (b[1].disabled = !1);
    a.getExtraMenuOptions && (c = a.getExtraMenuOptions(this, b)) && (c.push(null), b = c.concat(b));
    !1 !== a.clonable && b.push({content:"Clone", callback:h.onMenuNodeClone});
    b.push(null, {content:"Remove", disabled:!(!1 !== a.removable && !a.block_delete), callback:h.onMenuNodeRemove});
    if (a.graph && a.graph.onGetNodeMenuOptions) {
      a.graph.onGetNodeMenuOptions(b, a);
    }
    return b;
  };
  h.prototype.getGroupMenuOptions = function(a) {
    return [{content:"Title", callback:h.onShowPropertyEditor}, {content:"Color", has_submenu:!0, callback:h.onMenuNodeColors}, {content:"Font size", property:"font_size", type:"Number", callback:h.onShowPropertyEditor}, null, {content:"Remove", callback:h.onMenuNodeRemove}];
  };
  h.prototype.processContextMenu = function(a, b) {
    var c = this, d = h.active_canvas.getCanvasWindow(), e = null, k = {event:b, callback:function(b, d, e) {
      if (b) {
        if ("Remove Slot" == b.content) {
          b = b.slot, b.input ? a.removeInput(b.slot) : b.output && a.removeOutput(b.slot);
        } else {
          if ("Disconnect Links" == b.content) {
            b = b.slot, b.output ? a.disconnectOutput(b.slot) : b.input && a.disconnectInput(b.slot);
          } else {
            if ("Rename Slot" == b.content) {
              b = b.slot;
              var g = b.input ? a.getInputInfo(b.slot) : a.getOutputInfo(b.slot), f = c.createDialog("<span class='name'>Name</span><input autofocus type='text'/><button>OK</button>", d), k = f.querySelector("input");
              k && g && (k.value = g.label || "");
              f.querySelector("button").addEventListener("click", function(a) {
                k.value && (g && (g.label = k.value), c.setDirty(!0));
                f.close();
              });
            }
          }
        }
      }
    }, extra:a};
    a && (k.title = a.type);
    var g = null;
    a && (g = a.getSlotInPosition(b.canvasX, b.canvasY), h.active_node = a);
    g ? (e = [], a.getSlotMenuOptions ? e = a.getSlotMenuOptions(g) : (g && g.output && g.output.links && g.output.links.length && e.push({content:"Disconnect Links", slot:g}), b = g.input || g.output, e.push(b.locked ? "Cannot remove" : {content:"Remove Slot", slot:g}), e.push(b.nameLocked ? "Cannot rename" : {content:"Rename Slot", slot:g})), k.title = (g.input ? g.input.type : g.output.type) || "*", g.input && g.input.type == f.ACTION && (k.title = "Action"), g.output && g.output.type == f.EVENT && 
    (k.title = "Event")) : a ? e = this.getNodeMenuOptions(a) : (e = this.getCanvasMenuOptions(), (g = this.graph.getGroupOnPos(b.canvasX, b.canvasY)) && e.push(null, {content:"Edit Group", has_submenu:!0, submenu:{title:"Group", extra:g, options:this.getGroupMenuOptions(g)}}));
    e && new f.ContextMenu(e, k, d);
  };
  "undefined" != typeof window && window.CanvasRenderingContext2D && (window.CanvasRenderingContext2D.prototype.roundRect = function(a, b, c, d, e, f) {
    void 0 === e && (e = 5);
    void 0 === f && (f = e);
    this.moveTo(a + e, b);
    this.lineTo(a + c - e, b);
    this.quadraticCurveTo(a + c, b, a + c, b + e);
    this.lineTo(a + c, b + d - f);
    this.quadraticCurveTo(a + c, b + d, a + c - f, b + d);
    this.lineTo(a + f, b + d);
    this.quadraticCurveTo(a, b + d, a, b + d - f);
    this.lineTo(a, b + e);
    this.quadraticCurveTo(a, b, a + e, b);
  });
  f.compareObjects = function(a, b) {
    for (var c in a) {
      if (a[c] != b[c]) {
        return !1;
      }
    }
    return !0;
  };
  f.distance = K;
  f.colorToString = function(a) {
    return "rgba(" + Math.round(255 * a[0]).toFixed() + "," + Math.round(255 * a[1]).toFixed() + "," + Math.round(255 * a[2]).toFixed() + "," + (4 == a.length ? a[3].toFixed(2) : "1.0") + ")";
  };
  f.isInsideRectangle = C;
  f.growBounding = function(a, b, c) {
    b < a[0] ? a[0] = b : b > a[2] && (a[2] = b);
    c < a[1] ? a[1] = c : c > a[3] && (a[3] = c);
  };
  f.isInsideBounding = function(a, b) {
    return a[0] < b[0][0] || a[1] < b[0][1] || a[0] > b[1][0] || a[1] > b[1][1] ? !1 : !0;
  };
  f.overlapBounding = F;
  f.hex2num = function(a) {
    "#" == a.charAt(0) && (a = a.slice(1));
    a = a.toUpperCase();
    for (var b = Array(3), c = 0, d, e, f = 0; 6 > f; f += 2) {
      d = "0123456789ABCDEF".indexOf(a.charAt(f)), e = "0123456789ABCDEF".indexOf(a.charAt(f + 1)), b[c] = 16 * d + e, c++;
    }
    return b;
  };
  f.num2hex = function(a) {
    for (var b = "#", c, d, e = 0; 3 > e; e++) {
      c = a[e] / 16, d = a[e] % 16, b += "0123456789ABCDEF".charAt(c) + "0123456789ABCDEF".charAt(d);
    }
    return b;
  };
  G.prototype.addItem = function(a, b, c) {
    function d(a) {
      var b = this.value;
      b && b.has_submenu && e.call(this, a);
    }
    function e(a) {
      var b = this.value, d = !0;
      f.current_submenu && f.current_submenu.close(a);
      if (c.callback) {
        var e = c.callback.call(this, b, c, a, f, c.node);
        !0 === e && (d = !1);
      }
      if (b && (b.callback && !c.ignore_item_callbacks && !0 !== b.disabled && (e = b.callback.call(this, b, c, a, f, c.extra), !0 === e && (d = !1)), b.submenu)) {
        if (!b.submenu.options) {
          throw "ContextMenu submenu needs options";
        }
        new f.constructor(b.submenu.options, {callback:b.submenu.callback, event:a, parentMenu:f, ignore_item_callbacks:b.submenu.ignore_item_callbacks, title:b.submenu.title, extra:b.submenu.extra, autoopen:c.autoopen});
        d = !1;
      }
      d && !f.lock && f.close();
    }
    var f = this;
    c = c || {};
    var g = document.createElement("div");
    g.className = "litemenu-entry submenu";
    var h = !1;
    if (null === b) {
      g.classList.add("separator");
    } else {
      g.innerHTML = b && b.title ? b.title : a;
      if (g.value = b) {
        b.disabled && (h = !0, g.classList.add("disabled")), (b.submenu || b.has_submenu) && g.classList.add("has_submenu");
      }
      "function" == typeof b ? (g.dataset.value = a, g.onclick_callback = b) : g.dataset.value = b;
      b.className && (g.className += " " + b.className);
    }
    this.root.appendChild(g);
    h || g.addEventListener("click", e);
    c.autoopen && g.addEventListener("mouseenter", d);
    return g;
  };
  G.prototype.close = function(a, b) {
    this.root.parentNode && this.root.parentNode.removeChild(this.root);
    this.parentMenu && !b && (this.parentMenu.lock = !1, this.parentMenu.current_submenu = null, void 0 === a ? this.parentMenu.close() : a && !G.isCursorOverElement(a, this.parentMenu.root) && G.trigger(this.parentMenu.root, "mouseleave", a));
    this.current_submenu && this.current_submenu.close(a, !0);
    this.root.closing_timer && clearTimeout(this.root.closing_timer);
  };
  G.trigger = function(a, b, c, d) {
    var e = document.createEvent("CustomEvent");
    e.initCustomEvent(b, !0, !0, c);
    e.srcElement = d;
    a.dispatchEvent ? a.dispatchEvent(e) : a.__events && a.__events.dispatchEvent(e);
    return e;
  };
  G.prototype.getTopMenu = function() {
    return this.options.parentMenu ? this.options.parentMenu.getTopMenu() : this;
  };
  G.prototype.getFirstEvent = function() {
    return this.options.parentMenu ? this.options.parentMenu.getFirstEvent() : this.options.event;
  };
  G.isCursorOverElement = function(a, b) {
    var c = a.clientX;
    a = a.clientY;
    return (b = b.getBoundingClientRect()) ? a > b.top && a < b.top + b.height && c > b.left && c < b.left + b.width ? !0 : !1 : !1;
  };
  f.ContextMenu = G;
  f.closeAllContextMenus = function(a) {
    a = a || window;
    a = a.document.querySelectorAll(".litecontextmenu");
    if (a.length) {
      for (var b = [], c = 0; c < a.length; c++) {
        b.push(a[c]);
      }
      for (c = 0; c < b.length; c++) {
        b[c].close ? b[c].close() : b[c].parentNode && b[c].parentNode.removeChild(b[c]);
      }
    }
  };
  f.extendClass = function(a, b) {
    for (var c in b) {
      a.hasOwnProperty(c) || (a[c] = b[c]);
    }
    if (b.prototype) {
      for (c in b.prototype) {
        b.prototype.hasOwnProperty(c) && !a.prototype.hasOwnProperty(c) && (b.prototype.__lookupGetter__(c) ? a.prototype.__defineGetter__(c, b.prototype.__lookupGetter__(c)) : a.prototype[c] = b.prototype[c], b.prototype.__lookupSetter__(c) && a.prototype.__defineSetter__(c, b.prototype.__lookupSetter__(c)));
      }
    }
  };
  I.sampleCurve = function(a, b) {
    if (b) {
      for (var c = 0; c < b.length - 1; ++c) {
        var d = b[c], e = b[c + 1];
        if (!(e[0] < a)) {
          b = e[0] - d[0];
          if (0.00001 > Math.abs(b)) {
            return d[1];
          }
          a = (a - d[0]) / b;
          return d[1] * (1.0 - a) + e[1] * a;
        }
      }
      return 0;
    }
  };
  I.prototype.draw = function(a, b, c, d, e, f) {
    if (c = this.points) {
      this.size = b;
      var g = b[0] - 2 * this.margin;
      b = b[1] - 2 * this.margin;
      e = e || "#666";
      a.save();
      a.translate(this.margin, this.margin);
      d && (a.fillStyle = "#111", a.fillRect(0, 0, g, b), a.fillStyle = "#222", a.fillRect(0.5 * g, 0, 1, b), a.strokeStyle = "#333", a.strokeRect(0, 0, g, b));
      a.strokeStyle = e;
      f && (a.globalAlpha = 0.5);
      a.beginPath();
      for (d = 0; d < c.length; ++d) {
        e = c[d], a.lineTo(e[0] * g, (1.0 - e[1]) * b);
      }
      a.stroke();
      a.globalAlpha = 1;
      if (!f) {
        for (d = 0; d < c.length; ++d) {
          e = c[d], a.fillStyle = this.selected == d ? "#FFF" : this.nearest == d ? "#DDD" : "#AAA", a.beginPath(), a.arc(e[0] * g, (1.0 - e[1]) * b, 2, 0, 2 * Math.PI), a.fill();
        }
      }
      a.restore();
    }
  };
  I.prototype.onMouseDown = function(a, b) {
    var c = this.points;
    if (c && !(0 > a[1])) {
      var d = this.size[0] - 2 * this.margin, e = this.size[1] - 2 * this.margin, f = a[0] - this.margin;
      a = a[1] - this.margin;
      this.selected = this.getCloserPoint([f, a], 30 / b.ds.scale);
      -1 == this.selected && (b = [f / d, 1 - a / e], c.push(b), c.sort(function(a, b) {
        return a[0] - b[0];
      }), this.selected = c.indexOf(b), this.must_update = !0);
      if (-1 != this.selected) {
        return !0;
      }
    }
  };
  I.prototype.onMouseMove = function(a, b) {
    var c = this.points;
    if (c) {
      var d = this.selected;
      if (!(0 > d)) {
        var e = (a[0] - this.margin) / (this.size[0] - 2 * this.margin), f = (a[1] - this.margin) / (this.size[1] - 2 * this.margin);
        this._nearest = this.getCloserPoint([a[0] - this.margin, a[1] - this.margin], 30 / b.ds.scale);
        if (b = c[d]) {
          var g = 0 == d || d == c.length - 1;
          !g && (-10 > a[0] || a[0] > this.size[0] + 10 || -10 > a[1] || a[1] > this.size[1] + 10) ? (c.splice(d, 1), this.selected = -1) : (b[0] = g ? 0 == d ? 0 : 1 : Math.clamp(e, 0, 1), b[1] = 1.0 - Math.clamp(f, 0, 1), c.sort(function(a, b) {
            return a[0] - b[0];
          }), this.selected = c.indexOf(b), this.must_update = !0);
        }
      }
    }
  };
  I.prototype.onMouseUp = function(a, b) {
    this.selected = -1;
    return !1;
  };
  I.prototype.getCloserPoint = function(a, b) {
    var c = this.points;
    if (!c) {
      return -1;
    }
    b = b || 30;
    for (var d = this.size[0] - 2 * this.margin, e = this.size[1] - 2 * this.margin, f = c.length, g = [0, 0], h = 1000000, l = -1, n = 0; n < f; ++n) {
      var q = c[n];
      g[0] = q[0] * d;
      g[1] = (1.0 - q[1]) * e;
      q = vec2.distance(a, g);
      q > h || q > b || (l = n, h = q);
    }
    return l;
  };
  f.CurveEditor = I;
  f.getParameterNames = function(a) {
    return (a + "").replace(/[/][/].*$/gm, "").replace(/\s+/g, "").replace(/[/][*][^/*]*[*][/]/g, "").split("){", 1)[0].replace(/^[^(]*[(]/, "").replace(/=[^,]+/g, "").split(",").filter(Boolean);
  };
  Math.clamp = function(a, b, c) {
    return b > a ? b : c < a ? c : a;
  };
  "undefined" == typeof window || window.requestAnimationFrame || (window.requestAnimationFrame = window.webkitRequestAnimationFrame || window.mozRequestAnimationFrame || function(a) {
    window.setTimeout(a, 1000 / 60);
  });
})(this);
"undefined" != typeof exports && (exports.LiteGraph = this.LiteGraph);

